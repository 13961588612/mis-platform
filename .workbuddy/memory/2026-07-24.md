# 2026-07-24 工作记录

## 阶段5 后端扩展 T-ext / T-sum / T-stream（增量 SOP，已收口）
- 用户拍板"开 (A)"：补齐阶段5 设计中预留的三个后端扩展点。团队 `software-ai-backend-ext`（PM 许清楚 / 架构师高见远 / 工程师寇豆码 / QA 严过关）。
- 增量 PRD：`docs/backend-ext-prd.md`（许清楚）；增量设计+任务分解：`docs/backend-ext-design.md` + `docs/backend-ext-class.mermaid` + `docs/backend-ext-sequence.mermaid`（高见远）。
- 三层实现（寇豆码，IS_PASS=YES）+ QA 严过关验证判定 **NoOne**：
  - **平台 Python（可 pytest 实跑，170 passed）**：`mis-extract/runtime/prompts/system.md` 改为要求 `confidence:{key:0~1}` + `unmapped:[{raw,hint}]`；`mis-summary` 改为 `summary` + `points:[{label,value,risk}]` + `citations:[{field,value,source}]`（risk∈low/medium/high）；新增 `tests/test_mis_stream.py`（SSE 契约 delta→done、事件名精确、traceId、401 门禁、契约锚点）。
  - **BFF Java（沙箱 JDK8 不可编译，仅静态评审）**：`AiExtractResponse.confidence`→`Map<String,Double>`+`unmapped`；`AiSummaryResponse` 增 `summary`+结构化 `points/citations`（新增 `SummaryPoint`/`SummaryCitation`）；`AiChatRequest.stream`；`AiPlatformClient.chatStream` 返回 `Flux<ServerSentEvent<String>>`（1:1 透传 + `.onErrorResume` 补 error 帧）；`AiProxyController.chatCompletions` 按 `stream` 分支接活 `sseEnabled`；`AiFeaturesResponse`/`AiFeatureConfigService` 下发 `config.form-fill.confThreshold=0.85`（Q1）。
  - **前端 TS（可 build，TSC_EXIT=0）**：`fetch-event-source` 改 `@microsoft/fetch-event-source@^2.0.1`（npm 无 v3，微软官方具名导出包，已装）；`use-ai.ts` 注入 `stream:true`；`ai-copilot` 翻 `stream:true`；`ai-form-fill` 加 `confidence=null` 防御。
- **主理人独立核实**：pytest 170 passed、TSC_EXIT=0、读码确认 Java 三处 SSE 关键文件 1:1 透传正确；QA 额外核实 `pom.xml` 含 `spring-boot-starter-webflux` → servlet 栈返回 Flux SSE 可行，关键风险点解除。
- **偏差/待办**：① fetch-event-source 包名修正（设计写 ^3 不存在，改用 @microsoft 2.0.1，等价）；② `ai-rag.tsx` 故意保持 stream:false（BFF SSE 仅挂 chat/completions→mis-copilot，翻 rag 会坏，US-STREAM-2 待 BFF 按 capability 路由增强）；③ 401 透传仍 P2 预留（非阻断）；④ **生产须置 `mis.ai-platform.sse-enabled=true`**，否则 stream 静默降级缓冲；⑤ Java 层待内网 JDK17 CI 编译闸门 + SSE 端到端联调收口。

## 团队收尾
- 两名 worker（software-engineer / software-qa-engineer）已发 shutdown_request，待确认后 TeamDelete `software-ai-backend-ext`。
- **代码已提交（2026-07-24，本地未 push）**：分 5 个分层 commit —— `4f37245 docs`（AI 融合规格+进度同步，含新增 `docs/ai-fusion-delivery.md` 与 README 索引）、`b48826d feat(ai-platform)`（引入平台模块+融合扩展）、`74cbd1f feat(bff)`（AI 代理层扩展）、`4145dae feat(web)`（前端 MVP+流式）、`09bfcde chore`（dev 脚本 UTF-8）。已排除 `.workbuddy` 记忆、重复 V6 SQL、`nul` 残留（用 DeleteFileW 删除）。
- 待办同前：内网 JDK17 CI + `sse-enabled=true` 联调；US-STREAM-2 / 401 透传可选。

## 文档整理（2026-07-24 下午，已提交 `6fb3c81`）
- 用户要求：文档归置（位置不对）+ 临时文件清理。经拍板：建 `docs/ai-fusion/` 统一归置，**保留** `.workbuddy/memory/` 工作记忆。
- 归置：将散落根目录的 4 份早期探索稿（`mis_ai_*.md`：蓝图 PRD / 集成架构 / 阶段1-2 设计 / 可行性评估）+ `docs/` 根的 10 份（PRD/设计/审计/交付，含 2 张 mermaid）共 **14 份 AI 融合文档** `git mv` 进 `docs/ai-fusion/`。
- 链接修正：同目录文档 `docs/X.md` → 相对同辈引用；`docs/README.md` 两处 `ai-fusion-delivery.md` 补 `ai-fusion/` 前缀；顺带修复 `frontend-ai-integration-design.md` 此前指向根目录 `mis_ai_` 的断链。
- 临时文件结论：venv/_tmp_ai/nul 此前已清理；agent/.venv 被 `.gitignore` 忽略；本次无可清理垃圾（记忆文件按用户意愿保留）。
- 提交：`6fb3c81 docs: 归置 AI 融合文档至 docs/ai-fusion/ 并修正交叉引用`（15 files，14 rename + README 链接）。

## agent/ai-platform 与主项目融合分析（2026-07-24，用户问"哪些要融合，比如容器"）
- 现状对比（已实际读码确认）：主 MIS = Nacos 配置中心 + Flyway + `mis-gateway`(Java 8080) + `deploy/docker/Dockerfile.service`(MODULE 参数多阶段 Java 构建)；ai-platform = `agent/ai-platform/infra/` 下**独立完整栈**（postgres/redis/qdrant/embedding/squid-outbound-proxy/gateway(TS 3100)/backend(Py 8000)/H5 frontend/nginx(80)），配置走 yaml 目录挂载 + env。BFF `application.yml` 硬编码 `ai-platform.base-url: http://localhost:8000` + `sse-enabled: false`（**未进 Nacos**，容器环境必崩）。
- **必须融合（部署不通）**：qdrant/embedding/outbound-proxy 三个专属基础设施作为新 service 进主编排；ai-platform 网络与主网络打通；80 端口冲突（去 agent nginx，入口走 mis-gateway 反代）。
- **需决策（两边都有）**：Postgres（建议共享实例+独立库 ai_platform，Alembic 管 schema）、Redis（共享+独立 db index）、agent TS gateway 保留独立还是并入 mis-gateway（职责不同，建议独立）。
- **非容器必融**：BFF ai-platform 配置挪进 Nacos `mis-admin-bff.yaml` 做 dev/test/prod 差异化；JWT 密钥信任模型对齐（`backend/keys` vs agent `keys/*.pem`）；LLM key 经 outbound-proxy 统一密钥管理；Flyway(主库)/Alembic(ai_platform 库) 分工文档化；`scripts/start-integration-stack.ps1` 纳入起 ai-platform + 跑 Alembic。
- **待产品决策**：agent 自带 H5 frontend（独立对话 H5，区别于 mis-admin-web）是否纳入主部署。
- 结论：agent 当前是"独立栈 + URL 接入"MVP 形态；融合 = 把 ai-platform 相关 service 并入 `deploy/docker-compose.stack.yml`（或新增 `docker-compose.ai.yml`），共享主 pg/redis，去 nginx，配置进 Nacos。

## 融合部署架构决策评审（2026-07-24 下午）
- 建团队 `software-ai-fusion-deploy`；架构师高见远出 `docs/ai-fusion/ai-fusion-deploy-decision.md`（404 行，结构化决策评审：每条决策含 结论/推荐/备选/Mermaid/待确认 + 任务清单 DEP-0~10 + 风险）。
- 三条决策结论：① H5 纳入主部署+独立入口(iframe)可行，主栈新增「共享边缘 nginx」托管 H5 静态 + 反代 /api,/ws 到 TS gateway(3100)；H5 的 /api,/ws 指向 TS gateway 而非 mis-gateway(8080)（通路B）。② A2UI = backend 发 `ui.render{component,props}`，H5 组件注册表渲染；TS gateway 透明透传（H5 渠道 1:1、Bot 渠道降级 template_card），独立部署兼容。③ PG 共享(主实例新增 `ai_platform` 库 + Alembic)、Redis 共享(主实例 db2 + `aip:` 前缀)、去 agent nginx(职责移交共享边缘 nginx)。
- 主理人核实两关键事实属实：`gateway/src/middleware/auth.ts` 仅 HS256 验签+强校验 iss、不认 MIS RS256（嵌入鉴权阻塞 O1）；`frontend/src/hooks/useChat.ts` ui.render 仍是 `[UI: <component>]` 占位桩（A2UI 渲染器缺口 D2-1）。
- 待用户拍板：O1 嵌入鉴权走「TS gateway 信任 MIS JWT(RS256)」还是 SSO 桥；O5 A2UI 渲染器是否纳入本阶段必交付；推荐默认 O2 子域/O4 静态挂边缘nginx/O6 边缘nginx仅服务H5。P0 阻塞：DEP-5 BFF硬编码(`ai-platform.base-url: http://localhost:8000`+`sse-enabled:false` 未进Nacos)、DEP-1/2 共享、DEP-3 边缘nginx、DEP-7 嵌入鉴权。

## 融合部署实现 DEP-0~9（2026-07-24，工程师寇豆码，IS_PASS=YES）
- 决策全锁（O1 信任MIS JWT RS256 / O5 A2UI渲染器纳入 / O2+O4 子域+静态挂边缘nginx / O6 边缘nginx仅服务H5）。寇豆码按 DEP-0→9 落地，IS_PASS=YES：Python 7文件 py_compile 0、前端7改动文件 0 新增类型错误(修1 TDZ)、网关5改动文件 0 新增类型错误、compose 叠加 `config` 校验 0。
- 关键落地：① DEP-7 网关 `auth.ts` 新增 `verifyJwtRs256`（crypto.verify+MIS公钥 `/keys/public.pem`，iss=`mis-platform`，claim 映射 sub→userId 等），与 agent 自有 HS256 双验签；前端 `useAuth.ts` 加 postMessage(M1)+`?token=`(M2) 嵌入鉴权。② DEP-8 A2UI 新增 `frontend/src/components/a2ui/`（types/registry/ApprovalCardView/DataTableView/FormSheetView/A2uiRenderer），`useChat.ts` ui.render 改真实渲染，无 dangerouslySetInnerHTML。③ DEP-5 BFF 写 Nacos `mis-admin-bff.yaml`（base-url=http://ai-platform-backend:8000、sse-enabled=true），application.yml 改 `${...}` 可覆盖。④ DEP-2 Redis 两端 db2+`aip:` 前缀；DEP-1 PG 新增 `02-create-ai-platform.sql`(幂等建库建角色)+Alembic 管 ai_platform。⑤ DEP-3/6 新增 `deploy/nginx/edge.conf`(agent.* 入口+frame-ancestors+CORS+WS Upgrade+proxy_buffering off 保 SSE)+`docker-compose.ai.yml` overlay。
- **部署阻断(待主理人定)**：H5 前端基线 `npm run build` 有 13 个既有类型错误(markdownComponents/ApprovalCenterPage/SkillManagePage/approvalStore/cardAdapter/markdownNormalize.test 等，非融合改动)，会让 `ai-platform-frontend` 构建容器非0退出→edge-nginx 拉不起 H5 静态(DEP-6 不通)。需定：A) 修13错 / B) 容器构建改 `vite build` 跳过tsc门禁(跟踪技术债) / C) 先不动仅记录。
- 其他开放项：网关 tsc 20 个既有错误(走 tsx 直跑绕过，运行无碍)；edge.conf/CORS/`VITE_PARENT_ORIGINS` 占位域 mis.local 上线前填真实值；02-create-ai-platform.sql 仅首跑 PG 卷执行、存量需 DBA 手跑；Nacos 推送 README 未建。均未 commit，待分层提交+QA(DEP-10)。

## 融合部署验证 DEP-10（2026-07-24，QA 严过关，主判定 NoOne）
- 测试通过率：Python 新增 test_fusion_config.py(5) + 全量 175 passed；网关 auth.rs256.smoke.ts(13 passed，首跑 QA 自测漏 exp 自修)；前端 a2ui.render.smoke.tsx(14 passed)；compose config OK；BFF/Nacos/PG/nginx/嵌入鉴权静态核查符合。网关/前端无 jest/vitest，改用 tsx 直跑纯函数+react-dom/server 冒烟（可复跑）。
- 路由 Engineer 1 项(低风险非阻塞)：`agent/ai-platform/backend/.env` 第34行 `REDIS_DB=0`、第32行 `REDIS_HOST=localhost` 与 R2(db2)/config.py 默认/compose 不一致；容器由 compose 覆盖不受影响，本地联调共享 Redis 会撞库。待寇豆码改 `.env` 对齐 `REDIS_DB=2`。
- QA 新建测试：backend/tests/test_fusion_config.py、gateway/tests/auth.rs256.smoke.ts、frontend/tests/a2ui.render.smoke.tsx。
- 手工核查清单(环境依赖)：全栈 docker up、通路A(BFF SSE)、通路B(H5 postMessage+WS+ui.render 真实渲染)、PG ai_platform 初始化+Alembic、Redis db2 aip: 不撞 MIS、iframe frame-ancestors+token 兜底、A2UI SSE/WS 双通道同构。
- 至此融合部署 DEP-0~10 全闭环(仅剩 .env 对齐小修)。仍未 commit。

## 融合部署分层提交（2026-07-24 晚，本地未 push）
- 用户拍板「分层提交」。6 个分层 commit 已落入 master（本地，未 push）：
  - `5082aa2 docs(ai-fusion)`: 融合部署决策评审 + 前端技术债清单
  - `fc17a55 deploy`: docker-compose.ai.yml overlay + edge.conf 共享边缘 nginx + 02-create-ai-platform.sql + nacos-config(3 环境 mis-admin-bff.yaml)
  - `953d840 feat(ai-platform/backend)`: PG/Redis 共享配置 + aip: 前缀 + A2UI_COMPONENTS 双向登记 + test_fusion_config.py；.env.example REDIS_DB 0→2
  - `aa79602 feat(ai-platform/gateway)`: auth.ts RS256 双验签(信任 MIS JWT iss=mis-platform) + aip: 落 db2 + 冒烟测试
  - `eb97012 feat(ai-platform/frontend)`: a2ui/ 渲染器 + postMessage/?token= 嵌入鉴权 + Dockerfile.frontend 改 npx vite build + 冒烟测试
  - `47cae14 fix(mis-admin-bff)`: base-url 服务名化 ai-platform-backend:8000 + 迁入 Nacos
- 排除项：`.workbuddy/memory/` 两个日志（工作记忆不入库，符合一贯约定）；`.env` 被 gitignore 只提交样板 `.env.example`。
- 至此阶段5 后端扩展 + agent/ai-platform 融合部署（DEP-0~10）全部实现/验证/提交闭环，仅待上线前手工核查（DEP-10 §四 7 项）+ 填真实域名 + push。

## 文档整理：ai-fusion 拆分重归类（2026-07-24 晚，本地未 push）
- 用户要求"所有文档重新整理，过时修复/丢弃，文件夹关系重整理，分类清晰"。诊断：乱源仅在 `docs/ai-fusion/`（16 文件平铺、命名混搭、无索引）；其他目录（adr/architecture/api/backend/devops/frontend/project/agent）分类规范不动。
- 用户拍板：① 早期 4 份探索稿（mis_ai_deep_integration_blueprint_prd / mis_ai_integration_architecture / mis_ai_platform_integration_assessment / mis_ai_phase1_2_design）**直接删除丢弃**；② 进度同步稿 ai-fusion-delivery.md **删除**，状态提炼进新建索引；③ ai-fusion 建**三层子目录** decisions/ + specs/ + archive/ + 新建 README.md 索引。
- 实施：git mv 保留文件到新结构（decisions/{deploy,identity-jwt}.md、specs/{phase5-frontend-prd,phase5-frontend-design,phase5-backend-ext-prd,phase5-backend-ext-design,phase5-backend-audit}.md + 2 mermaid、techdebt.md、archive/identity-enrichment-task-list.md）；git rm 删 5 份；批量 sed 修正跨文件引用（jwt/identity-enrichment/phase5-*）+ 手动精修 deploy.md 同目录引用与已删稿引用；docs/README.md 目录图补 ai-fusion、进度表 AI 融合📝→✅、变更记录加 v1.5。
- 结果：ai-fusion 16→12 文件，无断链；总文档索引 `docs/README.md` 与 `docs/ai-fusion/README.md` 均已更新。
- **已提交**：`22df1db docs(ai-fusion): 重整理目录结构为 decisions/specs/archive 三层并补索引`（18 files, +57/-1793）。与上面 6 个融合 commit 共 7 个本地 commit，全部未 push。

## Sprint 2 联调计划评审（2026-07-24 晚）
- 用户给了一份「P0 Sprint 2 联调 1~1.5 周」计划（6 步：BFF 补 deptId 筛选 / 前端 users+depts api / UserListPage 左树右表 / 接 CRUD+重置密码+分配角色 / 删除保护 / OrgTreePage），要我详细检查是否合适。验收锚点 = `docs/project/sprint-plan.md` 第 3 节 #4 用户CRUD+部门筛选 / #5 重置密码 / #6 组织树三级增删 + Sprint2 验收 TENANT_ADMIN 不可删。
- 代码事实核实（关键）：① 后端 `UserController`(/api/v1/users) 已全齐 page/GET/POST/PUT/PUT-status/PUT-reset-password/DELETE/PUT-roles，**唯一缺口是 `page` 仅 status/username/page/size 无 deptId**（`UserAggregateService.page`→`IamWebClient.pageUsers(tenantId,appId,status,username,page,size)` 均无 deptId，需 IAM 层配合）；② `OrgController`(/api/v1/orgs listOrgs)、`DeptController`(/api/v1/depts tree) 已存在；③ 前端是 **AdminListPage 通用组件 + page-defs 配置驱动**（`def.sample` 内存数据，无远程），`/system/user` 的 filters 仅 username 无 dept；④ 删除保护「租户管理员」只在 sample 前端层(admin-list-page.tsx:278)，后端 `delete` 无校验（可被绕过）。
- 评审结论：方向对、覆盖核心验收，但(a) 第1步"补 deptId"低估为 IAM+BFF 双层改动，且影响 1 万条分页性能验收；(b) 第3步"实现 UserListPage 替换 sample"与现有配置驱动架构冲突，应明确"扩展 AdminListPage 支持 def.api(远程+服务端分页)+def.tree(左部门树)"还是独立写页；(c) 第5步"最后一个 admin"是计划新增(非 sprint-plan 要求)，且后端保护缺失须兜底；(d) 第2/4/6步实际是"前端接已有接口"，工作量比字面的"实现"小。建议重排：W1 后端 deptId 筛选 + AdminListPage 远程化改造 + api 层；W2 接真接口 CRUD/保护/OrgTreePage。待用户拍板：最后一个admin是否本期、AdminListPage 扩展 vs 独立页、数据权限(#8)本期还是 Sprint3。
