# 2026-07-18 工作记录

## 前端架构规划（mis-admin-web）
- 用户需求：规划前端架构。明确为「单体 SPA 起步 + 预留微前端演进 + 完整架构蓝图」。
- 现状：React 18.3 + Vite 5.2 + TS(strict) + Zustand(auth-store) + axios(拦截器/Refresh单飞)。仅 Sprint1 登录闭环，无 lint/测试/CI/组件库/服务端状态层。
- 交付：目标分层架构图（应用/特性/共享/基础设施/工程化 + 微前端演进虚线边界）+ 完整蓝图文档 `docs/frontend/architecture-blueprint.md`。
- 关键决策：TanStack Query 管服务端状态、Zustand 仅客户端状态；shadcn+Tailwind；features 严格按域、零跨域直接依赖；路由配置化+全 lazy；预留 qiankun/MF 演进边界。
- 成熟度评分约 4.8/10（⭐⭐），工程化与数据层为最大短板。

## Phase 2 脚手架落地（设计底座）
- 已落地并验证：pnpm install / typecheck / build / lint 全绿。
- 工程化配置：eslint.config.js(flat, 含 features/* 跨域依赖 no-restricted-imports 禁令=军规1 强制)、.prettierrc.json、tailwind.config.ts、postcss.config.js、components.json(shadcn)、.husky/pre-commit+pre-push。
- 设计底座代码：src/lib/utils.ts(cn)、src/lib/query/(queryClient+queryKeys)、src/app/providers.tsx(QueryClient+Theme+Sonner+ErrorBoundary)、src/components/common/error-boundary.tsx、src/components/layout/app-layout.tsx(Sidebar+Header+主题切换+退出, 用 shadcn token)。
- 接入：router 用 AppLayout 作 /dashboard 受保护布局壳(Outlet)；App.tsx 包 providers；login-page 的 DashboardPage 降级为纯卡片；globals.css 改为 Tailwind 指令+设计令牌(.dark)。
- 修的坑：①ESLint9 误配 typescript-eslint@7/react-hooks@4 → 升 8/5；②tsc -b 对非 composite tsconfig 报错 → 改 tsc --noEmit。
- 下一步：底座已跑通，可交 UI 设计专家/AI 出高保真（需附《设计对接 Brief》：已装 shadcn 组件清单+令牌表+页面路由清单+交互约束 §7.3）。

## 交付《设计对接 Brief》
- 文件：`docs/frontend/design-handoff-brief.md`。对接 UI 设计专家(人或AI)用。
- 内容：技术画布硬约束、设计令牌(已定 vs 待品牌决策, 引用 globals.css 实际 HSL 值)、布局骨架、页面高保真优先级(P0/P1/P2)、shadcn 组件词汇表+业务组合组件、交互与状态规范(加载/空/错/403静默/Toast)、交付物要求、待品牌决策清单。
- 标明已知债：登录页 login-page.tsx 仍硬编码 indigo #4f46e5，与令牌主色蓝不一致，属后续美化任务非阻塞。

## UI 设计方案交付（UI Designer）
- 用户需求：设计全新、美观且易用的管理后台界面，符合用户习惯。参考 docs/frontend。
- 交付两份，位于 `docs/frontend/design-proposal/`：
  1. `design-system.md` — 设计系统规格。在 shadcn 令牌体系内解决 Brief §8 全部待品牌决策：主色(沿用校准蓝#2563eb/备选靛/青)、补全语义色(success/warning/info HSL)、暗色微调、字阶(基准14px)、Logo文字标、登录页统一令牌(消 indigo 债)。含令牌总表(light/dark+hex)、组件映射、交互状态(加载/空/错/403静默/Toast)、响应式、WCAG AA、落地说明。
  2. `prototype.html` — 高保真可交互原型(Tailwind Play CDN + 真实 HSL 令牌 + lucide)，复刻 AppLayout 壳，含登录(令牌版)/仪表盘/用户管理(组织树+表+弹窗)/角色管理(三Tab Sheet)/菜单管理(三栏)，Light/Dark 切换、移动端抽屉。
- 关键约束遵守：未另起色彩体系、未破坏 --radius 0.5rem、未脱离 shadcn 组件词汇表。用 impeccable 技能的工艺标准(间距节奏/层次/空态)提升质量，但不违反项目令牌红线。
- 待产品/品牌拍板：主色与语义色是否采用本方案、登录页是否统一令牌；确认后并回 globals.css 即成为像素基准。

## UI 方案修订（用户拍板 3 项）
- 用户确认：① 主色=企业靛 `#4f46e5`(HSL 243 75% 59%，白字≈6.3:1 AA 通过)，`.dark` 提亮 `#6366f1`；`--primary`+`--ring` 同步改；② 采用我补的语义色(success/warning/info)，原型已启用；③ 登录页要**统一版与遗留版并排对比**。
- 已改 `prototype.html`：令牌改企业靛；登录视图改为左右并排「令牌统一版」vs「遗留硬编码版(#4f46e5/圆角16px/蓝调渐变)」+ 差异图例(圆角/渐变/主色/暗色/描边/字体栈) + 进入演示按钮。
- 已同步 `design-system.md`：§1.1 标记企业靛已定、§1.2 语义色已采用、§1.6 登录并排对比、§2.2 令牌表改靛色；`overview.md` 同步。
- 洞察：主色定为靛后，原 Brief「登录硬编码 #4f46e5」颜色债自然消除（同色系），统一版只需再规范圆角/渐变/描边/主题即完全合规——比最初「消债」说法更准确。

## UI 方案再修订（截图参考：企业微信中台·领券活动页）
- 用户提供企业微信中台「领券活动」截图作参考，要求按该风格优化 MIS 原型。提炼 8 个可借鉴点，落地 3 处到 `prototype.html`：
  1. 仪表盘统计卡 → 改为截图风「大数字(text-3xl)+状态说明+右上 rounded-full 圆标」(`StatCard`)。
  2. 用户管理 → 加 `FilterCard`(可折叠 + 「已设置 N 项条件」计数徽标 + 搜索/重置)、`StatusTabs`(全部/启用/禁用/锁定，下划线激活)、表格状态列与时间列徽标统一为「圆点+文字」(`inline-flex ... rounded-full` + `h-1.5 w-1.5 rounded-full` 圆点)。
  3. 侧栏 → 当前项维持 `bg-primary text-primary-foreground`(蓝底白字，与截图一致)，分组标题间距加大，给「操作日志」加 destructive 角标演示。
- JS 新增：`toggleFilter()`（折叠+箭头旋转）、`.user-tab` 点击切换高亮。
- 同步 `design-system.md`：§3.2 新增 StatCard/FilterCard/StatusTabs/Badge(圆点) 规格，§3.3 仪表盘+用户管理更新为 v1.1 范式。
- 校验：div 145/145、section 7/7、button 42/42 平衡；新元素齐全。
- 设计取向：借鉴真实中台的「列表页范式」(统计卡+筛选卡+状态标签+圆点徽标)，仍严格守 shadcn 令牌红线（企业靛主色 / --radius 0.5rem / shadcn 组件词汇）。

