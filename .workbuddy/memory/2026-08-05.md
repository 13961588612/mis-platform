# 2026-08-05 工作日志

## Coordinator–Worker 调度基座（C1/C2/C3/C5）— QA 独立验证完成

**团队**：`software-cw-orchestration`（架构师高见远→工程师寇豆码→QA严过关，主理人齐活林编排）

### QA 验证结论（IS_VERIFIED: YES）
- 全量 **430 passed**（346 基线 + QA 新增 73 + QA2 遗留 11），6 warnings 全 pre-existing
- 9 条回归红线**全绿**，其中 8 条 QA 写成可执行测试（非人工目检）
- **零源码 Bug**；本轮 4 个失败全是 QA 自身测试 harness 写错，已自修
- 关键交叉印证：QA2（software-qa-engineer-2）因网络 502 中断，但其断掉前留下的 `test_cw_audit_sse_contract.py`（11 条）被 QA1 复跑全绿，印证红线 5/6

### 主理人亲自核实（trust but verify）
- `config.py:319-320` `TASK_BRIEF_STRICT` 实现默认 **False**（已读）
- `invoke_agent.py:261` 读 False 走 lenient 分支，校验失败只 warning 照放（已读）
- `design-impl.md:1006` 架构师设计写 **True**，且标注"False 是灰度回滚开关"（已读）
- → 工程师把极性反了：设计意图"默认生效/False回滚"，实现成"默认休眠/True opt-in"

### 两处偏差裁定
- **D1（中）`TASK_BRIEF_STRICT` 默认值**：实现 False vs 设计 True。后果=C1 招牌能力「懒委托拦截」默认休眠。**待用户裁定**：(a) 翻 True 对齐架构师+QA 回归一轮；(b) 保持 False 灰度保守只同步文档。
- **D2（低）`INVOKE_AGENT_MAX_PARALLEL`**：实现 1 vs 设计 2。主理人任务书已裁定默认 1（等价串行），故**不算偏差**，仅需把 design-impl.md §6.2 line 1009 从 2 同步为 1。

### 其他观察（信息级，非阻塞）
- 交付清单漏列 2 文件：`src/coordinator/sessions.py`（389 行，C5 核心）、`src/config.py`（+38 行）——均已实现且有测试
- AgentEvent 实际只新增 `trace: dict|None` 单字段（非简报所称 dispatching/dispatch_trace 双字段），功能完整
- 既有技术债（非本次引入）：`resolve_whitelist(["","  "])` 返回空集不回落默认白名单——运维把白名单配成全空白会致委派整体失效
- `TASK_NOTIFICATION_MODE="json"`（默认关）下失败路径也会被 JSON 包裹，开启前需复核 Java/BFF 解析

### D1 裁定 + 收尾闭环（用户拍板：翻 True）
- 用户选 (a) 翻 True 对齐架构师。团队因网络中断被清，主理人重建 `software-cw-orchestration-95a1` 续做。
- 工程师寇豆码：`config.py:319-322` `TASK_BRIEF_STRICT` default False→True + description 极性修正；`design-impl.md:1009` `INVOKE_AGENT_MAX_PARALLEL` 文档 2→1；`test_dispatch_trace.py::test_invoke_lenient_mode_does_not_reject` 显式注入 `TASK_BRIEF_STRICT=False` 脱离全局默认。跑出 3 failed/460 passed（比预期多 1 个：`test_cw_audit_compat.py::test_timeout_text_verbatim` 用真实 Settings() 吃新默认，content 10 字<12 字在 brief 校验阶段被拒到不了超时分支）。
- QA 严过关第 2 轮：3 处测试自修（特征化断言翻 True、真实 Settings 默认契约翻 True、超时用例显式关 strict 进超时分支）+ 新增 `test_lazy_delegation_rejected_by_default_without_explicit_strict`（真实 Settings() 不显式设 strict，content="你看着办" 被拒+返回重写模板+Worker 零触达，证明 C1 默认生效）。
- **最终全量 464 passed / 0 failed**（460 基线 + 3 修复 + 1 新增）。IS_VERIFIED: YES。交付闭环。
- 智能路由：3 处全 Test-code Bug → QA 自修，无源码 Bug，未回工程师。

### 最终状态
- **未 commit**（按惯例等用户指令）。改动含：config.py(tracked,M) + design-impl.md/test_dispatch_trace.py(test_fix)(untracked 新文件) + test_qa_coordinator.py/test_cw_audit_compat.py(QA 修)。
- 工作区另有非本次任务的遗留改动：`deploy/ragflow/README.md`、`docs/backend/mis-kb-wave-d-progress-2026-08-04.md`、`deliverables/software-company/mis-kb-wave-d-qa-2026-08-04.md`——commit 时注意排除。
- C4（前端+BFF 透传）显式留待后续，本次范围不含。
- 团队已 TeamDelete 清理。

## C4 前端+BFF 收敛 — 已提交（2026-08-05）
- 提交 `1b29bc5`：feat(ai-platform): C4 Coordinator-Worker 前端+BFF 收敛（28 files, +4291/-14）
- 范围：Agent H5 仅暴露 Coordinator（role 由后端 /agents 下发过滤，不硬编码 id）；启用 dispatch.trace 独立事件通道（通道 C）前端展示调度轻提示；后端 AgentSummary 加 role 字段；通道 C 默认值双源对齐（config.py ↔ trace.py）；修复跨轮调度提示残留（sendMessage 清空 dispatchTrace）、企微 Bot 通道防御性分支（P3，回溯生产链路确认对线上行为无影响）
- 验证：主环境真实执行——后端 499 passed / 0 failed；网关+前端 tsc 对 C4 改动文件零新增错误（QA 子环境 round2 整轮故障，主理人补执行证据）
- 刻意排除并行改动（未纳入本次 commit）：C0-C3 的 coordinator 其他文件（__init__/brief/catalog/flags/sessions/notification）、mis-kb Wave D 文档、agent-ops-console、deploy/ragflow/README.md、memory 文件、AGENTS.md
- 至此 Coordinator-Worker C0-C5 全闭环（C4 已落库；C0-C3 等其余改动仍在工作区未提交）

## 全部分组提交收口（2026-08-05 续）
- 用户指令「都提交吧」，按功能线分 4 个 commit 收口（memory 本地记忆不入库）：
  - `12aab50` feat(ai-platform): C0-C3 Coordinator-Worker 调度引擎实现（39 files, +7317/-100）
  - `f846c43` docs(kb): mis-kb 知识库权限 Wave D 交付报告与进度文档（2 files）
  - `e3645b7` docs(agent-ops): 智能体运营控制台 PRD/架构/ADR/Spec/UI（7 files，含 AGENTS.md）
  - `8f6f7f1` chore(devops): 本地 dev 脚本、DB 初始化与运维/部署文档（9 files）
- 至此工作区业务改动基本清空。
- **意外发现（未提交，待决策）**：`deploy/nacos-config/integration/mis-common.yaml` 出现 M 改动，将 DB_HOST/REDIS_HOST 默认值从 localhost 改为内网 IP `10.254.16.6`。不在任何功能线清单、来源不明，疑似集成环境联调/配置漂移；硬编码内网 IP 进默认值会破坏本地开发默认连接。已**排除出本次提交**，建议还原或单独作为 deploy 配置 commit 确认。
- 仍不入库：`.workbuddy/memory/2026-08-04.md`、`.workbuddy/memory/2026-08-05.md`（本地工作记忆）。

## 智能体运营控制台（agent-ops-console）实现 — T01 完成、T02 已派单

**团队** `software-agent-ops`（主理人齐活林 → 架构师高见远 → 工程师寇豆码）。需求文档已锁定（prd/spec/ui/adr/architecture v1.4），走部分工作流跳过 PM。

### 用户四项拍板决策
① 会话列表 = 新增 PG 表双写；② 企微 Bot 本期只做多实例并存+独立启停，**不改** WS→HTTP 回调；③ Skill 执行码直接加码 fail-closed，**不做灰度**；④ 交付范围 = P0 全量（T01–T05）。

### 架构师关键勘察结论（`docs/ai-fusion/agent-ops-console/impl-plan.md`，1229 行）
- **spec.md §3 标注「既有」的 API 大面积失真**，8+ 组实际不存在（session 列表、agent skills/config-files/coordination、worker-catalog、dispatch-traces 等）→ 归入 T04 补齐
- 发现文档未提及的**第 6 条 Skill 执行路径**：BFF `POST /api/v1/ai/skill/execute` 与 `/apply` **未登记 sys_api ⇒ 任意登录用户可调**（§4.3 第 59/60 行）
- Skill 执行入口共 E1–E6 六条，当前 `skill.py`/`mcp.py` **零鉴权**（已抽查证实零 `Depends(`）
- 配置热更新链可复用：`ConfigManager.save_config()` → `on_config_change` → `refresh_worker_catalog()`，勿重造
- 迁移 5 个陷阱：D3 `uk_menu_app_permission` / D5 `sys_api` 无 tenant_id·app_id / D6 `uk_menu_api_api` / D12 枚举 / D13 `uk_api_method_path`
- 主理人核实：`sys_menu.visible` 列**确实存在**（V1__init_schema.sql）→ Q5 解决，3 个详情子路由用 `visible=0` type=2

### T01 已完成（工程师 IS_PASS: YES，主理人已核实落盘）
- 迁移 3 个：`V19__agent_ops_seed.sql`（app 92010 'agent' + module 92020 + 目录 92030 + 菜单叶 92031–92045 + role_id=1 授权）、`V20__agent_ops_api_perms.sql`（catalog 92090 + 58 条 sys_api 92100–92157 + sys_menu_api + 按钮码 92051–92063）、`V21__agent_skill_exec_perms.sql`（system App 目录 92200 + skill 执行码）
- BFF：`AppController.java:37` `ENTERABLE_CODES = Set.of("system","kb","agent")`
- 前端改 5 文件（router/host-apps/app-layout/icons/keep-alive-outlet）+ 新建 `lib/nav/agent-nav.ts` 与 `features/agent/**`（types/api/store/shell/badge + 12 页面空壳）
- 门禁：`npm run typecheck` exit 0；eslint 新增代码 0 error。**迁移未实库验证**（本地无 PG），留 QA
- 踩坑：typecheck 假报错源于残留 `tsconfig.tsbuildinfo` 增量缓存，清缓存即消；keep-alive-outlet 被 linter 自动格式化出重复 import 致 TS2300

### T02 派单前主理人自查纠正的 4 条硬事实（impl-plan 措辞失真，重要）
1. **BFF 零 JPA / 零 @Entity**，grants 无法「本地写 sys_role_permission」，唯一通路是 `IamWebClient.listRoleMenus` / `assignRoleMenus`
2. mis-iam `RolePermissionService.assignMenus:56` 是 **delete-all-then-insert 全量覆盖**语义 → grants 必须 **read-modify-write**，直接 PUT 单个 id 会清空该角色全部菜单权限（生产事故级坑）
3. 该方法结尾已调 `bumpUsersOfRole(roleId)`，缓存失效由 iam 侧保证，BFF 不需额外处理
4. skill_id ⇄ 菜单节点用 `SystemWebClient.tree(appId)` 扁平化匹配 permission；app code→appId 用 `IamWebClient.listApps(tenantId,"subsystem")`
- 另定：§11 Q1-b 取**方案 A**（创建 Skill 后同步 createMenu 注册执行码，失败不回滚主流程、返回 `permissionCodeRegistered:false`）；`AgentOpsProperties` 需 **两个 base-url**（backend 8000 / gateway 3100，仅 bots/health 走 gateway）

### T01 完整报告补充（工程师断点恢复后发现的中断残留 + 设计缺陷）
上一轮 499 canceled**不是干净截断**，留下两处残留，已修：
- `V20` 第 2–5 节整体重复写了两遍（718 行），ID 相同靠 `WHERE NOT EXISTS` 保护不炸库但 name 列有出入 → 删草稿拷贝，**现 372 行 / 5 条 INSERT / 58 条 sys_api**（主理人已核实计数）
- `features/agent` 存在嵌套目录版(11 文件)与扁平版(13+pages.ts)两套 → 删嵌套版，**现 19 文件全扁平**（已核实）；扁平版 13 页全部 import 路径写错（`./agent-page-shell` 实为 `./components/agent-page-shell`）已修

**原方案漏算的设计缺陷（重要，非残留）**：`keep-alive-outlet.tsx` 的 `matchDynamicPage()` 硬编码「前缀后恰好剩一段」(`rest.includes('/')` 即 continue)，而 `/agent/agents/:id/skills|config|coordination` 剩两段 → **16 条路由有 3 条白屏**。修法：`DynamicPageRule` 加可选 `maxSegments?: number`（缺省 1，agent 规则设 2）+ 拒尾部空段；kb 规则不传保持原行为。已核实落在 `keep-alive-outlet.tsx:145/163/176`。

### 主理人对工程师 6 项待确认的裁定（2026-08-05）
1. **D6 已过时**（主理人核实：V1:301 定义、**V8:37 已 DROP** `uk_menu_api_api`，V17:161/V18:279 早有记录）。但**仍守 1:1 纪律**——多挂会让 `ApiPermissionRegistry.match()` 取 permission 并集 = 判权稀释成「持任一码即可」。§10.4「三个唯一索引」应改为两个。
2. **maxSegments** 接受，属必要修正。
3. **#59/#60（`/api/v1/ai/skill/execute|apply`）维持不登记 sys_api**，归属 **T03 非 T02**。理由：真实粒度是 body 的 `skill_id`，URL 级码表达不了；form-fill 全员功能，挂码=给所有角色发码，判权强度≈0 却拉满运维负担。唯一门是 T03 `SkillPermissionChecker`，且 T03 须为这两端点各补一条 fail-closed 用例。
4. **Skill 码 ⇄ registry.yaml 漂移**：**不做启动期全量 upsert**（BFF 启动与 flyway 时序耦合）。改**懒注册** `SkillPermissionCodeService.ensureCode(skillId)` 单方法两调用点：(a) 创建 Skill 成功后；(b) 进 grants 页码不存在则补建。ID 走 `IdGenerator`，92200–92299 留给迁移种子。
5. `agent:mcp:call`(92060) 授 role_id=1 —— 接受，O1e 安全评审再复核。
6. 只授 role_id=1 —— 接受，等产品给角色×权限矩阵再补授。
- 顺带：`tsconfig.tsbuildinfo` 加 .gitignore + `git rm --cached`（tsc 增量缓存不该入库，已两次致假报错）。

### T01 自检基线（工程师实测）
typecheck exit 0；`eslint src/features/agent src/lib/nav/agent-nav.ts` 零输出零 error；全量 eslint 仍 11 error/14 warning **全是存量**（features/ai/context/form-fill-bridge.tsx ×1、features/system/admin-list-page.tsx ×5、user-list-page.tsx ×5），`arch/no-cross-feature` 未触发。22 项 SQL 静态核对全 PASS，**但本地无 PG，三个迁移均未实库验证**。
- 上线提醒：BFF 必须**重新部署**（`ENTERABLE_CODES` 是编译期常量，只跑迁移九宫格仍不可点）；`mis.api-permission.refresh-interval-seconds` 默认 **300s 缓存**，验 403 要重启或等过期。

### 推进波次
T01 ✅ → **T02（进行中，工程师）** ‖ **impl-plan 回写（架构师，纯文档不碰代码）** → (T03 ‖ T04) → T05 → QA。T03 是本期最高风险（E1–E6 fail-closed）。

### 进度核查（21:30，主理人实测磁盘而非听汇报）
- **架构师回写已落盘**：`impl-plan.md` 1229 → **1258 行**（21:22 写入），4 处修正全部就位——D6 勘误(L119)、`maxSegments`(L189 + L222-229 说明块)、`AgentOpsGrantService` 不碰 DB + read-modify-write(L434)、`ensureCode` 懒注册(L243 + 类图 L406/425)。
- **架构师遗留待收尾**：① 文件头 L4 版本号仍 `v1.0` 未升；② **L538 / L1200 / L1242 三处仍按旧口径把 `uk_menu_api_api` 当现存约束**，与 L119 勘误自相矛盾（L1200「三个唯一索引」应改两个）。下轮它回报时要求一并修掉，否则 T03 施工照旧文档还会踩。
- **工程师 T02 零落盘**：`mis-admin-bff` 157 个 java 文件、`find -ipath "*agent*"` 零命中、21:00 后无新文件 → 仍在勘察阶段（接单约 8 分钟，正常）。
- 工作区 T01 产物齐全：3 迁移 + `features/agent/` + `agent-nav.ts` 为 `??`；`AppController.java` + 5 个前端文件为 `M`。无游离临时文件。
- 教训复用：**worker 唤醒后只读最新一条消息**，长任务派单必须自包含（前置裁定 + 完整规格塞进同一条），否则会像本轮工程师那样重复复核上一批而不开工。

### ⚠️ 团队上下文丢失与重建（21:35，重要运维教训）
- 现象：`SendMessage` 报 **"Not in a team"**，`~/.workbuddy/teams/software-agent-ops/config.json` 文件仍在但当前 session 已脱离 team context；两个 worker 进程**均已结束**。
- 判定依据：工程师"reactivated"后 `mis-admin-bff` 仍零 agentops 文件 → 它**没真正开工 T02 就退出了**，之前那条"已接单开工"的判断是错的。**不能只凭 reactivated 通知就认为 worker 在干活，必须查磁盘产物。**
- 处置：`TeamCreate` 新建 **`software-agent-ops-t02`**（同名会被系统加后缀，索性显式换名），重新 spawn 两个 worker：
  - `software-engineer@software-agent-ops-t02`（task `agent-c102492f`）→ T02 BFF 网关层
  - `software-architect@software-agent-ops-t02`（task `agent-3174e114`）→ impl-plan 收尾 5 处
- **通用教训**：长会话（尤其经历 context 摘要后）团队上下文会丢。`Agent` 工具报 "No active team found" 时先 `TeamCreate` 再 spawn。派单 prompt 必须完全自包含——重建后的 worker 是全新进程，**零历史记忆**。

### ✅ impl-plan.md 收敛至 v1.1（架构师交付，主理人已核实）
1258 → **1266 行**。主理人指定 5 处 + 架构师 grep 自查补 4 处，共 9 处：
- 指定：L4 版本号升 v1.1 + L5 变更摘要；L539 改 1:1 工程纪律口径；L1201「三个唯一索引」→ **两个索引 + 一条纪律**；新增 **§11.3「已裁定事项」(L1245)** 收纳 Q1-b(方案A) 与 Q8(维持不登记 sys_api，归 T03)。
- 自查补：L532 §4.3 表头「外加两条既有端点补登记」→ 维持不登记；L535-536 #59/#60 行同步；**L828 §6.2 时序图 E6 分支**「需先补登记 sys_api」→「未登记，按 deny-unmapped 静默放行」（这处最险，不改 T03 看时序图会反向施工）；L305 §3.5「D3/D6/D13 三个唯一索引」→ 两个 + 纪律；L685 §5.4「Q1-b 待定」→ 指向 §11.3。
- **两组 grep 自查收敛**：`uk_menu_api_api` 剩 6 处（L5/120/305/469/539/1201）全为勘误/历史/纪律语境；「grants 本地写」失真已清（L861/L911/L924 三处改为「经 mis-iam / IamWebClient.assignRoleMenus」），剩 3 处经核为非失真表述。
- 主理人已抽查 L1201 / L539 / §11.3 原文，与汇报一致。**T03/T04/T05 可安全照此文档施工。**

### T02 工程师被框架错误打断（22:35）— 代码全落盘，仅差验证
- 失败通知：`software-engineer failed: 实现T02 BFF网关层 / Error: Cannot read properties of undefined (reading 'type') / Duration: 57m 1s`。**框架级错误，非代码问题**。
- 主理人立即盘磁盘（不听汇报只看产物），结论：**35 个 Java 文件全部落盘、零截断**（逐文件末尾大括号完整性检查全 PASS）：
  - `client/` AgentOpsClient / AgentOpsTransport / AgentOpsUri
  - `config/` AgentOpsProperties(新) + BffConfiguration(改)
  - `controller/` AgentOpsController(43 端点) / AgentOpsGrantController(3) / AgentOpsChannelController(6) = **52 映射**
  - `service/agentops/` AgentOpsFacadeService / AgentOpsGrantService / SkillPermissionCodeService / WecomBotFacadeService
  - `dto/agentops/` 21 个 VO/Request；`support/` AgentOpsErrorCodes / AgentOpsNotImplementedAdvice / DownstreamNotImplementedException
  - `application.yml` `agent-ops:` 已落（backend-base-url :8000 / gateway-base-url :3100 / timeout-ms 15000）
- **未完成的只有三件**：① mis-admin-bff 编译验证；② 端点⇄`V20__agent_ops_api_perms.sql` 逐条比对（Controller 52 映射 vs SQL 58 条，**6 条差额待解释**）；③ IS_PASS 一致性审查。已 SendMessage 让同一 worker 续做，明令**不许重写已落盘文件**。
- 工具坑记录：`find ... -exec` 报 `The environment is too large for exec()`（Windows/Git-Bash 环境块超限），改用 `wc -l` 单文件或小批量绕过，**不影响文件存在性判断**。
- 并行安排：架构师同步产出 `docs/ai-fusion/agent-ops-console/t03-failclosed-spec.md`（E1–E6 逐条落点 + Python/Java 文件契约 + fail-closed 测试矩阵含 #59/#60 用例 + 与 `SkillPermissionCodeService` 的时序衔接）。纯文档，与 BFF Java 零冲突。
- **团队并发约束**：一个 team 内 worker 名字必须唯一，而 SOP 要求 `name == subagent_type`，所以**同时只能有一个 `software-engineer`**。T03/T04 无法用两个工程师并行，只能串行；能并行的只有架构师(文档)与工程师(代码)。

### T02 编译验收通过 + 两个新踩的坑（22:50）
- **编译 PASS（主理人亲验 class 产物，非听汇报）**：`backend/mis-admin-bff/target/classes/com/mis/adminbff/` 22:45 全量重编，`dto/agentops` 24 class（21 源文件+内部类）、`service/agentops` 4、`client/AgentOps*` 3、`config/AgentOpsProperties` 1、`support/` 3、三个 AgentOps Controller class 齐。T02 编译项判定 PASS，不必重跑 mvn。
- **坑 A：`backend/logs/mis-admin-bff.log` 是幽灵进程日志**。PID **1504**，19:35 前启动，心跳到 22:45（每 5 min 一条 ApiPermissionRegistry 加载日志），**跑的是旧 class**。grep 不到 ERROR ≠ 新代码没问题。而且该文件是 **UTF-16 编码**（字符间夹 NUL），直接 grep 中文/关键字必然搜空 —— 要先 `iconv -f UTF-16 -t UTF-8`。**别把它当启动验证证据**。
- **坑 B：V19/V20/V21 三个 Flyway 迁移当前未 apply 到 DB**。证据：上述日志显示 `ApiPermissionRegistry` 只加载 **74 条规则**（新增 58 条 agent-ops sys_api 若已入库不止此数）。推论：① 端点⇄SQL 比对只能**静态**做；② 上线部署必须先跑 Flyway，否则 agent-ops 全部端点因 sys_api 未登记走 **deny-unmapped 静默放行**，等于零鉴权 —— 这条已写进 T03 前提。
- **补登记纪律**：V20 是 append-only 且可能已在他人环境 apply，若比对发现漏登记，**新开 V22**，不许改 V20。
- **team context 第二次丢失**，重建团队 `software-agent-ops-t02b`，重新 spawn：工程师(task `agent-07c5c653`，只做 58 条端点⇄SQL 双向比对表 + IS_PASS，明令禁止重写已落盘文件/禁止 mvn/禁止启服务) ‖ 架构师(task `agent-745a86b3`，产出 `docs/ai-fusion/agent-ops-console/t03-failclosed-spec.md`)。
- **长会话铁律**：team context 会随上下文压缩反复丢失，每次重建后 worker 都是**零记忆新进程**，派单必须 100% 自包含（含"哪些已完成不许重做"的硬性禁令），否则会重复劳动或覆盖成果。

### T02 验收通过（23:00）— 58:58 双射，IS_PASS YES
- 工程师交出双向比对表，主理人**抽查三点全中**（不听汇报看产物）：① `grep action:start|action:connect|action:enable` → 三条正则段确在 `AgentOpsController.java:132/:275`、`AgentOpsChannelController.java:77`；② V20 中 `921xx` 唯一 ID 计数 = 58；③ `agent:mcp:call` 独立码（按钮节点 92060 / api 行 92141 / 路径 `/mcp/servers/{name}/call`）未被 `agent:mcp:manage` 稀释。
- **52 → 58 差额的正解（重要，别再困惑）**：Controller 用了 Spring 正则段收口，1 个方法 = 多个逻辑端点：
  - `POST /agents/{id}/{action:start|pause|resume|stop}` = 4 端点（+3）
  - `POST /mcp/servers/{name}/{action:connect|disconnect|discover}` = 3（+2）
  - `POST /channels/wecom/bots/{botId}/{action:enable|disable}` = 2（+1）
  - 52 + 6 = 58，与 V20 的 58 条 sys_api 严丝合缝。**表 C（SQL 有代码无）为空，零冗余。**
- **正则收口的意外收益**：未登记动作串（如 `POST /agents/1/purge`）在**路由层就 404**，到不了 `ApiPermissionInterceptor`，等于给 `deny-unmapped: false` 这个宽松配置加了道兜底。
- **匹配语义已实证**：`ApiPermissionRegistry.match()` 用 `AntPathMatcher`，`{id}`/`{name}` 是单段占位不跨 `/`；`normalizePath()` 去 query、去尾斜杠。SQL 里直接写 `{id}` 是**正确写法，无需改成 `*`**；代码侧 `@PathVariable` 变量名与 SQL 占位名不同不影响匹配。
- **`match()` 多命中取 permission 并集且任一即可** → 宽松码会稀释高危码。已逐组核查无稀释。
- **⚠️ 上线部署双向陷阱（务必写进上线清单）**：
  - **只跑 V20 不跑 V21** → `ai:skill:*:run` 码不存在 → `SkillPermissionChecker` fail-closed 让**所有人含管理员技能执行全 403**。
  - **一条都不跑就启 BFF** → 58 端点全走 `deny-unmapped: false` 放行 → **任何登录用户可直调 `POST /mcp/servers/{name}/call` 执行任意 MCP 工具**（KB 自 V13 起的历史缺口原型重演）。
  - 正确顺序：**Flyway migrate（V19→V20→V21 全跑）→ 再启/重启 BFF**。已运行实例需重启，或等 `refresh-interval-seconds: 300` 自动重载。
- **#59/#60 豁免佐证**：V21 文件头 :55-66 写明刻意不登记，因真实鉴权粒度是 body 里的 `skill_id` 而非 URL，sys_api 挂任何单一 permission 都表达不了「能不能跑这个技能」。门是 `SkillPermissionChecker`(fail-closed) + `ReverseTrustInterceptor`。
- 工程师转入 **T03 现状摸底（纯只读）**：Java 侧 AiProxyController :263/:290 调用链 + ReverseTrust 语义澄清 + IamWebClient 是否有读权限码的方法；Python 侧 identity//skills//runtime//api/deps.py 现状 + 可复用 httpx/redis 基建；E1–E6 逐条真实鉴权状态表。与架构师写 spec 并行，「应该怎样」×「现在怎样」对齐后再下施工单。

### 🔴 T03 地雷：反向信任通道的 LoginUser 权限集合是硬编码假数据（23:15，主理人亲挖）
**这条不在 impl-plan.md 里，是读代码挖出来的，T03 能否一次做对的胜负手。**

证据链（三处，逐行读过）：
1. `mis-admin-bff/.../security/ReverseTrustInterceptor.java:63-64` — `REVERSE_PERMISSIONS = Set.of("ai:*:use")`，注释「覆盖写类端点 ai:*:use，确保既有 PEP 通过」。
2. 同文件 `:126-131` — `loginUser.setPermissions(REVERSE_PERMISSIONS)` **硬编码覆盖真实权限**，且**没有 `setAppId(...)`**。
3. `mis-admin-bff/.../security/UserPermissionLoader.java:41-60` — 两道连环短路：
   - L42-43：`appId == null` → 直接 `return Set.of()`（空集）
   - L46-47：`getPermissions()` 非空 → **原样返回、永不回源** Redis / IamWebClient
   - L54：`iamWebClient.loadPermissions(userId)` 是真实回源通路（存在，可直接复用）
   - L56-58：异常 → 返回 `Set.of()`（配合 `contains()` 天然 fail-closed，语义正确，需在文档写明是有意为之防后人误"优化"成放行）
   - L62-65：Redis 缓存键 `CacheConstants.RBAC_PERMISSIONS` = `tenantId + appId + userId` 三元组

**后果**：T03 的 `SkillPermissionChecker` 若按常规写 `userPermissionLoader.load(loginUser).contains("ai:skill:"+id+":run")` → E1/E2 路径（ai-platform 反向调 BFF `/api/v1/ai/skill/execute` 与 `/skill/apply`）下**所有技能执行一律 403，含有权限用户和管理员**。危险在于会诱导后人把 fail-closed 改成 fail-open"修好"。

**⚠️ 与「V21 未 apply 导致 403」是两个独立的坑**，别混。即使 V19-V21 全 apply、码存在、用户已授权，这条链依然 403。

**待架构师裁定的解法**（已发派单，要求给理由）：
- A：`SkillPermissionChecker` 绕开 LoginUser，直接 `IamWebClient.loadPermissions(userId)` + 自建缓存（爆炸半径最小，主理人倾向）
- B：改 `ReverseTrustInterceptor` 加载真实权限并入 `ai:*:use`（语义最正，但波及已上线 FormFill 反向链路 `ccf1ec2`/`cae4a60`，回归风险高）
- C：`SkillPermissionChecker` 读 `request.getAttribute("com.mis.adminbff.security.ReverseTrustApplied")` 分支识别，两条链路解耦
- 附带必裁：appId 从哪来（Skill 码挂 `system` App，该 id 硬编码/配置/运行时查？）；缓存键须用 MIS 侧 **db0 + `mis:` 前缀**，不是 `aip:`

**待工程师验的最后一环**：`ai:*:use` 里的 `*` 是**字面量**还是**通配符**——字面量比较 → 误拒（全员 403，吵但安全）；通配符匹配 → 可能误放（零鉴权，真危险）。同时要它产出「当前吃 `ai:*:use` 的端点清单」= 方案 B 的回归测试范围。

**语义澄清（文档里必须分清）**：`ReverseTrustInterceptor` 校验的是「调用方是不是可信 ai-platform」（平台凭证 + 上游 JWT 验签 + 来源网段三因子），**不是**「这个用户能不能跑这个 skill」。混淆会让人误以为反向信任已管住权限。

## T03 裁定书 R1（主理人拍板，10 条 · 已发架构师升 spec v1.2）

架构师产出 `docs/ai-fusion/agent-ops-console/t03-failclosed-spec.md`（377 行，E1–E6 落点表 + 36 条测试矩阵 + 8 条待裁定）。主理人实读代码核出 8 条硬事实后逐条裁定。

**必记硬事实（含行号，别再重查）**
- `mis-common-core/.../BusinessException.java:13` → `code` 是 **int**，不是 String；`:53` 有 `(int, String, Object data)` 三参构造。
- `mis-common-web/.../GlobalExceptionHandler.java:34-38` → `handleBusinessException` 返回 **`ResponseEntity.ok(...)`，HTTP 200**，不是 403；`body.setData(ex.getData())` 把结构化明细带进响应体。**所有"BusinessException → 403"的断言都是错的。**
- `ResultCode.java:51` → `FORBIDDEN(40300)`，403xx 段其余空闲；`AgentOpsErrorCodes.java:52` 已占 `SKILL_CODE_UNAVAILABLE=40917`。
- `mis-admin-bff/.../config/ReverseTrustConfiguration.java:29-32` → 反向信任**只拦 2 条路径**：`/api/v1/ai/skill/execute`、`/api/v1/ai/skill/apply`。`ai:*:use` 污染面 = E6，不外溢。
- `ReverseTrustInterceptor.java:83-91` **双模式**：无 `X-Platform-Token` 直接 `return true` 放行给 PEP（前端直连不被误杀）；`:54` `ATTRIBUTE_NAME`、`:125` setAttribute(ctx)、`:132` `ATTR_TRUST_APPLIED`。**来路标记现成**，`AiProxyController.java:323` 已有 `instanceof ReverseTrustContext` 先例。
- `IamWebClient.java:201` → `loadPermissions(Long userId)`，走 mis-iam `/internal/v1/permissions/{userId}`，**无 appId 入参**；`:206` body 为 null 时静默返 `List.of()`。
- `ApiPermissionInterceptor` 在 **`mis-common-security`**（公共基座，8 模块共用，不可为单需求改）。
- `ApiPermissionConfiguration.java:44` 只拦 `/api/v1/**`；BFF 全仓**零 `/internal` 路径**。

**裁定结论**
1. `_normalize` **作废**，`ai:skill:{id}:run` 点号原样；加跨语言黄金向量表 + 双侧单测（TC-37）防漂移。
2. Java 侧错误码必须**数字**：新增 `AgentOpsErrorCodes.SKILL_FORBIDDEN=40301`、`ACL_UNAVAILABLE=40303`；字符串 `AI_SKILL_FORBIDDEN` **降级为 `data.code` 语义标签**。40917 只留给 `ensureCode` 注册失败。
3. **否决**架构师推荐的"ai-platform 回传权限码"方案（循环信任 confused deputy / 平台凭证泄露即沦陷 / TC-28 测不出东西）。裁定采 A+C：`SkillPermissionChecker` 按 request attribute 分来路——反向信任支**禁读** `LoginUser.getPermissions()`，改 `iamWebClient.loadPermissions(userId)`；直连支读网关注入的真实码。**不改** `ai:*:use`（改它会把回归面扩到 T02 已验收的 58 条端点）。
4. E6 **完全绕开** `UserPermissionLoader`（`:42-43` appId==null 返空、`:46-47` permissions 非空永不回源，反向信任必踩第二道）。异常→40303、空集→40301 两码分离。缓存用**独立键 `mis:acl:skillperm:{userId}` TTL 300s**，空集写、异常不写；**严禁复用 `CacheConstants.RBAC_PERMISSIONS`**（含 appId、会污染登录态缓存）。
5. Q7 本期不落。E2 改**三档**：命中 skill 映射→用 skill 码；未命中→退判 **`agent:mcp:call`**（V20 已落地，节点 92060 / api 行 92141）；再没有→拒。原设计"全拒"会把 E2 打死。
6. E5 独立委派码本期不做（白名单+深度+worker剔除+下游递归四重兜底够 P0）。
7/8. 施工细节，确认固化。
9. **新增**：`/internal/permissions` **必须挂 `/internal/**`，禁止挂 `AgentOpsController`**。理由：PEP 只拦 `/api/v1/**`，挂过去会破坏 T02 刚验收的 58:58 双射（Flyway append-only 补救成本高）。鉴权在 `ReverseTrustConfiguration.addPathPatterns` 追加即可。
10. **新增**：§5 断言口径全量重写——Java/E6 侧 `HTTP 200 + body.code=40301 + body.data.code="AI_SKILL_FORBIDDEN"`；Python/E1–E5 侧维持 `HTTPException(403, detail.code)`。两栈 wire 形态**不强行统一**，只统一字符串语义标签。

**同步派给工程师 6 项只读核查（C1–C6）**：C1 mis-iam `/internal/v1/permissions/{userId}` 返全 App 并集还是 app-scoped（决定 E6 能否 app-scoped 判权、TC-34 是否适用于 E6）；C2 `ai:*:use` 在 PEP 是字面量还是通配符；C3 网关注入 LoginUser 带不带 appId；C4 `ApiPermissionInterceptor` 拒绝的响应形态（要与 E6 对齐）；C5 `IamWebClient.block()` 各失败场景抛的具体异常类型（决定能否分 40303/40301）；C6 Python `get_current_user` 能否拿到 appId（决定 `MisPermissionResolver.resolve` 签名与缓存键）。

## T03 裁定书 R1 修正案 + R2（主理人实读推翻/补齐，C1–C5 已全部结案）

**R1 修正案（推翻裁定 #3 的直连支取码）**：直连支 `LoginUser.getPermissions()` **恒为 null**，不存在"网关注入的真实码"。
- F9 `mis-common-security/.../support/LoginUserHeaderResolver.java:18-36` → 只 setUserId/TenantId/AppId/EmployeeId/Username，**从不 setPermissions**。
- F11 `ApiPermissionInterceptor.java:52-58` → `match.isEmpty()` 且 `deny-unmapped=false` 时**直接 return true**，L76-80 的回填根本执行不到（E6 未登记规则必走此支）。
- F12 `GatewayContextFilter.java:32-41` → finally 里 `SecurityContextHolder.clear()`；`ApiPermissionInterceptor:40-43` 对 `DispatcherType.ASYNC/ERROR` 直接 return true → **SSE 二次进入时 LoginUser 已空**，所以 T03 施工**禁止做成拦截器/AOP**，必须在 Controller 方法体内同步取。
- ⇒ 修正后：**E6 双支统一走 `iamWebClient.loadPermissions(userId)` 回源**，分支只决定 userId 来源（反向信任支从 `ReverseTrustContext` attribute 取，直连支从 `LoginUser.getUserId()` 取）。
- F10（C2 结案）：`ApiPermissionInterceptor:82-87` 是纯 `Set.contains` **字面量比较，无通配符**；叠加 F11（E6 路径提前 return），`ai:*:use` 是**死码**，风险从"可能误放"下调为"最多误拒" → 支持"不碰 `ai:*:use`"。
- C3 结案：直连支 appId **有值**（F9 L27 确实 setAppId），但 permissions 恒 null。
- C4 结案：PEP 拒绝形态 = **HTTP 200 + body.code=40300**，与 E6 规划同构，前端无需两套分支。

**R2 补充裁定（C1/C5 结案，3 条新裁定 #11–#13）**
- **F13（C1 结案）**：`/internal/v1/permissions/{userId}` 返回**跨 App 并集，非 app-scoped**。`mis-iam/.../service/PermissionService.java:38-46` 无 appId 入参；`SysRolePermissionRepository.java:15-20` JPQL 仅 `WHERE ur.userId=?1 AND rp.permType=?2`，**无 appId 条件**；`SystemMenuClient.java:37-45` 按 menuId 换码也无 appId。
- **F14**：`PermissionService.java:43` 写缓存用 `(tenantId, user.getAppId(), userId)`（appId 取自 SysUser 实体自带字段），键 `CacheConstants.java:56 RBAC_PERMISSIONS="mis:rbac:permissions:%d:%d:%d"` → **键 app-scoped 但值跨 App 并集**，既有语义错配，不在 T03 修复范围。
- **F16（C5 结案）**：`mis-admin-bff/.../client/AbstractDownstreamClient.java:73-85` `block()` **任何失败都抛 BusinessException，绝不静默**（BusinessException 原样透传下游真实码 / WebClientResponseException→50000 / 超时等→50000）；`RequestContext.java:64-72` unwrap：null→"下游无响应"、`!isSuccess()`→透传下游 code。⇒ `IamWebClient.java:206` 的 `List.of()` **只在「2xx+code=0+data 为 null」时触发**，语义是"查到了但没权限"。**40303 与 40301 可干净分离**。
- **F17（新风险）**：`mis-iam/.../support/RbacCacheSupport.java:47-54` 角色变更时**只 DEL `mis:rbac:permissions:*`**，不感知我们的独立键 → 权限撤销存在放行窗口。
- **裁定 #11**：E6 判权粒度 = **用户级跨 App 并集**，本期不做 app-scoped；S5 `InternalPermissionController` 端点**去掉 appId 参数**；backlog +Q9（app-scoped 判权 P2，需改 mis-iam 签名+JPQL，跨服务）。
- **裁定 #12**：40303/40301 判别式 = catch `BusinessException` 后按 `ex.getCode()` 分档——**40400（用户不存在）→40301**，**50000 系（超时/非2xx/无响应/下游故障）→40303**；未抛异常但空集或不含所需码 → 40301。
- **裁定 #13**：独立键 `mis:acl:skillperm:{userId}` TTL **300s → 60s**（用命中率换撤销窗口，E6 走对话触发 QPS 低）；backlog +Q10（撤销即时联动 **P1**，倾向方案①：读缓存时比对 `CacheConstants.java:69 RBAC_PERM_VERSION` 版本号不一致强制回源，不改 mis-iam）。
- 工程师核查范围**已缩到只剩 C6**（且降级为非阻塞——C1 已证实判不了 app-scoped，appId 只用于审计日志字段），另需回传 E1–E6 六条路径入口锚点（施工单要用）。

## 🔴 T03 裁定 #14（C6 自核挖出 userId 语义错配 · 水平越权风险）

**主理人亲核身份链，C6 结案的同时挖出比 C6 本身更重要的坑。**

- **F19**：MIS JWT `sub` = **userId**（`RsaJwtIssuer.java:46` `.subject(String.valueOf(claims.userId()))`；`JwtClaims.java:6-11` userId 与 employeeId 是**两个独立字段**）。
- **F20（决定性）**：Python `build_user_context`（`agent/ai-platform/backend/src/identity/models.py:273-296`）**L274 `user_id=str(p.employee_id) if p.employee_id is not None else str(p.user_id)`** → `UserContext.user_id` 装的是 **employeeId**；真正的 MIS userId 被塞进 **L291 `profile["mis_user_id"]`**。
- **F21**：`skills/reverse_trust.py:77-81` `resolved_user = user_id or session.user_id or identity["userId"]` → L119-120 写进 `X-User-Id` 头，**装的就是 F20 那个 employeeId**。
- **F22**：BFF `ReverseTrustInterceptor.java:183-198` 降级支（无上游 JWT）直接把 `X-User-Id` 当 userId 用；`:127` `loginUser.setUserId(ctx.userId())`。
- **F23**：`ReverseTrustContext.java:16-22` 已有 **`boolean fromUpstreamJwt`** 判别位（JWT 验签支 `:179` 置 true，降级支 `:197` 置 false）——现成，不用新增字段。
- **风险**：E6 若用降级支的 userId 去 `loadPermissions`，等于**拿 employeeId 当 userId 查权限** → 命中另一个用户的权限集 = **水平越权**（employeeId 与 userId 同为自增 Long，撞号概率极高）。
- **裁定 #14**：E6 反向信任支**仅接受 `ctx.fromUpstreamJwt() == true`**；`false` 一律 **40301 拒绝**，不做任何回源尝试。用例 TC-41/TC-42；backlog +Q11（P1：让 Python 降级链路带上真实 MIS userId 后再放开降级支）。
- **C6 本体结案**：`identity/mis_token.py:26-37 MisTokenPayload` 里 appId 在 `app_id`(L31)、MIS userId 在 `user_id`(L29)，Python **拿得到** appId；但因裁定 #11 已定不做 app-scoped，appId 仅用于审计字段。

### 待发裁定 #15（Python E1–E5 侧同类问题，故意暂缓到 v1.5 发）
挖 #14 时顺着往 Python 执行侧推，发现同一 bug 类在 E1–E5 爆炸半径更大：
- **F24**：`api/deps.py:112` RS256 支返回 `{"mis": True, **ctx.model_dump()}` → 顶层 `user_id` 是 employeeId(F20)，MIS userId 只在 `profile.mis_user_id`。
- **F25**：`api/deps.py:129` HS256 支返回 `payload.model_dump()`，而 `models.py:343-354 TokenPayload` 只有 `user_id/username/department/roles/channel/agent_id/iss/exp/iat` —— **无 tenant_id、无 app_id、无 mis_user_id、无 mis 标志**。该支（docstring 注明服务 H5 / 企微 Bot）**根本不存在 MIS 身份**，无法判 MIS 权限码 ⇒ 只能 fail-closed 拒，这是**产品级后果**（企微 Bot 用户技能执行会全拒），必须显式提请用户拍板而非闷头裁定。
- **F26**：`mis_user_id` 全仓**写 1 处读 0 处**（仅 models.py:291），无既有消费者，T03 可自由定义语义。
- **F27**：`api/routes/skill.py`(9 端点) 与 `api/routes/mcp.py`(9 端点) 函数签名**零 `Depends(`、零 `Header(`**，请求作用域内**根本没有身份对象**；`get_current_user` 全仓只被 agent.py/files.py/mis_capability.py/push.py 四处使用。`mcp.py:174 call_tool` = 任意 MCP 工具执行入口，直连匿名可调（对外经 BFF PEP 收口，但 Python 服务自身裸奔）。路由前缀见 `main.py:363-364`（`/api/v1/skills`、`/api/v1/mcp`）。
- ⇒ Python 侧 fail-closed **不是"选对字段"而是"先引入鉴权"**，工作量被原 spec 低估。
- **暂缓理由（重要流程教训）**：worker 唤醒只读最新一条消息，架构师正在吃 #11–#14 做 v1.4，此时插 #15 会导致它丢掉前四条。**改为 v1.4 落地回报后，以自包含 v1.5 增量单一次性下发。**

## v1.4 验收通过 + v1.5 增量单（裁定 #14–#18，2026-08-06）

### v1.4 验收（主理人验产物非听汇报）
`t03-failclosed-spec.md` **521 行**，#11/#12/#13 精准落地，附录 A **F1–F18** 齐，§6 S5 **已标解除阻塞**，用例 **40 条**。**TC-38「缺号」是虚惊**——v1.2 新增的是 **TC-04b** 占位，编号本就无 38，别再当 bug 查。**#14 零命中**（追加指令发出时它已停笔，9 小时前 23:51 停）。

### 用户两次拍板
1. 企微通道 fail-closed 取舍 → 选「**T03 内补身份映射**」（非严格全拒）；下发时机选「等 v1.4 落地后合并」。
2. 实地摸完发现方案地基是空的（见 F29/F30/F31），**重新拍板** → 选「**Python 侧手工绑定**」。

### 裁定 #15：Python E1–E5 身份链（三段边界，防 T03 膨胀）
- **#15-a（T03 内）** 统一身份解析 `resolve_mis_user_id`：RS256 取 `profile["mis_user_id"]`（**严禁**顶层 user_id，那是 employeeId）；HS256 查 `users.mis_user_id`；取不到 → fail-closed 403。
- **#15-b（T03 内）** Alembic `002_add_users_mis_user_id.py`：`users` 加 `mis_user_id BIGINT NULL` + 唯一索引，**只加列不回填**。
- **#15-c（拆出 T06，不阻塞 T03）** 绑定运营链路（接 `user_lookup` + 激活 wecom_sync + 运营台绑定页）。

### ⚠️ 主理人自纠：F27 警示曾把架构师带偏（已发纠正）
一度判断「Python 侧要先引入鉴权依赖、S2/S3 加码」——**对 E1–E5 是错的**。混淆了两个攻击面：
- `api/routes/skill.py`/`mcp.py` = **管理类 REST**，经 BFF PEP + 58 条 sys_api 收口，**不属 E1–E6**。F27 降级为旁路观察 → backlog **Q13(P2)**。
- **E1–E5 = Agent 工具注册表层**，身份走另一条通路且**一直都在**，织入点 `tool_registry_builder.py:527` 原本就对。

### 硬事实速查（F19–F41，已全部实读，别再重查）
**身份链**：F19 MIS JWT `sub`=userId（`RsaJwtIssuer.java:46`）；**F20** `models.py:274` `UserContext.user_id` 装 **employeeId**，MIS userId 在 `profile["mis_user_id"]`(L291)；F21 `reverse_trust.py:77-81`→X-User-Id 装的就是它；F22 BFF 降级支当 userId 用；F23 `ReverseTrustContext` **已有 `fromUpstreamJwt`** 判别位（现成）。
**Python 侧**：F24 `deps.py:112` RS256 支；**F25** `deps.py:129` HS256 支 `TokenPayload`(models.py:343-354) **无 tenant_id/app_id/mis_user_id/mis 标志**；F26 `mis_user_id` **写1处读0处**；F27 `skill.py`/`mcp.py` 零 Depends（旁路）；F28 `auth.py:187-194` 回退支 user_id=企微 userid 字符串；**F29** `models/user.py:78-108` users 表**无任何 MIS 标识列**；**F30** `routes/auth.py:101` 不传 user_lookup ⇒ 生产必走回退；**F31** MIS Java 全仓**零 wecom_user_id**；F32 Alembic 仅 1 个迁移，建表靠 `session.py:91 create_all`；F33/F34 `wecom_sync.py` 零引用、users 表**事实上无人读写**。
**工具层身份**：**F35** 身份来自 `ToolExecutionContext.metadata` 不经 Depends，`build_mcp_identity`(mcp_identity.py:35-48) 固定 4 字段 `userId/userMobile/channel/channelUserId`，**无 misUserId**；**F36** 唯一注入点 `oh_runtime_builder.py:218-223` → L233-237 `tool_metadata["identity"]`。⇒ 工具层 `identity["userId"]` 溯源就是 employeeId，E1–E5 直接用会踩同款越权。修法：`build_mcp_identity` 加第 5 字段 `misUserId` + 注入点填值 + `AclToolWrapper` 只认它（空串即拒），新增步骤 **S9**。

### 🔴 裁定 #16【严重】E2 第①档因工具名净化而永久失效（主理人抽查挖出）
- **F37** `tool_registry_builder.py:100-112 _sanitize_tool_segment` = `re.sub(r"[^A-Za-z0-9_-]","_",value)`；L184-186 工具名用**净化后**片段。
- **F38** `skills/registry.py:196` skill_id = `f"mcp-{server_name}-{tool.name}"` 用**原始未净化**名。
- **F39** `PlatformMcpToolAdapter.__init__` **L182 `self._tool_info = tool_info`** 完整保留原始名。
- **后果**：server 名 `member.profile` → registry key `mcp-member.profile-getUser`（带点）vs 工具名 `mcp__member_profile__getUser`（点变下划线）。若从工具名反解必 miss ⇒ **E2 三档退化两档**，全落 `agent:mcp:call`，per-skill 判权永久失效。`member.profile` 正是 §4.1 黄金向量样例，**非假想**。
- **裁定**：严禁反解工具名，必须取 `_tool_info` 原始名；取不到 → fail-closed。用例 **TC-46**。

### 裁定 #17【施工阻断】
**F40** `AiProxyController.java:291` `applySkillFill(@RequestBody SkillApplyRequest request)` **无 `HttpServletRequest` 形参**（skillExecute 有）。spec 原文让插 `assertCanRun(httpRequest,...)` ⇒ **照抄直接编译不过**。施工第一动作 = 先给签名加形参。

### 裁定 #18【配套 #14】
**F41** `resolveIdentity`(L321-328) 把两条信任支**揉平**，`ResolvedIdentity(userId,tenantId)`(L331) **不带 fromUpstreamJwt**。⇒ `SkillPermissionChecker` **不得复用它**，须自行读 attribute + `instanceof ReverseTrustContext` 后**先判 `fromUpstreamJwt()`**（false→40301 不回源）。**不改 `resolveIdentity` 本身**（波及已上线 FormFill 反向链路）。

### §1 落点表行号修正（工程师校验 + 主理人抽查复核）
4 处偏移：**E2 工具名格式在 L186 非 L190**；**E3-b `user-fill` 默认值在 L69/L48，L104 仅调用处**；**E6-a 签名 L264（L263 是注解）**；**E6-d 签名 L291（L290 是注解）**。
关键插入点**全部精确命中**：E6-b L267、E6-c L269、E6-e L295、E1-a L332、E1-b L527、E3-a L92、E3-c L335、E4-a L91/L32、E4-b L336、E5-a L187、E5-b L340。
E6-f/E6-h 经 grep 确认为真：`applySkillFill` 体内无身份解析；全文件无 `assertCanRun`/`@PreAuthorize`。

### v1.5 目标（编号收口后最终版）
用例 40→**47**（v1.5 基数 45 + TC-41/TC-42）；附录 A F1–F18→**F1–F41**；backlog +Q11(P1 降级支放开)/+Q12(P1 T06 绑定链路)/+Q13(P2 管理类 REST 裸奔)；裁定 **#11–#18** 全落地。

### ⚠️ 流程教训：给 worker 连发多封会造成编号冲突（本轮踩到）
我连发两封裁定，两封各自给 sanitize 漂移和 applySkillFill 缺参分配了 **同一批 F37–F40 编号**，#16 也一封整条、一封拆 a/b ⇒ 会污染 spec 附录 A 编号体系。
**已发第三封「唯一准据合并版」收口**，明确作废前两封的 #16–#18 / F37–F41 编号，并声明 v1.5 主体（#14/#15、F19–F36）不受影响、不要回退。
**教训固化**：① worker 只读最新一条消息 ⇒ 连发多封时，**后一封必须是前面所有封的完整超集**，否则前面的内容直接丢；② 事实编号（F 号）属于全局单调序列，**同一轮内不得并发分配**，要么一次发完，要么后发的显式声明作废前发的；③ 实现细节也别前后打架——我一封说"新增 `_raw_server_name` 两行属性"、另一封说"直接用 `_tool_info`"，已统一撤回前者（F39 已证 `_tool_info` 是单一数据源，再存一份违反单一事实源）。

### 并行派单：工程师查 sanitize 漂移现网影响面（只读）
判定 #16 是「存量已中招」还是「零存量但敞口永久」。四问：Q1 现网 MCP `server_name`/tool name 真实取值去重清单（顺 `tool_info` 赋值链追上游，可能在 yaml/json/DB/Nacos/代码常量）；Q2 含非 `[A-Za-z0-9_-]` 字符的命中率 N/M；**Q3 撞名检测**（不同原始名净化后相同 → 现网越权，最关键）；**Q4 首字符非字母触发 `mcp_` 前缀分支**（第二条漂移路径，裁定尚未覆盖）。
特别交代：若 `server_name` 上游是**运营可自由填写**的，则风险性质从"存量"变"**永久敞口**"，裁定要改方向。
