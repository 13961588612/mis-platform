# 2026-07-22 工作记录

## 微前端详细方案（各子系统接入）
- 用户需求：详细说明微前端原理 + MIS 各子系统接入方案。
- 关键发现（读 2026-07-22 代码）：当前已具备微前端雏形——PortalPage(门户/fetchApps 返回 AppItem 含 code/runtime/portalGroup) + AppLayout(工作台壳, KeepAliveOutlet/SideNav/TabBar) + SYSTEM_PAGE_DEFS(数据驱动页面注册) + SYSTEM_NAV(配置化导航)。这是天然拆分 seams，演进成本低于一般单体。
- 交付：`docs/frontend/micro-frontend-integration.md`。含：原理四要素、qiankun/micro-app/ModuleFederation 三方案详述+选型矩阵、当前架构 seams 映射、各子系统接入 spec(system 12页/monitor 试点/dashboard/未来 operations-platform)、SubAppManifest+ShellProps 契约(由 AppItem 扩展)、M0-M5 平滑演进里程碑(每步可回退)、风险(共享依赖单例/包体/样式隔离)。
- 结论：当前仍单体，守军规1(零跨域依赖)/军规2(Query不进store)+配置化路由+数据驱动页面，体量到阈值(M1-M5)再按域拆子应用。
- 关联：architecture-blueprint.md §9、admin-web-design.md、design-handoff-brief.md。
