# 2026-08-08 工作日志

## DEF-01 BFF 二次编码缺陷 —— 修复任务启动
- **背景**:Wave D 端到端联调(2026-08-07)发现 DEF-01:经 BFF 检索含中文/空格关键词一律 0 行。非 Wave D 引入,是 **Wave A 存量债**,被 6 处调用点复用。
- **团队**:`software-bugfix-def01-encoding`(主理人齐活林建),工程师 `software-engineer`(agent-479bdd01)已 spawn,执行中。
- **根因**:`backend/mis-admin-bff/.../client/KbWebClient.java` `buildUri(String,Map)`(951行)末行 `return builder.build().encode().toUriString();` 产出已编码字符串;6 调用点(500/518/527/550/643/751)用 `.uri(buildUri(...))` 传 `WebClient.uri(String)`,Spring `TEMPLATE_AND_VALUES` 把 `%`→`%25` 二次编码,下游解码乱码→0 行。
- **推荐修复**:`buildUri` 返回类型 `String`→`java.net.URI`,末行 `return builder.build().encode().toUri();`,6 处调用点不变(自动匹配 `uri(URI)` 重载,不再二次编码)。备选:6 处改 `.uri(URI.create(buildUri(...)))`。
- **验证要求**:JDK17 + classworlds 绕行 mvn(E2E 实跑);新增 `KbWebClientUriEncodingTest`(MockWebServer,断言 path 无 `%25`,中文→`%E5%AD%A3...`、空格→`%20`);既有 `KbWebClientSynonymPayloadTest` 12 例须仍绿。
- **git 纪律**:精确 add,禁 `git add -A`;暂未 commit/push(等主理人收口)。
- **状态**:⏳ 工程师修复中,待回报后派 QA 回归(真实栈验证 + MockWebServer 断言)。

## 集成栈环境(用户 2026-08-07 起,替代 Docker,仍可用)
- 监听:网关 8080、BFF 8081、微服务 8101–8108、TS 网关 3100、AI 后端 8000。PG/Redis/Nacos/MinIO 不在标准端口但应用已连用户替代实例。
- **鉴权配方(已验证)**:① `GET 8080/api/v1/auth/captcha` 取 `data.captchaId` + `data.imageBase64`(SVG);② 正则 `<text x='([\d.]+)'[^>]*>([^<]+)</text>` 按 x 升序拼出 captchaCode;③ `POST 8080/api/v1/auth/login` body `{"appCode":"system","username":"admin","password":"Mis@123456","captchaId":"..","captchaCode":".."}` → `data.accessToken`;④ 业务接口走 **网关 8080 + Bearer**,**不能直连 BFF 8081 带 token**(BFF 认网关注入的 X-User-Id 头,裸 JWT 401)。
- Wave D 端到端 K-1~K-6 已全消(2026-08-07 报告 `mis-kb-wave-d-integration-2026-08-07.md`),仅 DEF-01 待修。

## Wave A / D 整体收口状态(回答"是否全部完成"口径)
- Wave A:离线门禁 270 例全绿 + 端到端联调过,IS_PASS=有条件;§9 勾 3/4(切片重解析"可用"标 🟡 待 dev 栈)。遗留:T-2 缺 `KbDocumentServiceTest`(P1)、E-1~E7 环境项、D-1/D-2 文档债。
- Wave D:离线 128 例 + 端到端 38 例(36 过),IS_PASS=有条件(条件=DEF-01);K-1~K-6 全消;红线 WD-06/U4/40927 全清。
- 5 条技术债(规划 §11:11.1 parse_error 列 / 11.2 KB 全量 API 未登记 sys_api / 11.3 denyUnmapped fail-open / 11.4 public 库跳过 ACL / 11.5 审计脱敏盲区)明确不交付。
- **git**:全仓仍 **ahead 10 未 push**(Wave D 5 commit + Wave A 新增测试 + 本次 DEF-01 待合)。

## DEF-01 修复 + 回归 —— 已收口(08-08)
- **工程师修复(方案 C,非 A/B)**:`buildUri` 返回 `Function<UriBuilder, URI>`,走 `uri(Function)` 重载,保留 baseUrl 且只编码一次。A/B(`uri(URI)`)被否——反编译证明 `uri(URI)` 不走 `uriBuilderFactory` 会丢 baseUrl,静默打错主机。额外加固:参数值用 `{p0}` 占位 + `build(Map)` 展开,防 `{}` 当模板 / 堵查询串注入。
- **改动文件(暂存未 commit)**:`KbWebClient.java`(M, buildUri 重写 + Javadoc 锁死)+ `KbWebClientUriEncodingTest.java`(A, 13 例, MockWebServer 假 mis-kb, 断言 `getRawQuery()` 无 `%25`)。6 调用点(504/522/531/554/647/755)零改动。
- **QA 严过关独立回归 IS_PASS=通过**:单测 25/25 + 本模块 116/116 全绿;真实栈受控 A/B(另起 8091-8094 临时实例 + 8118 抓包器,**用户 8080/8081/8108 进程未动**)证明 keyword=季度 修复前 `%25E5%AD%25A3...`(双重编码)→ 修复后 `%E5%AD%A3%E5%BA%A6`(单次);线级抓包 35 条 Host 100% 正确(寻址无回归);Nacos LB 模式实测 7/7 一致无回归。源码缺陷 0。环境已恢复(临时实例停、数据清、Nacos 未污染)。报告 `deliverables/software-company/def01-regression-2026-08-08.md`。
- **A-1 护栏已补(工程师在 QA 回归后采纳)**:`KbWebClientUriEncodingTest` 13→**17 例**,新增 `AbsoluteAddressing` 3 例(反射取 `buildUri` 产物,断言绝对 URI scheme/host/port 齐全、LB 模式 host 保留 `mis-kb`、baseUrl base path 追加不覆盖)+ `RootCauseFossil.relativeUriCarriesNoHost` 1 例 + 集成用例加 Host 头断言。生产代码未再动,`git diff` 工作区 vs 暂存区 = 0 行;自测 29/29 指定 + 120/120 全模块全绿。暂存区 2 文件 544 插入。
- **方案 A/B 安全风险确认(比初判更重)**:工程师反向实验改方案 A,17 例挂 14,**11 例报 `HTTP 404` 但假 mis-kb 是 `createContext("/")` 全 200 不可能 404** → 证明请求静默发到**非预期真实主机**并拿到真实响应。这些请求带 `X-User-Id/X-Tenant-Id` 登录上下文头,不仅是功能故障,**沾数据外发风险**。若当初按 A/B 合入,是「双重编码功能 bug」换成「更隐蔽的安全问题」。值得在 ADR/编码规范留条。
- **QA Round 2 复核(08-08)**:工程师补护栏(测试13→17例)独立复跑 29/29 绿;生产代码 `git diff`+`sha256`+`javap` 三重取证逐字节一致(故未重跑真实栈)。方案 A/B 风险定性升级**独立求证确认**:baseUrl 指向无人端口,`uri(String)` 得 Connection refused(遵守 baseUrl),`uri(URI)` 相对路径得 404 落到本机 80 端口无关 IIS——确认会静默把带 `X-User-Id/X-Tenant-Id` 登录头的请求发到非预期主机=**凭证外发风险**;方案 C 已被 `AbsoluteAddressing` 用例永久封死。
- **DEF-02 新缺陷(需立项,QA 捞出+工程师独立核验确认存在)**:`mis-admin-bff` 用户管理 `GET /api/v1/users` 用户名搜索中文/空格/`%` → 500。栈 `IllegalArgumentException: Invalid character '张' for QUERY_PARAM` @ `IamWebClient.pageUsers(IamWebClient.java:74)`(`queryParamIfPresent("username",..)` + `.build(true)`,`build(true)` 声明已编码触发 `verify()` 抛异常)← `UserAggregateService.page:50` ← `UserController.page:40`;前端 `user-list-page.tsx:600-606` 无校验。紧急度 P2(真实可达必现,日志窗口 5 次异常全为 QA 探针无自然用户命中)。与 C-1 同源(`build(true)` 误用)但独立,分开立项。
  - **`.build(true)` 全量(工程师实测 21 处/8 文件,修正 QA 的 17 处/7 文件)**:QA 行首锚定 grep 漏 3 处单行 `builder.build(true).toUriString()`(`IamWebClient:188`、`SystemWebClient:85`、`KbWebClient:264`)。用户可控 String 仅 2 处(DEF-02=`IamWebClient:74` + C-1=`IamUserClient:43`);其余 19 处参数为 Long/Integer/ID 串,**当前巧合安全非约束**。工程师已逐个确认安全:`IamWebClient:114/123/176/188`、`SystemWebClient:71/85/95/178/198`、`KbWebClient:264`;**未核完**:`OrgWebClient` 6 处、`AuditWebClient:43`、`AuthWebClient:40`、`SystemMenuClient:44`(签名初判 Long/ID,工程师未全走到调用点,勿当已确认)。
  - **修法(与 DEF-01 不同病)**:DEF-01 病=编码两次,DEF-02 病=声称编码过其实没有。`build(true)`→`build()`(让框架编码)即够,不需 DEF-01 的 `{pN}` 占位;改完须补护栏(复用 `KbWebClientUriEncodingTest` 的 JDK HttpServer 写法)。根治建议:在 `AbstractDownstreamClient` 收统一查询串构建方法,属重构单独排期。
- **遗留(均不阻塞合入)**:C-1 `mis-auth/IamUserClient.java:38-45` 中文/空格用户名登录 500(同类编码债,待立项,失败模式=构造 URI 时抛异常,验证看异常日志而非抓包);C-2 `UserPermissionLoader.java:62` Redis 故障未兜底 500(健壮性问题,待立项)。
