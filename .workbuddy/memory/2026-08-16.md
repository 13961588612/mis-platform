# 2026-08-16 工作记录

## 组织人事域设计（部门管理 + 岗位管理）— 团队 software-org-position
- 用户原始 4 点需求（用户/部门/岗位/员工管理），本期只做「部门管理」+「岗位管理」设计（先设计再评审，暂不写码）。内置账号豁免手机号校验为已锁定跨模块约定。
- 已交付两份文档：
  - `deliverables/software-company/org-position-prd-2026-08-16.md`（PM 许清楚，简单 PRD，范围正确）
  - `deliverables/software-company/org-position-architecture-2026-08-16.md`（架构师高见远）
- 设计阶段修正的 PRD 关键缺口（代码核实）：
  - V1：BFF/gateway **无** `/api/v1/depts/{id}/staffing` 路由（内部 mis-org 已有 `DeptStaffingService.staffing()`）。前端要调编制接口必须 BFF 新增路由+facade+client（tenantId 由 BFF 注入）。已入任务 T-B2。
  - V3：部门管理页实为三 Tab（组织架构/岗位编制/组织穿透，V40 已实现）。PRD 自相矛盾（§4.1 写移除、N6 写保留）；架构建议保留组织穿透（R1 待拍板）。
  - V7：通用引擎 `FieldType.multiselect` 仅在表单实现，筛选栏只渲染 select/text；POST-02/03 多选筛选须改引擎（筛选栏渲染 multiselect + serverFilterKeys 跳过客户端二次过滤）。
- 任务拆分：T-B1（mis-org `GET /posts` 扩 deptIds/orgIds）→ T-B2（BFF 透传 + 编制路由）→ T-D（部门管理接真实编制）/ T-P（岗位管理改造）；前端 T-F1→T-F2/T-F3→T-P；T-V 联调+typecheck。新增依赖 = 0。
- 待用户拍板项（架构 §8，均给推荐默认值）：Q1 编制口径、Q3 部门侧配置、Q4 岗位单部门、Q6 新增表单组织上下文、Q7 组织多选含下级、Q8 缺编覆盖、R1 组织穿透 Tab 去留。
- **10:31 用户已拍板：全部按推荐默认值**（R1 保留穿透 / Q1 岗位数=已配置岗位数、任职数=有任职人的岗位数、缺编=差 / Q2 不增 headcount 表 / Q3 部门侧只读展示 / Q4 单部门 / Q6 树内先选组织再出部门树 / Q7 仅精确匹配 / Q8 实时算不可覆盖）。进入实现阶段。
- 实现+验收全部完成（团队 software-org-position-5dcd）：工程师 8 任务 T-B1..T-V 落盘、`IS_PASS:YES`、前端 typecheck 0 错误；QA 24/24 前端 + 17/17 mis-org + 10/10 BFF 全 PASS、9 项验收全 PASS、路由 NoOne。
- 真实落盘文件：mis-org PostController/PostService；BFF PostController/DeptController/OrgFacadeService/OrgWebClient + 3 POJO(DeptStaffingVO/PostStaffingVO/EmployeeLiteVO)；前端 depts.ts/posts.ts/types.ts/admin-list-page.tsx/dept-tree-page.tsx/page-defs.ts + 新 components/common/dept-tree-select.tsx。新增依赖=0、无 Flyway 迁移。
- QA 交付：`deliverables/software-company/qa/verification-report.md` + 测试源码（frontend/mis-admin-web/qa-tests/、2 个 Java 测试）+ `deliverables/software-company/qa/mvn-run.sh`（直调 classworlds Launcher+JDK17 绕过损坏 mvn）。
- ⚠️ 环境坑：本机 `mvn` 直调损坏（缺 classworlds），须用 `mvn-run.sh` 或 JDK17 直启 Launcher；团队空闲约被系统回收重命名（software-org-position→software-org-position-5dcd），任务随之清空需重建。

## 架构文档笔误修正（2026-08-16 11:27）
- 用户要求「改文档」：修正 `org-position-architecture-2026-08-16.md` 类图/POJO 描述 7 处字段名笔误——`PostStaffingVO.postTypeName→postType`、`EmployeeLiteVO.avatar→isPrimary`（均以内部 mis-org 真实代码为准：PostStaffingVO.java:11=`String postType`、EmployeeLiteVO.java:9=`Integer isPrimary`）。注意 PostVO（岗位列表响应）的真实字段就是 `postTypeName`（PostVO.java:12），第 201 行无需改。
- 已验证方式：grep 内部 mis-org dto 源码确认真实字段名，避免误改。

## 剩余模块启动（用户管理 + 员工管理）— 团队 software-org-user-employee
- 用户要求「剩余模块」：上一轮只做部门/岗位管理，本次做剩下的 ①用户管理（权限设置时组织多选+部门完整列出且多选，修正"组织无法多选/部门未完整多选"）②员工管理（手机号必填且唯一+内置账号豁免+部门树形单选，部门→岗位联动形态待定）。
- 已锁定跨模块约定沿用：内置账号豁免手机号校验。
- 可复用上轮资产：DeptTreeSelect 树形单选组件、`admin-list-page.tsx` 引擎 multiselect+serverFilterKeys+optionsFrom:'org'。
- 流程：先设计再评审（同上一轮）。已建团队 software-org-user-employee，派 PM 许清楚产出 PRD
- 架构设计已交付（`org-user-employee-architecture-2026-08-16.md`，另附 class.mermaid + sequence.mermaid）：9 章齐全、代码二次核实、新增依赖=0。关键发现：
  - USR：组织/部门多选后端已支持（`user-list-page.tsx` perms Sheet checkbox 多选 + `UserService.replaceUserOrgs/Depts`），本期仅确认。但 **N7 回填 bug**：`openPerms(row)` 只用 `row.orgId/row.deptId` 单值回填，`UserView` 无 orgIds/deptIds 列表 → 多组织/部门用户打开 Sheet 只回填首个，违反 USR-01/03 验收，已列 P1 修复项（T-FE-USR）。
  - EMP：手机号当前全链路可选（SysEmployee.phone nullable 无唯一约束、EmployeeService 仅校验 employeeNo）→ EMP-01/02 净新增；DeptTreeSelect 可复用（EMP-04）；GET /posts?deptIds= 已透传（EMP-05 直接复用 BFF）；SysEmployee 无 isBuiltin 字段（Q2 阻塞）。
  - 任务拆分：T-INFRA(V47 is_builtin 迁移+EMPLOYEE_PHONE_EXISTS 错误码)→T-BE-MODEL(SysEmployee/Repository/DTO/VO)→T-BE-VALIDATE(EmployeeService 校验)→并行 T-FE-EMP/T-FE-USR→T-VERIFY。
- 待用户拍板（架构给推荐默认）：Q1 部门"完整列出"=维持按已选组织聚合（阻塞项）；Q2 内置识别=新增 sys_employee.is_builtin(SMALLINT DEFAULT 0)+V47 迁移+DTO+表单（阻塞项，强推加字段）；Q3 手机号唯一=租户内；Q4 岗位=单行单选；Q5 允许多组织多部门；Q6 本期不纳入员工↔账号建链；Q7 部门树形保持单选。
- **11:57 用户拍板：全部按推荐默认**（Q1 维持聚合 / Q2 加 sys_employee.is_builtin(SMALLINT DEFAULT 0)+V47 迁移+DTO+表单 / Q3 租户内唯一 / Q4 单行岗位单选 / Q5 允许多组织多部门 / Q6 不纳入员工↔账号建链 / Q7 部门树形保持单选）。进入实现阶段，派工程师寇豆码按 T-INFRA..T-VERIFY 实现，再交 QA 严过关验收（标准 SOP 后半段）。

## 实现 + 验收完成（用户管理 + 员工管理）— 团队 software-org-user-employee
- 实现：工程师寇豆码按 T-INFRA→T-BE-MODEL→T-BE-VALIDATE→T-FE-EMP→T-FE-USR 落地全部代码，前端 `npm run typecheck` exit 0。关键发现：mis-iam `UserVO` 本就返回 `orgIds`/`deptIds` 数组，N7 回填修复纯前端即可，无需改 IAM 后端（比架构默认预期更优）。
- QA 严过关 2 轮验证：Round1 发现 isBuiltin 接线 2 缺陷（①新建默认内置→普通员工豁免手机校验失效；②EmployeeItem 未透传 isBuiltin→编辑静默清空内置标记），路由 Engineer 修复；Round2 全过，路由 NoOne。
- 最终：前端 typecheck exit 0、后端 mis-org+mis-common 编译 BUILD SUCCESS（用 `deliverables/software-company/qa/mvn-run.sh`）、9 项验收（USR-01/02/03、EMP-01~06）全 PASS、0 遗留、未 commit。
- 改动文件 13 个（后端 8 + 前端 5；前端含本轮缺陷修复 4 个增量叠加）：V47 迁移、ResultCode(EMPLOYEE_PHONE_EXISTS 40917)、SysEmployee、SysEmployeeRepository、EmployeeCreateRequest/UpdateRequest、EmployeeVO、EmployeeService、page-defs、employees、admin-list-page、types/api、user-list-page、types(AdminField.defaultValue)。
- ⚠️ 环境：后端仍须 `mvn-run.sh`（JDK17）编译，本机 mvn 损坏。

## 部门三视图合一 + 岗位类型多层化 + 岗位管理交互改造 — 团队 software-org-dept-post-redesign
- 用户 3 点需求：①部门管理 3 个 Tab 合一张表；②岗位类型支持多层+末级标记+仅末级可选；③岗位管理：3.1 所属组织改下拉列表、3.2 部门改下拉树、新建岗位选部门弹窗消失 bug、新建岗位岗位类型改下拉树仅末级。
- 设计：架构师高见远交付 `org-dept-post-redesign-2026-08-16.md`（+class.mermaid/sequence.mermaid），8 项决策全按推荐默认锁定（E.1 单树表+行内展开+下钻抽屉 / E.2 历史 5 条不重分类 / E.3 组织多选保持 / E.4 行内下钻抽屉 / E.5 仅末级可选 / E.6 组织选择器移出 Popover / E.7 部门树多选 / E.8 类型树表+层级列）。
- 实现：工程师寇豆码 21 文件（V47 迁移 1 + 后端 14 + 前端 12），IS_PASS=YES，前端 typecheck 0 错误。新增 BFF 接口 `GET /api/v1/post-types/tree`（透传 mis-org `/internal/v1/post-types/tree`）；DeptController 新增 `GET /api/v1/depts/{id}/staffing`（支撑 E.1 行内编制懒加载）。
- QA Round 1：typecheck PASS / 编译 9 模块 BUILD SUCCESS / Bug 专项 12/12 PASS，但回归 288 例 **1 FAIL** —— `BffApiRegistryDiffSurveyTest` 红灯：`depts` 域未登记端点无处置结论。根因=V47 新增 `GET /api/v1/depts/{id}/staffing` 未写 sys_api 注册表迁移 + 测试 REGISTERED_FIXTURE 漏登；同源 `GET /api/v1/post-types/tree` 也漏登且 PostController 未纳入测试扫描（逃逸）。路由 Engineer。
- **Round 2 修复中**：派工程师寇豆码 ① 新增 V48 迁移补登两端点（system:dept:staffing:view / system:post-type:tree，对齐 V40 sys_api 写法、id 避撞）② 测试 REGISTERED_FIXTURE 加 2 行 ③ registerAllControllers 注册 PostController。修完交 QA Round 2 回归。团队 software-org-dept-post-redesign-05e8。未 commit。

## Round 2 验收 + Round 3 补登（同一团队 05e8）
- QA Round 2：编译 9 模块 BUILD SUCCESS、typecheck 0 错误、全量 288 例仅 1 FAIL（BffApiRegistryDiffSurveyTest）。**本功能 V49 修复完全生效**（depts/post-types 失败闭环），但失败位移到断言④——暴露更早提交就存在的 **agent-ops 注册缺口**（被 Round1 提前中止掩盖，与本次功能无关）：`sessions/{id}/timing`、`sessions/timing/batch`、`skills/parse` 漏登 sys_api + `skills/builder/chat`(V46 已登) fixture 漏镜像。
- **Round 3 修复（工程师寇豆码，IS_PASS=YES）**：① 新增 `V50__agent_ops_sessions_timing_and_skill_parse_api.sql` 补登 3 端点（92173 skills/parse / 92174 sessions/{id}/timing / 92175 sessions/timing/batch，父 92090，四重 NOT EXISTS 守卫）；② 测试 fixture 补 4 行（上述 3 + 镜像 builder/chat）。预期测试 1 FAIL→0 FAIL。团队 05e8 未 commit。
- ⚠️ **重大存量隐患（已核实，超出本轮 scope，未改）**：`sys_api.id=92158` 被 V29（mcp/tools）与 V46（skills/builder/chat）两套迁移同时占用。V46 带 `WHERE NOT EXISTS(id=92158)` 守卫 → V29 先占后 V46 的 builder/chat **被跳过未插入**；且 V46 的 `sys_menu_api(92158→菜单92051)` 仍指向错位的 92158。后果：`POST /api/v1/agent-ops/skills/builder/chat` 运行时可能被 ApiPermissionInterceptor 拦（deny-unmapped）。这是仓库既存 bug，与本次改造无关。建议后续单独 SOP 修：用空闲 id 重插 builder/chat 并修正 sys_menu_api 关联。
- **QA Round 3 闭环（software-qa-engineer-3 从零实跑，因 qa-2 实例 TaskStop 工具故障中断后重派）**：编译 BUILD SUCCESS(7/7) / `BffApiRegistryDiffSurveyTest` Tests run 1 Failures 0（1 FAIL→0 FAIL） / 全量 ~339 例 0 失败 0 错误 / 前端 typecheck exit 0 / V50 三端点与 AgentOpsController 逐字一致。路由 **NoOne**。SOP 全闭环，working tree 未 commit。

## V29/V46 sys_api id=92158 碰撞修复 — 团队 software-bugfix-v29-v46-conflict（BugFix 路径）
- 用户要求修 V29/V46 在 sys_api 上 id=92158 的碰撞，并询问迁移进度 + V40/V47 是否冲突。
- 诊断（主理人亲自核查）：① 仓库最新迁移文件 = V50；服务器实际进度需 `SELECT version FROM flyway_schema_history ORDER BY installed_rank DESC LIMIT 1` 确认。② **V40/V47 无冲突**：V47 是纯 DDL（只给 sys_post_type 加 parent_id/is_leaf，不分配 id），V40 用 org 域 91xxx 段，两者无重叠。③ 真碰撞在 V29 vs V46（都写 id=92158 + code=00920059），V46 守卫 `NOT EXISTS(id=92158)` 恒 false → builder/chat 静默跳过 → 运行时 403（正是 V46 文件头警告的情形）。
- 约束：用户确认 V29/V46 已在服务器运行 → 严禁改已发布迁移，修复走新迁移。
- 工程师寇豆码交付 V51：`backend/mis-migrator/src/main/resources/db/migration/V51__agent_ops_skill_builder_chat_api_fix.sql`，用空闲且连续 id 92176 / code 00920077 补登 builder/chat（挂 catalog 92090），并插 sys_menu_api(92176, 92051=agent:skill:manage, 92176)；三重 NOT EXISTS 守卫，幂等。仅改测试注释（BffApiRegistryDiffSurveyTest:627 由"V46 已登记"改"V51 接管补登"），未动 V29/V46/V40/V47/V45。
- QA 严过关 Round1 实跑通过：`mvn-run.sh -Dsurefire.failIfNoSpecifiedTests=false` → Tests run 1 Failures 0 BUILD SUCCESS；builder/chat 已落入已登记段、未出现在未登记清单。路由 NoOne、IS_PASS YES。未 commit。
- ⚠️ 同源遗留（建议后续，不阻塞）：① `AgentOpsController.java:30` javadoc 仍写"见 V46"，应改"见 V51"（注释级，零功能影响）；② V50 的 92173-92175 三端点只有 sys_api 无 sys_menu_api 绑定（同类登记隐患）。

## Flyway 对服务器执行 migrate 报错的排查（22:25）
- 用户执行 `.\mvn.ps1 -pl mis-migrator flyway:migrate -Ddb.host=10.254.16.6 ...` 报错。
- 已核：① `backend/mvn.ps1` 仅要求 `$env:JAVA_HOME_17` 并 `& mvn @MavenArgs` 转发，参数名 `-Ddb.host/port/name/user/password` 与 `mis-migrator/pom.xml` 的 `${db.*}` 插值**完全对得上**（非参数名问题）。② 迁移文件夹是**稀疏集**：仅含 V4,V5,V40–V51，**缺 V1–V3、V6–V39**。若服务器 `flyway_schema_history` 是生产完整集（V1–V46 已应用），Flyway 校验会报 *Validate failed: … migrations applied to database but not found in classpath*，导致一条都不应用就退出——**最可能是本次报错根因**。
- 修复：先只读确认 `.\mvn.ps1 -pl mis-migrator flyway:info "-Ddb.host=..." ...`（不落库，仅连库+校验+列待执行）；若确为上述校验错，migrate 加 `-Dflyway.validateOnMigrate=false`（或 `-Dflyway.skipValidation=true`）再跑。
- **22:33 用户实测 `flyway:info` 不报错** → 推翻稀疏文件夹/校验失败假设；证明 DB 连接+账号+JDK17/mvn 环境此刻均正常（info 与 migrate 用同一套参数与环境）。故首次 migrate 报错极可能是**环境类**（当时 JAVA_HOME_17 未设/mvn 不在 PATH，被 mvn.ps1/shell 在 Flyway 前拦下），现已自愈。当前建议：直接重跑 `flyway:migrate`（无需 skipValidation），V47–V51 幂等应一把过。若仍报错，需用户贴 info 表格（V47–V51 哪些 Pending）+ migrate 完整报错（失败 migration 名 + SQLSTATE）才能定位执行期 SQL 错误。
- **22:36 用户重跑 `flyway:migrate` 成功** → V47–V51 已写入服务器 `flyway_schema_history`（DB 层修复落地）。下一步：① 只读确认 V51 落地（`SELECT ... sys_api WHERE id=92176 OR path_pattern='/api/v1/agent-ops/skills/builder/chat'` 应返回 1 行；`sys_menu_api` 关联 92051 应存在）；② 因 `deny-unmapped=true`，BFF 的 api-permission 注册表是启动时加载，**必须重启/重部署 mis-admin-bff** 才能加载到 92176、让 builder/chat 不再 403；③ 注意：本次仅应用了 DB 迁移，**org/dept/post 功能代码（21 文件 + V47-V50 对应改动）及 AgentOpsController 本身尚未构建部署**——若 builder/chat 代码已随现有 BFF 部署则只需重启，若未部署则需连同 BFF 一起发布。④ working tree 仍全未 commit。
- **23:0x 用户要求提交代码**：working tree 混了 3 条独立功能流（员工模块 / Org-部门-岗位+V29V46 / 智能体耗时埋点），按用户选择「按功能分多个提交」拆成 4 个 commit（本地 commit，未 push）：`5ef1454` feat(org)员工模块增强(15f/+981)、`5076db6` feat(org)部门三视图合一+岗位类型多层化+V29V46(42f/+4323)、`b08a974` feat(agent)会话耗时埋点(34f/+3389)、`4fcd77d` chore workspace记忆(3f)。**故意排除**：`backend/mis-migrator/tmp_checksum.py`（临时脚本）、`deliverables/software-company/qa/`（易变测试日志）。提交用系统 Git `C:/Program Files/Git/cmd/git.exe`（PortableGit 缺 git-remote-https）。
