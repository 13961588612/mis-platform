# 2026-08-13 工作日志

## ✅ 系统管理三页真实化交付提交 + 遗留处理（2026-08-13，标准 SOP 交付收口）
- **背景**：8-12 完成「系统管理三页示例数据真实化（全站 sample 清零）」（员工/岗位/系统参数 3 页接真实 DB，标准 SOP：PRD→设计→实现→两轮 QA）。今日收口：代码提交 + 遗留处理。
- **提交分组（3 个 commit，按仓库惯例）**：
  1. `feat(system): 系统管理三页真实化`（主交付 ~50 文件：mis-org 岗位 CRUD+员工全量端点、mis-system 系统参数 CRUD、mis-admin-bff 5+2 端点透传、V39 迁移种子+权限登记+按钮补授、前端 3 页 loader+api 封装、docs/deliverables）
  2. `fix(kb)+chore(ui): 遗留修复`（KB 测试编译债 4 处 10参→8参构造 + header-toolkit 文案中性化）
  3. `chore(memory): 工作日志`
- **遗留处理 3 项**：
  - KB 测试编译债：`KbEngineReconcileVO` 8 参 record vs `KbFacadeServiceTest`/`KbControllerDeleteReconcileTest` 10 参构造 → 去掉尾部 `0, List.of()` 修复，mvn 12/12 全绿（5+7）
  - 消息中心占位文案：header-toolkit.tsx「演示数据 · 后续接入消息中心」→「通知中心建设中」
  - ModulePage(/system/module) 无 def：**核实为误报**——keep-alive-outlet L85 挂 ModulePage → admin-list-page L1682 返回独立组件 ModuleManagePage（接真实 API），不走 SystemAdminPage 未配置分支，未改代码
- **门禁**：KB 两测试类 12/12 + 前端 typecheck exit 0 + KbEngineReconcileVO 本体未动
- **QA 截断教训**：QA 返回只有"等待 clean mvn 完成通知中"即结束（无完整报告）→ 主理人亲自复验门禁（mvn 从 backend/ 根跑：`cd backend && /d/software/mvn-fixed -pl mis-admin-bff -am test -Dtest=...`，仓库根会报 "Could not find the selected project in the reactor"）

## 🔧 岗位类型界面 + 组织穿透（2026-08-13，标准 SOP，未提交）
- **需求**（用户拍板）：①岗位类型管理界面 → 岗位管理页内子 Tab（不新建菜单）；②组织穿透 → 立项完整开发 + 部门天然归属组织（轻量语义）。
- **关键案例（设计核心）**：总部组织 → 百货分公司部门 → 无锡八佰伴部门（**手工打标 linked_org_id=组织"无锡八佰伴"**）→ 穿透模式浏览到该节点可下钻到该组织顶级部门树，递归。**穿透=锚点机制，非 org 树汇总**。
- **设计**：docs/backend/org-pierce-design-2026-08-13.md（D1-D6）+ class/seq mermaid。V40 迁移：sys_org.parent_id(NOT NULL DEFAULT 0+索引)、sys_dept.linked_org_id(NULL+部分索引)、sys_api 91195-91198/code 00900101-00900104、sys_menu 297-299(system:post-type:add/edit/delete 挂 285)+补授 role1、sys_menu_api 91284-91287。
- **实现**（T01-T05，~54 文件）：mis-org 组织树(BFS 环路+删除硬拦截 existsByParentId/existsByOrgId)、DeptService.pierce 懒加载 forest+validateLinkedOrg 自环校验+PUT 总下发 linkedOrgId、PostService 岗位类型 CRUD+countByPostTypeId 引用计数+删除 409 拦截、code 不可编辑；BFF 透传 /api/v1/depts/pierce + /post-types CRUD；前端 post-manage 子 Tab+post-type-manage-page+post-type-version-store、org-list 树形+上级下拉排自身子孙、dept-tree 穿透视图（钻取栈 pierceLevels+visited 集合防 A→B→A 循环+toast 警告+面包屑回退+只读）。
- **门禁**：mis-org/BFF 编译 SUCCESS、mis-org 测试 1/1、前端 typecheck 0 错、V40 段位全仓唯一（299 在 V19/V21 仅为注释区间，非冲突）。
- **QA 截断复发**：QA 再次只回"等待后台门禁通知"即结束 → 主理人亲自补做契约/迁移/边界核查。**经验：QA agent 跑长门禁时经常只返回中间状态，主理人必须自行复验门禁+静态审查，不能盲信通知**。
- **组织删除行为变更**：级联软删 → 硬拦截（Q4 拍板），mis-org 无既有测试依赖此行为，无回归。
- 未 commit（工作树保留，等用户授权）。
- ✅ **已提交**（`3a61a48` feat(org) 52 文件 + `51597e0` chore(memory)）。

## ✅ agent 平台工作线提交（2026-08-13 11:10，3 commit，工作树现已干净）
- `b6b6305` feat(agent): mis-rag 知识库问答链路（KB QA 编排下沉，4 文件 +298）：KbQaPipeline 全流程 visible-libraries→retrieve→拼 Prompt→生成→citations→落库；is_kb_qa_request（agent_id==mis-rag 或 capability==rag，前置 MIS_KB_QA_ENABLED）；session.py 路由。**用户问题"协调者丢给 mis-rag 会去知识库问答吗"→ 会（代码层面）**。
- `01b0503` feat(agent): T03 ACL fail-closed 增强 + 技能注册扩展 + 运营台助手 MarkdownView 渲染（8 文件 +157）。
- `bffc23b` style(ui): 登录/操作日志表格单元格样式统一（tdClass 单行截断，1 文件 +21）。
- **提交门禁**：pytest 33 用例全绿（.venv 在 agent/ai-platform/backend/.venv，用 ./.venv/Scripts/python.exe）+ 前端 typecheck 0 错。
- **教训**：git commit -m 用双引号时 `\n` 是字面量不会换行 → 用 `$(cat <<'EOF' ... EOF)` heredoc 写多行 message（已 amend 修正 b6b6305）。

## ✅ mis-rag 执行链路核实（2026-08-13 11:20，纯读码分析）
- **结论**：mis-rag **必调 mis-kb**，全程 5 类内部端点（QaInternalController，Java 侧）：① GET /internal/v1/kb/rag/resolve-visible（可见库 ACL 裁定）② POST /internal/v1/kb/rag/retrieve（检索→RAGFlow，NoopAdapter 空 hits 兜底）③ POST /internal/v1/kb/qa/sessions ④ POST /internal/v1/kb/qa/messages ×2（user+assistant）⑤ POST /internal/v1/kb/qa/citations/batch。
- **三个不直连**：mis-rag 不直连业务库（ACL 留 Java 侧）、不直连 RAGFlow（在 mis-kb 后面）、不直连 LLM（generate 回调由调用方注入——session.py 走 instance.process_message() Agent 运行时）。
- **鉴权**：优先透传用户 JWT（ctx.authorization），缺失回退 MIS_KB_AGENT_TOKEN 服务账号；透传 X-User-Id/X-Tenant-Id/X-App-Id/X-Trace-Id。
- **降级**：检索失败抛 KbClientError；落库失败静默降级（答案照返，sessionId/messageId 为空）；越权续聊被拒时丢弃未校验外部 sessionId 不回显。
- **配置**：MIS_KB_BASE_URL（本地 .env=127.0.0.1:8108，生产 http://mis-kb:8108）、MIS_KB_AGENT_TOKEN、MIS_KB_TIMEOUT_SECONDS、MIS_KB_QA_ENABLED。

## ✅ mis-rag 技能/人设配置生效边界（2026-08-13 12:30，纯读码分析）
- **人设 system.md（configs/agents/mis-rag/runtime/prompts/system.md）→ 生效，作用在「生成」环节**：loader.py L140-145 把 prompts.system 文件读成 system_prompt → oh_runtime_builder.py L219-223 与 build_runtime_system_prompt 拼接 → QueryEngine(system_prompt=...) 每次 LLM 调用注入；内容与 qa_pipeline._build_prompt 的硬编码「输出要求」（JSON answer+citations / 行内角标）同源一致。**只管回答纪律，管不了检索范围（检索=mis-kb ACL 裁定）**。
- **可用技能（skills/enabled-skills.yaml enabled: []）→ 当前完全无效**：KB 检索由 pipeline 前置调 mis-kb 完成不走技能系统；生成虽走通用运行时（process_message→QueryEngine，allowed_tools=[skill]），但技能集为空无可调工具。**配了技能 KB 分支也不走**，无需配置。
- **personality.md（agent.yaml memory.static.personality: memory/personality.md）→ 幽灵配置**：memory/ 目录整体不存在，代码无读取消费点（personality_file 仅是 config.py 字段），不生效不报错。建议删声明或补文件。
- **runtime.yaml 的 kb: 段仅是期望声明**，实际生效值走 MIS_KB_* env（get_settings）。
- **提交给 mis-kb 的内容**：仅 question/library_ids/top_k/threshold + 身份头（JWT/X-User-Id/X-Tenant-Id/X-App-Id/X-Trace-Id），**不提交技能/人设**，mis-kb 不消费 agent 配置。

## ✅ mis-rag→LLM 组装细节（2026-08-13 13:25，补证「人设+检索结果同一次调用」）
- **同一次 LLM 调用**：KbQaPipeline._build_prompt 构造的检索增强文本（`## 检索到的知识库片段` + `## 用户问题` + `## 页面上下文` + 输出要求）作为**用户消息** → mis_capability._generate/_generate_stream → instance.process_message → openharness.py build_native_query_engine(agent_config 含 system_prompt) + engine.submit_message(prompt) → gateway_api_client.stream_message → `_convert_messages_to_openai(request.messages, request.system_prompt)`（openharness.api.openai_client，system_prompt 变第一条 system 消息）→ LLM。
- **人设** = config.system_prompt = loader 读 system.md 内容 + build_runtime_system_prompt(oh 运行时提示) 拼接（oh_runtime_builder L219-223），每次调用都注入 system 消息。
- **小张力**：system.md 强制 JSON 输出，流式 structured=False 分支 user 消息要求「自然语言不要 JSON」——模型通常以更具体的 user 指令为准，且 pipeline 有兜底解析（_parse_generation 失败整段当答案 / _parse_inline_citations 无角标回退全部命中），不会挂；如需严谨可给 system.md 补「流式按用户消息要求输出」措辞（可选优化，非 bug）。

## ✅ mis-rag system.md 流式措辞已修补（2026-08-13 13:40，快速模式，未提交）
- 改 `agent/ai-platform/configs/agents/mis-rag/runtime/prompts/system.md`「输出要求（强制）」小节（+5/-1）：新增优先级总则「输出格式以用户消息中的输出要求为准」+ 拆「JSON 场景（默认）/流式·自然语言场景」两段；流式措辞与 qa_pipeline.py `_build_prompt` structured=False 分支逐字对齐（QA 逐句 6/6 验证）；「作答纪律」小节与 JSON 示例原样未动。消除 system.md 强制 JSON 与流式分支的指令张力。
- QA 独立验证 PASS（NoOne）：numstat +5/-1 吻合、范围仅此 1 文件（工作区另有 4+1 历史遗留改动：memory/.env.development/DeptService.java/dept-tree-page.tsx/新测试，勿混入提交）。
- 工作树未提交；system.md 修补未 commit，若要提交需 `git add agent/ai-platform/configs/agents/mis-rag/runtime/prompts/system.md` 单独隔离（其余 4 个历史遗留改动属另一批，勿混）。

## ✅ KB 智能问答会话管理现状（2026-08-13 13:35，纯读码分析）
- **前端 kb-qa-page.tsx 无「新建/删除会话」入口**：左侧仅「我的问答历史」列表（点开回看详情，`onOpenSession` 回填 turns + setActiveSessionId）；`onAsk` 把 `activeSessionId` 作为 sessionId 传后端，初始 null、提问完成后 `setActiveSessionId(result.sessionId)`、点历史会话后=该会话 id。
- **自动新增会话判断（两层）**：前端层 `activeSessionId==null`（首次提问/刷新后）→ 传 sessionId=null；mis-rag 层 `qa_pipeline.py _persist` L659-667：`req.session_id is None` → 调 `KbClient.create_session`（`POST /internal/v1/kb/qa/sessions`）自动新建，非空则续聊（越权验证失败丢弃外部 id）。mis-kb `KbQaService.createSession` 只落 KbQaSession 实体（owner=透传 X-User-Id，appId，createdAt），**无标题字段**。
- **mis-kb 用户侧 QaController 仅 4 端点**：GET /sessions/mine、GET /sessions/{sessionId}、POST /feedback、GET /sessions/{sessionId}/feedback——**无创建/删除/重命名会话端点**（创建端点在 QaInternalController 仅 agent 内部链路用）；`KbQaSessionVO` 仅 id/userId/appId/createdAt，无标题 → 历史列表只显示「会话 #id + 时间」。
- **结论**：自动新建合理（无活动会话即建），但缺手动新建按钮（续聊后想开新会话只能刷新页面）+ 缺删除会话功能（数据无限累积）——如需补齐是**新功能**（后端删端点+级联删除决策+前端入口+会话标题），非 Bug。

## ✅ KB 智能问答会话管理三功能已实现（2026-08-13 14:20，标准 SOP 全流程，未提交）
- **需求**：手动新建会话 + 会话标题（首问 30 字截断）+ 删除会话（软删除用户侧不显示）。团队 software-kb-qa-session-mgmt：PM PRD → 架构师设计 → 工程师 21 文件实现 → QA PASS(NoOne)。
- **V41 迁移**（backend/mis-migrator V41__kb_qa_session_title_and_soft_delete.sql，55 行三段）：① kb_qa_session 加 `title VARCHAR(255) NULL` + `deleted_at TIMESTAMPTZ NULL`（ADD COLUMN IF NOT EXISTS 幂等）；② **sys_api 登记 91199/00900083(module 91020)/parent 91172/DELETE /api/v1/kb/qa/sessions/{sessionId}/sort 78**（fail-closed 红线，V39 同款坑）；③ sys_menu_api **91288**/menu 91036(kb:qa:ask)/api 91199（复用既有授权无需补授 role）。
- **mis-kb**：KbQaSession+title/deletedAt；QaSessionCreateRequest/KbQaSessionVO record 末位 +title（**无 deletedAt 暴露**）；Repository +findByUserIdAndDeletedAtIsNullOrderByIdDesc（仅用户侧，运营侧 findByCreatedAtBetween/countByCreatedAtBetween 原样）；KbQaService createSession 落 title+truncateTitle 255 防御、listMySessions 软删过滤、+deleteSession 幂等软删（requireOwnedSession 用 findById 容忍已删会话重复删，越权统一 KB_SESSION_NOT_FOUND）；QaController +DELETE /sessions/{sessionId}（currentUserId 归属）。
- **mis-rag**：KbClient.create_session +title（非 None 才入 payload）；qa_pipeline +`_session_title(question,max_len=30)` 纯函数（strip、超 30 取前 30+"…"、空→None），_persist 新建时传。
- **BFF**：KbQaSessionVO(独立 VO)末位+title；KbWebClient+deleteSession；KbController +DELETE /qa/sessions/{sessionId}（挂 @OperLog 审计，R1 覆盖测试要求）；KbControllerRegistryCoverageTest 73→74。
- **前端** kb-qa-page.tsx：PageHeader「新建会话」按钮（清 turns+activeSessionId=null+保留 libraryId）；历史列表主行 `s.title ?? \`会话 #${s.id}\``+次行时间+hover Trash2 删除（stopPropagation）+Dialog 确认弹窗「删除后将在问答页不再显示」；删除后 filter+当前会话重置+toast。
- **门禁**：mis-kb 测试 9+1 全绿 / BFF test-compile 0 / pytest 820 passed / 前端 tsc 0 错误。
- **⚠️ 段位红线（高频踩坑）**：sys_api id 91199（V40 用到 91198）、sys_menu_api **91288**（91268 被 V39 占用，误写会因 WHERE NOT EXISTS(id) 静默跳过→绑定缺失）；code 00900083 是 **module(91020) 内唯一**（V35 module 4 也有 00900083，系统约束 uk_api_module_code 本就 module 内唯一，有跨 module 复用先例）。
- **未提交**：本次 21 文件 + 6 项历史遗留（Dept 守卫 3 件、.env.development、system.md、memory）全在工作树。提交时建议：KB 会话管理单独 commit（含 V41），Dept 守卫单独 commit，system.md 单独，env 调试改动单独确认。

## ✅ 已提交 4 commit（2026-08-13 14:15）
- `e13e2c8` feat(kb) 会话管理 21 文件；`5f3f84f` feat(system) Dept 守卫 3 文件；`78c000b` fix(agent) system.md；`0b203ec` chore(memory)。工作树仅剩 `.env.development`（8000→8002，未提交待用户定夺）。

## ✅ KB 删除/归档「引擎侧不存在」确认流已实现（2026-08-13 15:00，标准 SOP，未提交）
- **需求**：物理删除/归档时若 RAGFlow dataset 已不存在 → 提示确认后直接本地删。团队 software-kb-delete-missing：PM PRD → 架构师设计（Q1 查证：rename 不存在 dataset 双形态 HTTP404 或 code!=0+missing 文案，双兼容；Q2 裁决 VO 字段+HTTP200 非错误码）→ 工程师 18 文件 → QA PASS(NoOne，mis-kb 550 tests)。
- **机制**：新增 `EngineDatasetMissingException`（extends BusinessException，com.mis.kb.engine，code 50000）作跨 client→service missing 信号；`RagflowClient.deleteDataset` 404→missing（原静默幂等改为抛异常，**deleteDocument 仍走原 deleteWithJsonBody 404 幂等不变**）；`renameDataset` 404 或 code!=0 且 message 含 not found/not exist/不存在/missing（不分大小写）→missing；**ResourceAccessException 不判 missing**。
- **两段式确认流**：`DELETE /api/v1/kb/libraries/{id}?mode=&force=`（force 默认 false，sys_api 91149 已登记无需新登记，BFF @OperLog 自动采 force 进审计）。force=false+missing → HTTP200 提示态 VO{engineMissing=true, 本地零变更}；前端警示态+「确认本地删除/归档」（type-to-confirm 沿用）→ force=true 重调 → 跳过引擎直接本地删/归档。
- **force 语义（严格）**：只对 engineMissing 生效；**不豁免 deleteSupported 门控（40934）**；非 missing 物理失败仍 40935 回滚、归档非 missing 改名失败仍置 DRIFT_OR_FAILED(3) 待对账（R6 不变）；force 幂等（本地不存在+force=true → 幂等回执 0/0）；无 engineLibraryRef 不触发检测；**归档 force+missing 置 MISSING_IN_ENGINE(2) 不置 3**。
- **VO**：KbLibraryDeleteResultVO record 第 8 位 +boolean engineMissing（BFF 镜像 +Boolean engineMissing 可空兼容）。
- **测试**：RagflowClientMissingTest + KbLibraryServiceEngineMissingTest（两段式/幂等/40934 不豁免/40410/deleteDocument 404 保护断言）。
- **未提交**：18 文件 + `.env.development` 在工作树。提交建议：`feat(kb)` 删除引擎缺失确认流单独 commit。

## ✅ KB 展示增强两需求已实现（2026-08-13 15:10，快速模式，未提交）
- **需求 1**：搜索权限页主体表只显示 subjectId → 显示主体名称。BFF `KbAclVO` record 追加第 8 参 `subjectName`（可空）；`KbFacadeService.listAcls` 透传后调新增私有 `fillAclSubjectNames`（仿既有 fillSubjectNames，走 `subjectProxyService.resolveNames`，key=`type:id`），**异常 catch 降级返回原始列表**（名称回填失败不允许打挂接口）；前端 `KbAcl.subjectName: string | null` + kb-permission-page 新增「主体名称」列（colSpan 5→6）+ 撤销确认名称优先。QA 补 4 用例（三类主体回填/异常降级/缺字段保 null/空列表）。
- **需求 2**：知识库详情页显示 RAGFlow dataset id。后端链路早已完备（mis-kb `GET /libraries/{id}/engine-ref` 独立端点 = F8 红线妥协，权限码 `kb:library:engine-ref:view` + BFF @OperLog 审计；前端 getEngineRef + KbEngineRef 已封装），**唯一缺口是前端详情页未调用**。kb-library-detail-page：`Promise.all([getLibraryDetail, getEngineRef(id).catch(()=>null)])` 并行拉取，失败降级 null；基本信息 Tab「建库引擎」行后新增「RAGFlow Dataset ID」MetaRow（font-mono + 引擎同步状态 Badge，KB_ENGINE_SYNC_STATUS_META[status] 含 label/variant/hint）。
- **门禁**：BFF KbFacadeServiceTest 13/13 + KbControllerDeleteReconcileTest 6/6、test-compile EXIT=0、前端 tsc 0 错误。QA 路由 NoOne。
- **复用要点**：BFF 主体名称回填已有两处先例（KbAclSummaryVO 详情页 + LegacyAclInventoryVO 存量清单），listAcls 回填是第三处同构扩展——以后 ACL 相关新展示直接走 SubjectProxyService.resolveNames 批量回填。
- **未提交**：本次 7 文件（KbAclVO/KbFacadeService/KbFacadeServiceTest + types.ts/kb-permission-page/kb-library-detail-page/kb-api.ts JSDoc）叠加在任务 14 的 18 文件之上，全部在工作树。提交建议：任务 14 单独 commit（`feat(kb)` 删除引擎缺失确认流）+ 本次单独 commit（`feat(kb)` 权限主体名称与详情 dataset id 展示）。

## ✅ 已提交（2026-08-13 15:10，用户要求「一起提交」→ 两任务合并单 commit）
- `de2193e` feat(kb): 删除引擎缺失确认流 + 权限主体名称与详情 dataset id 展示（21 文件 +1502/-233，任务 14 的 18 文件与展示增强 7 文件合并提交，无迁移）
- `15687d6` chore(memory): 工作日志（本节之前的版本）
- 工作树仅剩 `agent/ai-platform/frontend/.env.development`（8000→8002 调试改动，未提交待用户定夺）。

## 🔧 BugFix：RAGFlow 已删 dataset 返回 403 lacks permission 漏判（2026-08-13 15:30，未提交）
- **用户实测**：本地物理删除知识库（该库之前在 RAGFlow 已手工删除）→ 报「引擎侧删除失败，本地未做任何变更：... lacks permission for datasets: 'xxx'」。
- **根因**：RAGFlow 批量删除接口 `DELETE /api/v1/datasets` 对**已不存在 dataset** 返回 **HTTP 403+lacks permission**（权限层先做归属校验：不存在→查不到归属→报无权），**不是 404**；原 missing 判定只认 404 → 403 落入普通失败分支。
- **修复**（2 文件，团队 software-bugfix-kb-missing-403）：`RagflowClient` 新增 `isForbiddenMissing(ex)`（status==403 且 body/getMessage 双来源任一命中 lacks permission，不区分大小写；**403+其它文案不判 missing**）；`deleteWithJsonBodyDetectMissing`（`missing.contains(status) || isForbiddenMissing(ex)`）与 `renameDataset`（`status==404 || isForbiddenMissing(ex)`）两处 catch 接入；`isDatasetMissingMessage` 追加 `lacks permission`（业务 code!=0 双保险）；deleteDocument 语义未动；javadoc 同步。`KbLibraryService` 未改（catch EngineDatasetMissingException 先于 catch(Exception)，L387/L457，无需动）。
- **测试**：RagflowClientMissingTest +5 用例（delete/rename 403+lacks-permission→missing、403+Forbidden→不误判、业务 code!=0+lacks-permission→missing）。
- **门禁**：mis-kb 全量 **Tests run=555, Failures=0, Errors=0**（QA 独立实跑，首轮后台被环境杀、前台重跑成功）；QA 路由 NoOne。
- **QA 非阻断建议（可选加固）**：isForbiddenMissing 内 getResponseBodyAsString() 极端场景（403+不支持 charset）可能抛 IllegalStateException → 可 try/catch 仅依赖 getMessage()；触发也仅退化普通失败分支不破坏数据，本轮未处理。
- **未提交**：2 文件在工作树（+ `.env.development` 遗留）。

## ✅ 已提交 403 修复（2026-08-13 16:00）
- `88981ed` fix(kb): 删除引擎缺失识别补 403 lacks permission（2 文件 +145/-24）；`eb14b41` chore(memory)。工作树已完全干净（.env.development 已不在改动列表，用户自行处理）。

## 🏗️ 「查看文档切分效果」已实现（2026-08-13 19:00，标准 SOP，未提交）
- **需求**：知识库文档管理页查看某文档被引擎切出的全量 chunk（内容/顺序/页码/关键词/字符数），支撑切片参数调优闭环。团队 software-kb-chunk-view：PM PRD → 用户拍板（方案 A 引擎全量接口 + 复用 kb:document:list + @OperLog）→ 架构师设计（**实测现网 RAGFlow 10.254.16.6:9380**）→ 工程师实现 32 文件 + 补齐 16 文件 → QA 独立验证全绿 NoOne（mis-kb 589 tests + 补测 9 用例）。
- **架构师实测结论（写死共享知识）**：① 端点 `GET /api/v1/datasets/{ds}/documents/{doc}/chunks` 可用，page 1-based，keywords 服务端过滤生效（total=过滤后总数）；② **per-chunk 无 token_count**（仅 doc 级有）→ chunk 卡展示字符数、统计条总 token 用 doc.token_count；③ content 无 `<weight>` 但命中带 `<em>` → 后端两步剥离（`</?(?:weight|sep|em)[^>]*>` + 残留 `<[^>]+>` 保留换行）+ 前端本地 `<mark>` 高亮（防 XSS）；④ positions 首元素即页码。
- **全链路**：文档行「查看切分」（Eye + PermissionGate kb:document:list，切片设置旁）→ 右抽屉（统计条双口径「共 chunkCount 块·命中 total 块」+ 总 token + 本页字符 + 300ms 防抖搜索 + 卡片分页 pageSize 50/上限 100 + 空态）→ BFF `GET /api/v1/kb/libraries/{id}/documents/{id}/chunks`（requirePermission + @OperLog recordParams）→ mis-kb `GET /internal/v1/kb/libraries/{id}/documents/{id}/chunks`（**权限双闸门**：requireLibrary + requireDocumentRead 复用 KbVisibilityService.hasPermission READ 同命中测试）→ RagflowAdapter（MIS id 翻译 + cleanContent）→ RagflowClient.listChunks。**全程只读**。
- **状态降级**：pending/parsing →「解析中暂无可查看的切分结果」；failed →「解析失败，请先重解析」；engineDocumentRef 空 →「尚未同步到引擎」；引擎异常 → 空 VO+hint「引擎暂不可达」不打断接口。
- **V42 迁移**：sys_api **91200**/code **00900084**/parent 91170(V36 文档 catalog)/sort 57 + sys_menu_api **91289**(91288 已被 V41 占用)/menu 91034(=kb:document:list,V13)/module 91020；三重去重+父节点存在性校验。
- **QA 补测 9 用例（回归护栏）**：CleanContent 6（crlf 归一/空行压缩/script 标签剥离/带属性标签/孤立 `<` 不误剥/null keywords→空列表）+ KbWebClientListChunksEncodingTest 3（真 HttpServer 断言原始 query 无 %25 双重编码、keywords 中文/空格还原、空白关键字不发送）。工程师顺带修了 HEAD 就存在的 BffApiRegistryDiffSurveyTest V40/V41 fixture 缺失（绿灯化）。
- **门禁**：mis-kb 全量 **589/0/0/0**；BFF 受影响 26/0/0 + 模块 surefire 累计 275 tests 0 失败；前端 tsc exit 0。QA 路由 NoOne。
- **经验**：① 本环境后台任务（run_in_background）长 mvn 易被系统杀（输出停在中间无汇总）→ 门禁一律前台重跑验证；② 工作树是混合状态——`agent/ai-platform/**` + `features/agent/**` + `kb-qa-page.tsx` + `kb-ticket-dialog.tsx` 是另一套并行「问答点赞/吐槽反馈+来源披露」功能，与本任务无关，提交时严禁混入。
- **未提交**：本任务约 48 文件（32+16）在工作树（+ 并行分支改动）。提交建议：`feat(kb)` 文档切分效果查看单独 commit（含 V42），与并行分支改动严格隔离。

## ✅ 已提交文档切分（2026-08-13 22:05）+ 并行分支门禁红灯（未提交）
- `b917c55` feat(kb): 查看文档切分效果（**33 文件 +2231/-8**，精确隔离：mis-kb 18 + BFF 10 + V42 迁移 1 + 前端 4；kb-api.ts/types.ts diff 复核确认只含 chunks 代码，无反馈类混入）。
- **并行分支「问答点赞/吐槽反馈+来源披露」36 文件仍留在工作树，门禁红灯不能提交**——Python 全量 **828 passed / 4 failed**，全部为并行分支引入：
  1. `test_no_third_party_imports[coordinator/trace.py]`：trace.py 新增 import redis → 违反第三方依赖红线
  2. `test_a1_worker_receives_self_contained_brief`：委派 mis-rag 时 MagicMock `MIS_KB_BASE_URL.rstrip()` 崩溃（invoke_agent.py 新增 `_run_child_kb_qa` 在 mock 环境访问 settings）
  3. `test_depth_is_incremented_inside_worker_and_restored_after`：同 2 根因（委托 mis-rag 失败）
  4. `test_raised_max_parallel_allows_overlap`：worker_sessions 回归
- **结论**：并行分支尚未达到可提交状态，待其所属工作线修复 4 个失败后再提交；本次仅提交了已全绿的文档切分。
