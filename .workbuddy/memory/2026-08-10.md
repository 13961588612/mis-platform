# 2026-08-10 编码缺陷 Sweep（DEF-01 收尾 + DEF-02/C-1 开修）

## 用户三项决策（本轮）
1. **DEF-01 仅 commit 不 push**：已精确提交 `2562ec2`（仅 `KbWebClient.java` M + `KbWebClientUriEncodingTest.java` A，544 插入/18 删除），未 push，其他 ahead 提交未动。旧团队 `software-bugfix-def01-encoding` 已无残留。
2. **新建 sweep 团队修 DEF-02 + C-1**：团队 `software-bugfix-encoding-sweep` 已建，工程师已派工（task `agent-0d13df07`）。
3. **C-2（UserPermissionLoader Redis 未兜底 500）与 21 处 build(true) 统一封装重构 → 单独排期**，不在本次 sweep。

## 关键工程洞察（致后续接手者）
- DEF-02/C-1 的根因同 DEF-01：`.build(true).toUriString()` 产出已编码字符串再传 `WebClient.uri(String)`/`RestClient.get().uri(String)`，`TEMPLATE_AND_VALUES` 把 `%` 二次编码；且 `build(true)` 对非 ASCII 直接抛 `IllegalArgumentException` → 500。
- **修复范式（照搬 KbWebClient 方案 C）**：改走 `uri(Function<UriBuilder, URI>)`（WebClient）或相对 `URI`/Function（RestClient），让查询串**只编码一次、baseUrl 不丢**。**绝不能**只把 `build(true)` 改成 `build()` 继续传 String——会重演二次编码。
- DEF-02 精确点：`mis-admin-bff/.../client/IamWebClient.java` `pageUsers` :64-76（改 :74 的 `build(true)`→Function 范式）；同文件 :114/:123/:176/:188 也有 `build(true)`，:188 的 `kind` 是 String 需审计是否用户可控。
- C-1 精确点：`mis-auth/.../client/IamUserClient.java` `findByUsername` :38-45（RestClient，改 `getAuthUser` 走 Function/相对 URI）。
- 21 处 `build(true)` / 8 文件清单（工程师核过，QA 早前漏 3 处单行写法）：IamWebClient:74/114/123/176/188、SystemWebClient:85、KbWebClient:264、IamUserClient:43、OrgWebClient/AuditWebClient/AuthWebClient/SystemMenuClient 其余。其中用户可控自由文本仅 2 处（DEF-02 + C-1），其余为 Long/Integer/ID 串“巧合安全非约束”。

## 测试基线（沿用，JDK17 + classworlds）
- 范式：`backend/mis-admin-bff/src/test/java/com/mis/adminbff/client/KbWebClientUriEncodingTest.java`（JDK `com.sun.net.httpserver.HttpServer` 起假下游，断言 `getRawQuery()` 无 `%25`）。
- sweep 新增 `IamWebClientEncodingTest`（mis-admin-bff）+ `IamUserClientEncodingTest`（mis-auth），`@SpringBootTest` 覆盖 iam base-url 指向假服务器，断言请求命中测试服务器 + raw query 解码回中文/空格原值 + 无异常。
- 命令：JDK17 直启 `D:/software/apache-maven-3.9.16/boot/plexus-classworlds-2.11.0.jar`，`-pl mis-admin-bff,mis-auth -am -Dtest='IamWebClientEncodingTest,IamUserClientEncodingTest'`。注意 `@Nested` 外层 `Tests run:0` 正常。

## 收口（工程师交付 + 主理人独立验证兜底 + 落库）
- **工程师交付**：DEF-02（`IamWebClient.pageUsers` :93-117 改 `uri(Function)` 范式）+ C-1（`IamUserClient.findByUsername`/`findById`/`getAuthUser` 改 Function 范式，RestClient 侧），IS_PASS: YES。新增 `IamWebClientEncodingTest`（12 例）+ `IamUserClientEncodingTest`（11 例），均 JDK HttpServer 起假 mis-iam，含 RootCauseFossil 反向护栏。21 处 `build(true)` 审计表：仅 #1(用户名)/#6(用户名) 用户可控已修，其余 19 处数值/ID 串“巧合安全”保持不动。
- **QA 异常**：QA 子进程标记 completed（22m54s）但报告正文未回传主理人。按铁律不能代写 QA 结论，主理人启动**独立验证兜底**：① 复跑定向 23 例（IamWebClientEncodingTest 12/12 + mis-auth BUILD SUCCESS）；② 复跑两模块全量 `Tests run: 132, Failures: 0, Errors: 0`，BUILD SUCCESS；③ 独立复核审计 #5（`listApps` 全仓仅 2 调用点 `AgentOpsGrantService:183` 传 null、`AppController:48` 传字面量 `"subsystem"`，无用户输入，工程师判定站得住）。验证层结论：修复有效、无回归、审计成立。
- **落库**：精确 commit `cda463e`（仅 4 文件：`IamWebClient.java` M / `IamUserClient.java` M / 两个新测试 A；776 插入/23 删除），未 push。工作区其他无关改动（.workbuddy/memory、docs、deliverables、frontend/tsc）原封未动。
- **团队清理**：`software-bugfix-encoding-sweep` 已无残留（TeamDelete 返回 Not in a team）。

## C-2 收口（2026-08-10 用户拍板"立项"并已完成）
- 走 BugFix 快捷路径：团队 `software-bugfix-c2` 已建，工程师派工（task `agent-05cdd6c0`）。
- 根因：`UserPermissionLoader.readRedis`（:62-64）调 `redisTemplate.opsForValue().get()` 无 try-catch，Redis 故障抛 `RuntimeException` 冒泡出 `load()` → 权限加载 500。修复：`readRedis` 包 try-catch（catch Exception，含 opsForValue() 取连接步骤），Redis 异常 `return null`（降级=缓存 miss，落入 `load()` 回源分支兜底）；仅改方法体，不重构。
- **验收**：工程师 IS_PASS: YES（9 例新测试 + 141 全量全绿 + 反向护栏）。本次 QA 报告**完整回传**（吸取 DEF-02/C-1 教训），独立复现 9/9 + 141/141、逐类加总核对 132 存量+9 新增=141、独立复现反向护栏（裸调用恰好 3 失败 6 绿）、审计确认仅改 readRedis 且 opsForValue() 被 try 罩住，路由 NoOne。
- 精确 commit `0242294`（仅 2 文件：UserPermissionLoader.java M + UserPermissionLoaderTest.java A；273 插入/2 删除），未 push。团队 software-bugfix-c2 已清理。

## KB 详细设置设计咨询（仅需分析，团队 software-kb-settings-analysis）
- 用户问：① 知识库详细设置界面是否增加嵌入模型/重排模型选择；② 切片参数能否文件级 vs 知识库级。
- **现状事实（已核实）**：① `RagSettings.embeddingModel`（库级 String）**已存在且前端表单已有自由文本输入框**（kb-library-page.tsx），建库期下发 RAGFlow dataset `embedding_model`（RagflowClient:125-127）；重排**只有库级开关**（`rerank` Boolean），模型 ID 是平台级 `mis.kb.engine.rerank-model-id`（MIS_KB_RERANK_MODEL_ID），库级不可选模型。② 切片参数（chunkMethod/chunkTokenNum/separator）在**库级** RagSettings（存 kb_library.rag_settings_json），建库/更新 dataset 时下发（chunk_method + parser_config.{chunk_token_num,delimiter}）；**KbDocument 实体无任何配置字段** → 文件级当前不可能；切片字段属建库期参数（RetrieveQueryResolver:316 只合并检索 5 字段）。
- 分派：产品经理（task agent-53e494cc）做模型选择产品分析（竞品 Dify/FastGPT/RAGFlow/Coze 对标）；架构师（task agent-096ef44f）做切片粒度技术评估（RAGFlow per-doc chunk 能力核验 + mis-kb 改造面 + 推荐方案）。只分析不改码，结论回主理人汇总后由用户拍板。
- **遗留（非严重，供排期）**：Redis 整体宕机时请求全穿透到 mis-iam 有惊群风险；熔断/本地短 TTL 兜底建议单独立项，不在本次。

## 知识库概览「检索引擎 类型：noop」答疑（本轮）
- 用户问概览页显示「类型：noop · 状态：UP · engine reachable」对不对。结论：显示逻辑无 bug，但 noop 表示未启用真实检索引擎。
- 链路：前端 `health.engineType` ← BFF `KbFacadeService.engineHealth()` 调 `kbWebClient.engineType()` ← mis-kb `KbEngineConfigService.engineType()` → `enginePort.engineType()`；`RagflowProperties.type` **默认即 "noop"**。
- `NoopAdapter.health()` 返回 `EngineHealth.up()`（healthy=true,status="UP",detail="engine reachable"），写死常量，故 noop 永远显示健康——是设计如此，非探测真实服务。
- 三种类型（Nacos `mis.kb.engine.type` 决定，前端不可在线切）：`ragflow`（真实）/ `noop`（默认，检索静默返空，安全降级，retrieve 打 WARN）/ `mock`（本地桩数据）。
- 判据：本地跑通主流程 → noop 是预期；联调/生产期望真检索 → noop 说明没配 ragflow，需改 Nacos `mis.kb.engine.type=ragflow` + base-url + api-key + rerank-model-id 并重启 mis-kb。

## 知识库 noop 实锤排查（2026-08-10 二查，用户让"查一下"）
- **实测**：`curl localhost:8108/internal/v1/kb/engine/type` → `data:"noop"`；`/health` → `status:"UP", detail:"engine reachable"`（NoopAdapter 写死）；`/capabilities` 全 UNSUPPORTED。**运行实例就是 noop**，页面如实渲染。
- **矛盾点**：远程 Nacos（10.254.16.6:8848）integration 命名空间 `mis-kb` 配置默认 `type: ${MIS_KB_ENGINE_TYPE:ragflow}` + base-url `http://10.254.16.6:9380`；且**远程 RAGFlow 活着**（根路径 HTTP 200）。mis-kb 实例已注册到该 Nacos（10.254.121.94:8108 healthy）。
- **结论**：进程没吃到 Nacos 的 type 值 → 最可能是 IDE Run Configuration 设了 `MIS_KB_ENGINE_TYPE=noop`（同占位符同时覆盖 jar 默认与 Nacos 默认），或该进程启动时 Nacos config 未加载回落 jar 默认 noop。查证方法：curl 远程 Nacos `/nacos/v1/cs/configs?dataId=mis-kb&group=MIS_GROUP&tenant=integration`。
- **三环境配置**：test=noop（本地/无引擎）、integration=ragflow(10.254.16.6:9380)、prod=ragflow（key 由运维注入）。⚠️ integration 的 api-key 明文进了 Git（`deploy/nacos-config/integration/mis-kb.yaml`），与注释"切勿提交真实 key"自相矛盾，供留意。
- 本地连接线索：`.env.integration`（MIS_REMOTE=true / NACOS_SERVER=10.254.16.6:8848 / NACOS_NAMESPACE=integration / DB/Redis 均 10.254.16.6）；`local-server.txt` 记 `nacos 10.254.16.6:8848`、`ragflow 10.254.16.6:9380`。Docker Desktop 当前未运行。

## 知识库设置模型答疑（嵌入/重排/切片层级，2026-08-10）
- **嵌入模型**：库级、可选。`RagSettings.embeddingModel`，前端「RAG 设置」Tab 文本框，placeholder「留空使用引擎默认」；非空才下发 RAGFlow `embedding_model`（`RagflowClient.updateDatasetSettings` :125-127）。
- **重排模型**：**不需要选模型**。平台全局统一 `mis.kb.engine.rerank-model-id`（jar 内默认空=禁 rerank），库级只有「启用重排」布尔开关（前端 WA-06 区明示"不可单独指定模型"）；检索期经 rerank_id 生效，建库期不下发（RagflowClient :105-108）。
- **切片方法**：**知识库级，不支持文件级**。chunkMethod/chunkTokenNum/separator 全挂 `RagSettings`（T03/V15），经 `updateDatasetSettings` 下发 RAGFlow `chunk_method`+`parser_config{chunk_token_num,delimiter}`（:129-141）；上传 `uploadDocument(libraryId, file)` 只传文件本体，重解析仅触发 `parseDocuments`，均无文件级参数入口。改切片只影响此后新解析文档（前端 WA-10 重解析引导）。
- 关键文件：`RagSettings.java` / `RagflowClient.java` :98-152 / `kb-library-detail-page.tsx` :382-525。

## RAG 设置页企业级充分性评审（2026-08-10，团队 software-kb-settings-review）
- 用户问：知识库详情设置界面作为企业级产品参数是否足够。走部分工作流（仅分析），派产品经理许清楚（task agent-08113813）对标 RAGFlow/Dify/Coze/千帆/百炼评审。
- **总体结论**：检索核心调参够用，企业治理维度不足，充分度 **5.5/10**。现有参数仅 10 项：检索（topK/阈值/检索方式/嵌入模型/向量权重/rerank 开关）+ 切片（方法 6 种/长度/分隔符）+ 空结果策略。
- **P0 缺口**：操作审计日志（等保合规硬性要求）。
- **P1 缺口**（挡企业落地）：检索过滤（文档/标签/时间/密级限定，引擎 metadataFilterSupported 已留但页面无入口）、结果去重、OCR 开关与语言+overlap（parser_config 现仅下发 chunk_token_num/delimiter）、解析进度/失败原因/重试（引擎 progress 未透出）、文档版本管理、过期/批量下架、容量配额、模型下拉选择（现自由文本手填）、敏感词/脱敏、检索侧 QPS/延迟/未命中 Top 可观测。
- **Top5 建议**：① 检索过滤与范围限定（首推）；② 解析治理可视化+OCR/overlap；③ 反馈→调参闭环（复用已有看板/反馈数据）；④ 模型选择器+用量统计；⑤ 生命周期与审计。
- **不建议做**：库级重排模型选择、文件级切片覆盖、HyDE/query 改写、RAPTOR/图谱、自研模型管理。
- **落地分期**：一期（2-4 周）模型下拉+解析可视化+OCR/overlap+审计+简单检索过滤；二期 版本/过期/去重/脱敏/配额/反馈建议；三期 文档级权限/数据源同步/质量体检。
- 注：QA 反馈/看板/工单/同义词/命中测试等平台能力已存在，评审未重复主张为缺口。团队待收尾。

## KB 双 Bug 修复（2026-08-10，团队 software-bugfix-kb-doc-sync，已验收）
**用户报**：① 停用文件后 RAGFlow 仍启用/可检索；② 切片分隔符设 "###" 无效果。

**B1 根因（三重证据实锤：远程 RAGFlow 10.254.16.6:9380 实测+代码+日志）**：`RagflowAdapter.setDocumentEnabled` 旧实现「停用=deleteDocument（不可逆删）、启用=空操作」，且注释声称的"mis-kb 侧 enabled 过滤"是假的——`retrieve`/`KbRetrieveService`/`KbHitTestService` 检索链路全无过滤。RAGFlow 真实端点：`PUT /api/v1/datasets/{id}/documents/{docId}` body `{"enabled":0|1}`（**必须整数，布尔被拒 code:102**），实测停用后命中 7→0、恢复 0→7，文档/chunks 保留。

**B2 根因**：`RagflowClient.updateDatasetSettings` 用 `postFor` 发 **POST** `/api/v1/datasets/{id}`，RAGFlow 更新 dataset 是 **PUT**（实测 POST 405 MethodNotAllowed，设置从未到达引擎；mis-kb.log 有 `RagSettingsService.syncToEngine ERROR: ...405...` 实锤）。且该实例 PUT body pydantic 严格校验(extra=forbid)，`top_k`/`similarity_threshold` 任一出现全拒 code:101。

**修复（M 文件）**：`RagflowClient.java`（新增 putFor()；updateDatasetSettings POST→PUT + body 只留 embedding_model/chunk_method/parser_config{delimiter,chunk_token_num}，删 top_k/similarity_threshold；新增 updateDocumentEnabled）；`RagflowAdapter.java`（setDocumentEnabled 改真实启停、删删除逻辑；retrieve 加本地 enabled≠1 过滤，doc==null 维持旧行为透传；reparseDocument Javadoc 说明）；Noop/MockAdapter 注释；`kb-library-detail-page.tsx` 两处文案改「删除、再重新上传才会生效（重新解析不应用新分隔符）」。

**QA 独立验证**：JDK17 classworlds `-pl mis-kb -am clean test` → BUILD SUCCESS，**Tests run: 163**（150 存量+QA 新增 13，逐报告加总核对）；新增 `RagflowClientHttpTest`（本地假 RAGFlow HttpServer + 真实 RestClient 字节断言：PUT 非 POST/白名单四键/enabled 整数/停用发 PUT 非 DELETE 等）；前端 `npm run typecheck` EXIT=0。**路由 NoOne，验收通过**。

**⚠️ 用户操作路径变更**：改分隔符后对存量文档点「重新解析」**不会**应用新分隔符（RAGFlow 用文档上传时 parser_config 快照）→ 必须**删除后重新上传**才生效。

**🔴 待立新任务（工程师实测发现，超本次范围未改）**：
- **疑似第三 Bug·检索格式**：`RagflowClient.retrieve`(:291+) 发扁平字段（top_k/similarity_threshold/keyword/vector_similarity_weight 在 dataset_ids 平级）被 RAGFlow **静默忽略**——嵌套 `retrieval_setting` 才认，同组参数嵌套命中 7 条、扁平 0 条；且响应 chunk 字段是 `content` 非 `text`，RfChunk DTO 需核对。检索可能一直在用引擎默认参数跑。
- **DELETE 405**：本实例 SDK API DELETE /datasets/{id}、DELETE /documents/{id} 均 405，`deleteDocument/deleteDataset` 用 toBodilessEntity 吞错误体→删除库/文档可能静默失败。
- **探测残留**：工程师在 RAGFlow 建过探测库 `mis-kb-bugfix-probe-20260811`（DELETE 405 清不掉），需人工在 RAGFlow 控制台删除。
- 改动未 commit/push，工作区有前序会话遗留脏改动（KbDocumentService/KnowledgeEnginePort/RfDocument 等，非本次）。

## B3 检索参数修复（2026-08-10，团队 software-bugfix-kb-retrieve-format，已验收）
**用户报**：处理上轮工程师发现的「疑似第三 Bug」——检索参数未生效/可能空结果。

**根因（远程 RAGFlow 10.254.16.6:9380 实测）**：
1. `RagflowClient.retrieve` 请求体 `keyword` 映射：hybrid→**true**（旧实现），该实例 keyword=true 走全文候选、query 无全文命中返回 0 条——**空结果直接根因**；且 `top_k` 误塞 effectiveTopK()（top_k=候选数非输出条数，截断召回）。
2. 响应 `data` 是**对象** `{chunks, doc_aggs, total}` 非数组，chunk 字段 content/similarity/document_keyword 而非 text/score/document_name——旧 `RfResponse<List<RfChunk>>` 反序列化失败/全字段 null。
3. **⚠️ 上轮"嵌套 retrieval_setting 才认"是误判（已翻案证实）**：实测 retrieval_setting 内所有字段（含 bogus 字段）被静默忽略、回落默认；那次扁平 0 条只因带 keyword:true。**正确格式=顶层扁平**：question/dataset_ids/page_size(=topK 输出条数)/similarity_threshold/keyword/vector_similarity_weight/rerank_id(顶层可选)，**不加 top_k/retrieval_setting/search_method**。

**修复（M/A）**：`RagflowClient.java`（retrieve 去 top_k、keyword hybrid→false：vector→false/keyword→true/hybrid→false、page_size=effectiveTopK、响应改 RfResponse<RfRetrievalData>）；`dto/RfChunk.java`（content→text、similarity→score、document_keyword→documentName、删 page_num_int、firstPage() 从 positions[0][0] 解析防御式）；`dto/RfRetrievalData.java` A（{chunks,doc_aggs,total}+chunksOrEmpty）；`dto/RfDocAgg.java` A（{doc_id,doc_name,count}）；`RagflowClientHttpTest.java`（mock 响应改 data 对象+新字段；RetrieveBodyFormat 嵌套类）。

**QA 独立验证**：JDK17 classworlds `-pl mis-kb -am clean test` → BUILD SUCCESS，**169** 单测（167 存量 + QA 补 2 条：无 search_method 断言 + firstPage 解析 + data 对象解包）；surefire 逐文件加总双口径核对；远程实测四连证（keyword true→0/false→5 条、嵌套含 bogus 照常返回、page_size=1 恰 1 条 top_k 不影响、data 结构字段集合吻合）；前端 typecheck EXIT=0。**路由 NoOne，验收通过**。QA 进程曾因等后台任务被提前标 completed、报告未回传，SendMessage 唤醒补交（同 DEF-02 教训）。

**⚠️ 遗留（配置层，需用户操作）**：`deploy/nacos-config/*/mis-kb.yaml` 的 `rerank-model-id: qwen3-rerank` 是**裸模型名**，该实例拒绝（code:100 Provider not found）→ 需改**全限定格式 `qwen3-rerank@Tongyi-Qianwen@Tongyi-Qianwen`** 重排才生效；或保持空=安全禁用。**B3 代码未改配置文件**。
- 检索方式语义：该实例无真正 hybrid 双通道候选（search_method 被忽略），MIS hybrid 现以 keyword=false + 权重混合打分实现；纯 keyword 检索仍 keyword=true（无全文命中会空属预期语义）。
- 探测库 `mis-kb-bugfix-probe-20260811` 已由 QA 清理（远程仅剩 1 库）。**DELETE 405 仍待处理**（deleteDocument/deleteDataset 用 toBodilessEntity 吞错误体→删除库/文档可能静默失败，未在本次范围）。

## KB 设置改造立项（2026-08-10 用户拍板，标准 SOP 开发，团队 software-kb-settings-dev）
- **用户三项决策**：① 立项开发；② 模型池来源 = **RAGFlow 服务器已配置模型**（嵌入/重排都探测拉取，不自建配置表）；③ 切片 = **方案 B（库级默认 + 文件级覆盖）**。
- 采纳的产品形态（来自前两轮分析报告）：嵌入模型=创建向导下拉/编辑态只读/存量自由文本灰态兼容；重排模型=库级下沉+继承全局 `mis.kb.engine.rerank-model-id`+不可用回退全局默认（P0 必含降级）；切片文件级=上传可选切片方式/token/分隔符+来源回显+改参触发重解析。
- 关键代码事实：`RagSettings`（kb_library.rag_settings_json）已有 embeddingModel String + chunkMethod/chunkTokenNum/separator（库级）；`KbDocument` 无配置字段（需 Flyway V23 加 rag_settings_json）；RAGFlow 支持文档级切片=**上传后 PUT 更新文档 + 重解析**两步式（非上传时指定），embedding 仍 dataset 级无向量空间风险；改造面约 4-6.5 人日（T01 数据层→T02 合并校验→T03 引擎适配→T04 前端→T05 联调）。
- 风险要点：RAGFlow 版本差异（chunk_token_num vs chunk_token_count 键名，联调确认）；改文件级参数先删旧 chunk 再重切（解析期间文档不参与检索，UI 明示）；文件级显式设置=固化不跟随库级（R4 语义）。
- ⚠️ 与 B3 遗留联动：rerank-model-id 需全限定格式 `xxx@Tongyi-Qianwen@Tongyi-Qianwen`（裸名被拒 code:100）；本次模型池探测应直接返回全限定 ID。
- 流程：PRD（product-manager，task agent-886c426e）→ 架构设计+任务分解 → 代码 → QA → 主理人精确 commit（不 push）。PRD 落盘 `deliverables/software-company/kb-settings-model-chunk-prd-2026-08-10.md`。

## KB 设置改造收口（2026-08-10，commit `aff1f8a`，未 push）
- **工程师实现**（T00–T05，含一次 499 瞬态取消后 SendMessage 续跑）：T00 探测实锤 **GET /api/v1/models**（/llm/list 404）、模型 id 全限定 `name@instance_name@provider_name`（裸名 code:101/100）、文档级 PUT 白名单 `chunk_method`+`parser_config{chunk_token_num,delimiter}`（parser_config 内未知键 code:102，键名 chunk_token_num 非 count）、**PUT 后不自动重解析须显式 POST /chunks 两步式**。T01 数据层（V23 三列可空 + RagSettings rerankModelId 末位零 DDL + DocumentChunkConfig/EffectiveChunkConfig/DocumentChunkConfigResolver）；T02 模型池（EngineModel/EngineModelPool/EngineModelPoolService 60s TTL + unavailable 降级 + RfModel + RagflowClient.listModels + EngineConfigController GET /engine/models + BFF 透传）；T03 重排库级合并（RetrieveQueryResolver S4 库级??全局 + peekPool 校验回退全局 + degradedReasons，Resolver 铁律不破）；T04 文件级切片（DocumentUploadInput 末位 chunkConfig + RagflowAdapter 两段式上传 + KbDocumentService 落库三列 + updateChunkConfig）；T05 前端（types/api/store/library 两页/document-table + 2 新弹窗组件）。
- **QA 独立回归**：IS_PASS 通过、路由 NoOne、0 阻断。mis-kb **188**（169 基线+19 新增，逐类加总核对，无测试删除/弱化）、mis-admin-bff **141** 全绿（不带 clean 与带 clean 双模式 BUILD SUCCESS，证实工程师所述 clean 失败=Windows 文件锁间歇环境问题）；前端 typecheck EXIT=0；T00 curl 实锤复核（embedding=text-embedding-v2/v3/v4、rerank=gte-rerank/gte-rerank-v2/qwen3-rerank）；RagSettings 构造点 11 参末位无错位；上传不带参与现状一致（无多余 PUT）；V23 幂等。
- **落库**：精确 commit `aff1f8a`（56 文件、5107 插入/177 删除）。⚠️ **含 B1/B2/B3 历史遗留补交**（RfChunk/RfDocument/RfRetrievalData/RfDocAgg/RfDocumentPage/RagflowParseStatusMapper/RagflowClientHttpTest/RagflowParseStatusMapperTest/MockAdapter/NoopAdapter 注释/kb-overview-page 等）——因与本次改动同一批文件（RagflowClient 等）且存在编译依赖（listModels 引用 RfModel、B3 部分引用 RfRetrievalData/RfChunk），无法拆分，一并补交（均此前已验收）。无关杂项未提交（nacos yaml 配置密钥/start-dev.ps1/.workbuddy/logs/tsc-out 构建产物/nul/历史报告等仍在工作区）。
- **QA 非阻断建议**：RagflowAdapter.probeModelPool/两段式 uploadDocument/updateDocumentChunkConfig、RagflowClient.listModels/updateDocumentConfig、KbDocumentService.upload/updateChunkConfig 无直接单测（现状=EngineModelPoolServiceTest 5 例 + 读码 + T00 实锤），下轮补 HTTP 级单测钉死「无 chunkConfig 无多余 PUT」与「全限定 id 拼接」。
- **待用户操作**：RAGFlow 需配置可用 embedding/rerank 模型后，模型池下拉才有真实选项；`deploy/nacos-config/*/mis-kb.yaml` 的 rerank-model-id 若用需改全限定格式（B3 遗留，未在本次改）。

## 待排期（用户已拍板单独排期）
- **RAG 设置页企业级增强（2026-08-10 用户确认"先记录，后期做"）**：基于上方评审（充分度 5.5/10）。一期优先（2-4 周）：① embeddingModel 改下拉选已注册模型；② 解析进度/失败原因/一键重试可视化（引擎 progress 透出）；③ 切片区加 OCR 开关+语言+chunk overlap；④ 操作审计（谁改参数/删文档）；⑤ 检索过滤简单版（按文档/时间，联动命中测试）。二期：文档版本/过期/批量下架、结果去重、敏感词脱敏、容量配额告警、反馈→调参建议。三期：文档级权限下沉、数据源同步、质量体检、多租户容量。P0=操作审计（等保）。立项时从评审报告取完整差距清单。
- **统一封装重构**：21 处 `build(true)` 借修复收口到 `AbstractDownstreamClient` 统一查询串方法（中风险，改动面大，单独排期）。
- 注：`git push` 仍 pending（DEF-01 `2562ec2` + DEF-02/C-1 `cda463e` + C-2 `0242294` + 仓库 ahead 10+ 均未推远端），待用户授权。

## 技术债一批处理（2026-08-10，团队 software-techdebt，commit `c29d92c` 未 push，仓库 ahead 15）
- **T1 DELETE 405**：`RagflowClient.deleteDataset/deleteDocument` 弃 toBodilessEntity 吞错，新增 `deleteFor(uri,prefix)` 统一出口：RestClient 默认 errorHandler 对 4xx/5xx 抛 RestClientResponseException→BusinessException + 状态码二次 2xx 双保险。注释明确「PUT enabled=0 停用」≠ 删除（B1 语义已被 updateDocumentEnabled 占用）。新增 `RagflowDeleteHttpTest` 4 例。
- **T2 惊群防护**：`UserPermissionLoader` 本地短 TTL 缓存（ConcurrentHashMap+时间戳，零依赖，3s 默认、上限 8192），**仅 redisDown 时参与**（健康期穿透语义不变、过期回落回源，C-2 降级兼容）。新增 `UserPermissionLoaderLocalCacheTest` 5 例。
- **T3 统一封装**：`AbstractDownstreamClient.queryUri(path,name,value,...)` 返回 Function<UriBuilder,URI> 静态方法（单次编码、baseUrl 不丢、null/空白跳过、特殊字符整体编码），21 处 `.build(true)` 收口（Audit1/Auth1/Iam4/Kb1/Org6/System5/SystemMenu1）；**DEF-01/02/C-1 已修点不回退**；全局主代码 0 残留。新增 `DownstreamClientQueryUriTest` 8 例。
- **T4 HTTP 补测**：`RagflowUploadAndModelsHttpTest` 9 例——无 chunkConfig 无多余 PUT、两步式 POST→PUT→POST 顺序固定、updateDocumentConfig body 白名单（extra=forbid）、listModels 全限定 id 拼接。
- **T5 配置安全**：integration api-key 明文→`${MIS_KB_ENGINE_API_KEY:}`；rerank-model-id 裸名→全限定 `qwen3-rerank@Tongyi-Qianwen@Tongyi-Qianwen`；prod/test 新增 `${MIS_KB_RERANK_MODEL_ID:}`（空=安全禁用）；integration YAML 头注释 ASCII 化（防 Windows JVM GBK 解析故障）；**.gitignore 补 `.env.integration`**（QA 发现含明文 key 未被忽略）。
- **回归**：mis-kb 201/201（188+13）、mis-admin-bff 154/154（141+13）、mis-iam test-compile 过，QA 独立复跑逐文件加总双口径核对，路由 NoOne。QA 遗留非阻断：`.env.integration` 已忽略✓；prod/test YAML 中文注释残留（存量）；mis-iam SystemMenuClient 无直接单测（menuIds 纯数字串巧合安全）；T2 本地缓存返回共享 Set 引用（只读使用低风险）。
- 提交 18 文件 +1203/−141，未 push；工作区 `kb-library-page.tsx` 前序遗留 UI 微调未并入（与本次无关）。

## P1 三项（2026-08-10，团队 software-kb-p1，commit `8c571ea` 未 push，仓库 ahead 17）
- **P1-3 前端（唯一代码改动）**：`kb-document-upload-dialog.tsx`（+62/−2）`suggestChunkMethodByFileName` 扩展名映射（md/txt/json/html/doc/docx→naive、pdf→paper、xls/xlsx/csv→table、ppt/pptx→presentation、jpg/jpeg/png→picture、其他→naive），映射值与前端 `KB_CHUNK_METHOD_OPTIONS`（types.ts:819-829）及后端 `DocumentChunkConfig.VALID_CHUNK_METHODS`（9 码值 naive/qa/paper/book/laws/presentation/table/picture/one）一致；`chunkMethodTouched` 用户手改后不覆盖、reset 重置。typecheck EXIT=0。QA 复核路由 NoOne。
- **P1-1 换嵌入模型（联调实测，零代码改动）**：RAGFlow **支持在线 PUT 切 embedding_model**（200/code=0，探测库 probe-p1-embed-switch-0812 实测 v4→v2），但**光切不重切检索必坏**（ES `field [q_1536_vec] does not exist` 映射错误）；切+全量重解析（POST /chunks）后恢复；**DELETE dataset 405 删除重建不可行**（与 T1 一致）→ 可行路径=在线切换+全量重解析。MIS 换模型链路已通（KbLibraryService.update→updateDatasetSettings PUT embedding_model 非空才下发）；**缺「库级一键重解析」接口**（当前仅 DocumentController 单文档 reparse，BFF/前端均仅单文档）——产品功能缺口待决策。
- **P1-2 重排端到端（联调实测，零代码改动）**：本地 mis-kb 已是 ragflow 引擎（.env.integration MIS_KB_ENGINE_TYPE=ragflow），engine/models → globalRerankModelId=qwen3-rerank@Tongyi-Qianwen@Tongyi-Qianwen（全限定注入生效）；RAGFlow retrieve 带 rerank_id 排名/score 真实重排（银联退款 #5→#3 等）；裸名 code=100 / 全限定 code=0；MIS hit-test rerank 开/关 402→588ms 差异可见；RetrieveQueryResolver S4（:213-252 库级??全局、globalConfigured 闸门、peekPool 校验回退+degradedReasons）读码核对无缺口。
- **QA 复核要点**：前端枚举两侧集合一致无漂移；「库级批量重解析」全仓确认不存在（Controller/BFF/api 三处均单文档）；远程实测证据与代码路径一致性核对无矛盾且与 T00/B3 记忆吻合。
- **⚠️ 操作注意**：工程师联调把「运维」库 2 文档从 disabled 恢复 enabled（B1 遗留停用状态），检索恢复 14 chunks；探测库 `probe-p1-embed-switch-0812` + 历史 `probe-t00-0810` 待人工 RAGFlow 控制台清理（DELETE 405）。
- **遗留**：P1-1 换模型后重解析完成前检索报错需产品定 UI 提示；库级一键重解析接口待立项；前端 suggestChunkMethodByFileName 无单测（无 vitest，仅 typecheck）。

## 库级一键重解析（2026-08-10，团队 software-kb-reparse-all，commit `2848aff` 未 push，仓库 ahead 19）
- **需求背景**：P1-1 换嵌入模型的最后一公里——RAGFlow 在线切 embedding_model 后必须全量重解析恢复检索（q_1536_vec 映射错误）；此前仅单文档 reparse。
- **mis-kb**：`DocumentController` POST `/{libraryId}/documents/reparse-all`；`KbDocumentService.reparseAll` 遍历库内全部文档逐个触发（**同步/异步取舍**：RAGFlow POST /chunks 引擎端异步执行，串行提交即可不重复入队）；单文档失败不中断（失败明细 List.copyOf 不可变）；PARSING 短路幂等跳过；空库返回 success=0 明确结果；无引擎映射文档计失败、库无引擎映射抛 KB_LIBRARY_NOT_FOUND；新增 `KbReparseAllResult` record（total/success/failed/skipped/failedDocuments）。
- **BFF**：KbController→KbFacadeService→KbWebClient 三层透传，`KbReparseAllResultVO` 字段逐一对齐（纯透传不加工）。
- **前端**：`kb-document-table.tsx`「全部重解析」按钮（kb:document:edit 权限门、空库禁用、reparsingAll 防重入）；确认弹窗含**换模型期提示**（重解析期间检索可能不可用/刚切嵌入模型解析完成前可能报错——静态提示为最小诚实方案，未做运行时探测）；结果 toast（失败明细前 5 条）；`kb-api.ts`/`types.ts` 对齐。
- **⚠️ 主理人独立验证兜底发现并修复测试代码 bug**：`KbDocumentServiceReparseAllTest` 2 处 Mockito void mock 语法错误（`when(...).thenThrow(...)` 对 void 方法编译失败"此处不允许使用'空'类型"）→ 改 `doThrow(...).when(enginePort).reparseDocument(...)` + 补 doThrow import。**教训：工程师交付后测试编译不过（未真正跑测试/报告声称全绿不实），QA 又因团队通信丢失未上场，主理人独立核验兜底时发现**。
- **验证**：mis-kb **212/212**（201 基线 + 8 service + 3 controller，逐文件加总核对）、mis-admin-bff **155/155**（154 + 1 KbWebClientReparseAllTest）、前端 typecheck **EXIT=0**。
- 提交 13 文件 +713/−1；**排除**：AgentOpsController/AgentOpsFacadeService（前序 config-files 遗留）、kb-library-page.tsx（前序 UI 微调）、agent.yaml/coordination.yaml、docs/knowledge-base-phase2-plan.md、run-tests.bat、.workbuddy/* 等。
- **团队通信教训**：本批工程师完成通知到但交付报告正文未回传、SendMessage 报 Not in a team（团队上下文随会话切换丢失）、后台任务句柄失效——主理人走独立核验兜底（读码+亲跑测试），流程可复现但非标准。

## YAML 中文注释 ASCII 化收口（2026-08-10，团队 software-yaml-ascii，commit `5eebc7d` 未 push，仓库 ahead 21）
- **背景**：Windows JVM 默认 GBK 解析 UTF-8 中文注释 YAML 抛 MalformedInputException → 配置回退 noop。T5 只 ASCII 化 integration/mis-kb.yaml 头注释，prod/test 及新增 rerank 行注释仍中文（存量）。
- **改动**：8 文件 23 行注释（prod/test mis-kb.yaml 各 6、integration/prod/test mis-admin-bff.yaml 4/2/2、integration/prod/test mis-common.yaml 各 1）；保守翻译保留语义（「严禁写真实 key 进 Git」警示、「空=安全禁用」、noop「health 恒绿/联调需改 type=ragflow」）；§S-04 非 ASCII 拉丁符号一并 ASCII 化。
- **QA 最强证据**：git diff 100% 为 # 注释行（非注释行过滤零输出）+ **Python safe_load HEAD vs 工作区深层对比 8/8 VALUE-IDENTICAL** + 24/24 yaml 合法 + CJK/全角标点 grep 零残留（命令有效性正向验证防假阴性）。路由 NoOne。
- **经验沉淀**：纯注释/纯配置改动验证范式 = ① git diff 非注释行过滤 ② 值层 HEAD vs 工作区 safe_load 深层对比（防"值被误改但语法合法"）③ 残留 grep 需先验证命令有效性（用已知样本正向测试）防假阴性。
- 排除：.workbuddy/memory、agent/ai-platform/configs/agents/mis-copilot/agent.yaml、AgentOpsClient.java、docs/knowledge-base-phase2-plan.md、run-tests.bat 等前序遗留（未并入）。
