# 2026-07-29

## 系统管理四项交互补全（员工/部门/角色/用户）
- 前序轮因 429 配额中断，本轮续做并收尾。
- **员工多岗位标签簇**：`types.ts` 的 `AdminColumn` 加 `tags?: boolean`；`admin-list-page.tsx` 加 `c.tags` 分支 + `TagCluster` 组件（首项填充/其余描边）；`page-defs.ts` 员工加「任职岗位」列(`tags:true`) + 样例 `posts` 数组。`npm run typecheck` 0 错误。
- **部门 hover 显隐**：确认 `tree-table.tsx` 已原生支持（`rowActions` 包 `opacity-0 group-hover:opacity-100`，`<tr>` 带 `group`），无需改。
- **角色 应用+菜单权限**：`role-list-page.tsx` 补 JSX——表格「所属应用」列、编辑表单应用下拉、菜单权限 Sheet 顶部「菜单所属应用」下拉(`reloadMenus(appId)`)；`roles.ts` 的 `createRole`/`updateRole` 扩展 `appId`。`load()` 已并行 `fetchApps()`。
- **用户 统一权限模式**：`user-list-page.tsx` 的 `openRoles` 升级为 `openPerms`(mode `'perms'`)，Sheet 内组织+部门+角色联动（`loadPermsDepts(orgId)` 解耦左侧部门树）；`users.ts` 的 `updateUser` 扩展 `orgId`/`deptId`；列表加「组织」列(colSpan 8→9)，按钮"分配角色"→"权限"。
- 文档：`ui-refactor-route.md` §16、`design-system.md` §3.4 已消费页面列表补充。
- 待办：提交并推送（前序 commit f423599 仍在本地未推送，因 GitHub 网络超时；本轮改动也待提交）。

## 交互微调（第二轮）
- **部门按钮常显**：`tree-table.tsx` 加 `actionsAlwaysVisible?: boolean`（默认 false 保留 hover，不影响其它消费者）；`dept-tree-page.tsx` 传 `actionsAlwaysVisible`，编辑/删除/子部门按钮常显。修正了上一轮"无需改"的判断——用户明确要求部门行内按钮不依赖 hover。
- **用户 组织/部门 多选**：权限 Sheet 的 `form.orgId`/`form.deptId` 单选改 `form.orgIds`/`form.deptIds` 数组 + 复选框列表；`loadPermsDepts` 改收 `orgIds[]`，并行拉各组织部门树 `flatMap` 合并，取消组织时自动剔除脏部门选中；`updateUser` 扩展 `orgIds`/`deptIds` 数组并发送。创建/编辑表单保留单部门(`form.deptId`)互不干扰。`npm run typecheck` 0 错误。
- 文档：`ui-refactor-route.md` §17。
- 注意：多选保存依赖后端支持 `orgIds`/`deptIds` 数组字段；当前 `UserView` 仅返回单 `orgId`/`deptId`，前端用 `row.orgId?[row.orgId]:[]` 兜底初始化。

## 后端接口对齐 + 员工多部门多岗位 + 部门岗位编制视图（第三轮）
- **员工多部门多岗位（前端）**：通用引擎扩展——`types.ts` 加 `AdminField.type='multiselect'` + `AdminPageDef.detailExtra`；`admin-list-page.tsx` 的 `FieldControl` 加多选胶囊渲染（首个标「主」）、`detailValue` 兼容数组、`view` 模式跳过 multiselect 字段并由 `detailExtra` 渲染标签簇；`detail-def-list.tsx` 原生渲染 `string[]`/`number[]` 为标签簇；`page-defs.ts` 员工加「兼任部门」列(`tags`)+`depts`/`posts` 多选表单 + `detailExtra`(主部门+兼任部门+任职岗位标签) + mock 多部门多岗位数据。
- **部门岗位编制视图（前端）**：`dept-tree-page.tsx` 顶部「组织架构|岗位编制」分段切换；编制模式扁平部门列表显示 `X岗/已满Y/空缺Z`，点击 Sheet 展示三卡汇总 + 各岗位任职/空缺 + 部门任职人员；mock `STAFFING` 数据。
- **后端 mis-org 对齐**：新增 `sys_employee_dept`(实体+仓库)；`EmployeeVO` 加 `deptIds`/`primaryDeptId`/`posts`(`EmployeePostVO`)；`EmployeeCreate/UpdateRequest` 加 `deptIds`/`posts`(`EmployeePostItem`)；`EmployeeService` 维护两张关联表 + `toVo` 聚合；新增 `DeptStaffingService` + `GET /internal/v1/depts/{id}/staffing`；`SysEmployeePostRepository` 加 `findByPostIdAndStatus`/`deleteByEmployeeId`。
- **后端 mis-iam 对齐**：新增 `sys_user_org`/`sys_user_dept`(实体+仓库)；`UserVO` 加 `orgIds`/`deptIds`；`UserUpdateRequest` 加 `orgIds`/`deptIds`；`UserService` 维护两张关联表 + `toVo` 聚合。
- **迁移**：`V11__employee_multi_dept_and_user_org_dept.sql` 建 `sys_employee_dept`/`sys_user_org`/`sys_user_dept`（与 V1 同源 mis_platform 库，由 mis-migrator 管理）。
- **验收**：前端 `npm run typecheck` 0 错误。后端 Java 改动沙箱 JDK8 不可编译，需 JDK17 验证；`UserController.update`/`EmployeeController` 经 JSON 反序列化自动透传新字段，未改控制器签名。已知 `EmployeeService.toVo` 列表查询有 N+1，上线前按需批量聚合优化。
- 文档：`ui-refactor-route.md` §18。
- **推送完成**：commit `c345608` 已于网络恢复后 `git push origin master` 成功（`eb77146..c345608 master -> master`）。所有第三轮改动已上远端。

## 员工任职多行展示 + 部门编制空白修复（第四轮）
- **员工任职改多行**：`types.ts` 加 `FieldType='assignments'` + `Assignment{dept,post,startDate}` + `AdminField.deptOptions/postOptions`；员工 `page-defs` 移除 `depts`/`posts` 标签列与多选表单，改为 `assignments` 字段 + `assignmentCount` 列 + 新 sample（每行一个任职记录，带 startDate）。引擎 `admin-list-page.tsx` 新增 `AssignmentEditor`(可增删行选择部门/岗位/开始时间) + `AssignmentTable`(只读子表) + `getAssignments` 容错；列表行「任职数」按钮可展开子表多行；详情 Sheet 过滤掉 `assignments` 字段并单独渲染 `AssignmentTable`；`detailExtra` 仅留主部门。`npm run typecheck` 0 错误。
- **部门编制空白根因修复**：`dept-tree-page.tsx` 旧 `STAFFING` 按硬编码部门名匹配实时部门树，名称对不上导致整页空白。改为 `EMPLOYEE_ASSIGNMENTS` 派生（部门->岗位->任职人），保证有任职的部门必有编制数据；移除 `PostStaffing.type` 字段并更新清单/Sheet。空缺=有岗位定义但无任职人（当前派生数据暂无空缺示例，后端真实接口返回空缺时正常显示）。
- 待办：本轮回退了第三轮员工 `multiselect`（depts/posts 标签）方案，改结构化任职行；后端 `EmployeeVO.posts/deptIds` 仍是对的结构，无需改。

## 员工任职/部门编制第五轮打磨（用户复审反馈）
- 用户反馈：① 编制视图仍空白；② 任职展开第一行多余「任职明细」行要去掉、表头加粗、每行加分隔线；③ 编辑任职时增加「是否主职」选择；表单里「主部门+职位」不显示或只读。
- **编制空白的真正根因**：第四轮仍用 `STAFFING[r.node.name]` 匹配后端部门树名，只显示「树里有且 STAFFING 也有」的部门；一旦后端树为空/改名就整体空白。本轮改为**直接从 `STAFFING` 派生键**生成编制部门列表（`Object.keys(STAFFING)`），彻底不依赖后端树名，凡有任职的部门必出现；Sheet 仍按 `staffingDept.name` 取 `STAFFING`（同源，必中）。
- **任职子表**：`AssignmentTable` 表头改 `font-bold`、每行 `border-t` 分隔线、多加「主职」列（主=徽标，兼职=灰字）；列表展开区**删除**原「任职明细(N)」标题行，只留子表。
- **主职标记**：`Assignment.isPrimary` 新增；`AssignmentEditor` 每行加「主」按钮（单选，标记唯一主职），新增行默认首行为主、删除后自动补主。
- **主部门/职位只读派生**：员工 `page-defs` 表单移除 `dept`(主部门 select)/`title`(职位) 字段；`detailExtra` 与 `decorate` 的 `主部门/主职位/主部门列/dept列` 全部由「主职(=isPrimary，否则首行)任职记录」派生，详情与列表自动同步。sample 全部补 `isPrimary`。
- `npm run typecheck` 0 错误；commit `184d5e7` 已推 origin/master。

## 侧栏（sidenav）两处修复（用户反馈）
- **展开宽度收窄**：`app-layout.tsx` 的 `<aside>` 展开态 `w-64`(256px) → `w-56`(224px)。用户反馈原宽度太宽。折叠态 `w-16` 不变。
- **折叠态分支点击无反应（根因+修复）**：`side-nav.tsx` 分支按钮 `onClick` 原 `if (collapsed) return;` 直接丢弃点击，且子项渲染受 `open && !collapsed` 限制，折叠时既无展开也无飞出。新增 `onExpandBranch(title)` 回调：折叠态点分支 → `setCollapsed(false)` 并 `setNavExpanded({[title]:true})`（展开侧栏并自动打开该分支）；非折叠态仍走原 `onToggleBranch`。`SideNav` props 加 `onExpandBranch?`，`AppLayout` 接 `handleExpandBranch`。
- 验收：`npm run typecheck` 0 错误。**尚未提交**（用户本轮仅报修，未要求提交；待确认是否入库/推送）。

## 侧栏折叠态改为「悬浮飞出子菜单」（用户反馈：要 rail 风格飞出，不要点击展开整条侧栏）
- 回退上一版"折叠态点分支→展开整条侧栏"的交互，改为 **hover flyout**：折叠态下鼠标移到带子菜单的分支图标上，旁边（固定定位）弹出小菜单（`w-52`，含分支标题 + 子项链接），移开 140ms 后收起；鼠标移入飞出面板取消收起（跨 6px 间隙不丢）。
- 实现：`side-nav.tsx` 内 `flyout` 状态（`{title,top,left}` 视口坐标，来自 `getBoundingClientRect`）+ `closeTimer` ref 做延迟关闭；飞出面板用 `position: fixed`（视口坐标）渲染在 `<nav>` 末尾，规避侧栏 `overflow-x-hidden`/`overflow-hidden` 裁切；飞出面板内子项 `NavLink` 点击后 `cancelClose + hideFlyout + onNavigate`。
- **保留点击兜底**：折叠态点分支仍走 `onExpandBranch`（展开整条侧栏并打开该分支），作为键盘/点击可达路径；非折叠态 `onToggleBranch` 不变。
- 颜色对齐应用其他 popover：`bg-popover` + 标题 `text-muted-foreground` + 子项 `text-foreground hover:bg-accent`（飞出面板浮在主区上方，不用 sidebar token）。
- 类型：`flyoutNode` 用 `as Extract<SystemNavNode,{kind:'branch'}>|undefined` 收窄，typecheck 0 错误。
- 本地提交（按用户"不需要推远端"偏好，未推送）。

## 补文档 + 提交（用户反馈：更新文档 提交代码）
- 设计文档 `ui-refactor-route.md` 新增 **§20「侧栏宽度收窄 + 折叠态悬浮飞出子菜单」**（20.1–20.4），记录 `w-56` 收窄、折叠态点击无反应根因与两轮迭代、hover flyout fixed 定位实现、保留 onExpandBranch 点击兜底。
- `app-layout.tsx`（宽度 `w-56` + `handleExpandBranch` 接线）此前漏提交，随文档本轮一并本地入库。`side-nav.tsx` 已在 `6e55b5d` 入库。
- 按用户偏好，本轮提交未推送。本地领先 origin/master 3 个提交。

## 表格表头统一 + 任职子表竖线分隔 + 配色方案（2026-08-01）
- **#1 表头统一**（已实施）：全站表格 thead/th 统一对齐员工管理表（`bg-muted/60 border-b backdrop-blur` + `font-bold text-muted-foreground`）。涉及 10 文件：tree-table.tsx（加 bg+bold+sticky）、user-list/role-list/log-pages/dict/org（bg-muted/40→60 + 加 z-10 backdrop-blur）、dashboard（去 uppercase tracking-wide）、module-manage-page（加 bg+bold）、list-page-skeleton（bg-muted/80→60）、admin-list-page 的 AssignmentEditor（font-medium→bold）+ AssignmentTable。
- **#2 任职子表**（已实施）：AssignmentTable 表头加 `bg-muted/60 border-b font-bold`；表体去掉 `border-t` 行间横线，改为每列 `border-l border-border/60` 竖线分隔（首列无竖线），实现"只显示列分隔线"。
- **#3 配色方案**（仅定方案，待拍板）：CSS 变量 `--muted: 214 32% 93%` hue=214 已是冷灰。推荐**方案 A（精炼冷灰）**——不改色值，只优化渲染（行线 border-border/50 减半透明、可选斑马纹 even:bg-muted/20、hover:bg-muted/30 更淡、主色/语义色不变）。方案 B（无框极简，去行线靠留白）适合数据稀疏场景但密集表难扫读。方案 C（改 CSS 变量全局冷灰）影响面大需全站回归。typecheck 0 错误。
- 本轮改动尚未提交（等用户拍板配色方案后一并入库）。
