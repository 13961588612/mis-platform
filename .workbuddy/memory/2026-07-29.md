# 2026-07-29

## 系统管理四项交互补全（员工/部门/角色/用户）
- 前序轮因 429 配额中断，本轮续做并收尾。
- **员工多岗位标签簇**：`types.ts` 的 `AdminColumn` 加 `tags?: boolean`；`admin-list-page.tsx` 加 `c.tags` 分支 + `TagCluster` 组件（首项填充/其余描边）；`page-defs.ts` 员工加「任职岗位」列(`tags:true`) + 样例 `posts` 数组。`npm run typecheck` 0 错误。
- **部门 hover 显隐**：确认 `tree-table.tsx` 已原生支持（`rowActions` 包 `opacity-0 group-hover:opacity-100`，`<tr>` 带 `group`），无需改。
- **角色 应用+菜单权限**：`role-list-page.tsx` 补 JSX——表格「所属应用」列、编辑表单应用下拉、菜单权限 Sheet 顶部「菜单所属应用」下拉(`reloadMenus(appId)`)；`roles.ts` 的 `createRole`/`updateRole` 扩展 `appId`。`load()` 已并行 `fetchApps()`。
- **用户 统一权限模式**：`user-list-page.tsx` 的 `openRoles` 升级为 `openPerms`(mode `'perms'`)，Sheet 内组织+部门+角色联动（`loadPermsDepts(orgId)` 解耦左侧部门树）；`users.ts` 的 `updateUser` 扩展 `orgId`/`deptId`；列表加「组织」列(colSpan 8→9)，按钮"分配角色"→"权限"。
- 文档：`ui-refactor-route.md` §16、`design-system.md` §3.4 已消费页面列表补充。
- 待办：提交并推送（前序 commit f423599 仍在本地未推送，因 GitHub 网络超时；本轮改动也待提交）。

## 交互微调（第二轮）
- **部门按钮常显**：`tree-table.tsx` 加 `actionsAlwaysVisible?: boolean`（默认 false 保留 hover，不影响其它消费者）；`dept-tree-page.tsx` 传 `actionsAlwaysVisible`，编辑/删除/子部门按钮常显。修正了上一轮"无需改"的判断——用户明确要求部门行内按钮不依赖 hover。
- **用户 组织/部门 多选**：权限 Sheet 的 `form.orgId`/`form.deptId` 单选改 `form.orgIds`/`form.deptIds` 数组 + 复选框列表；`loadPermsDepts` 改收 `orgIds[]`，并行拉各组织部门树 `flatMap` 合并，取消组织时自动剔除脏部门选中；`updateUser` 扩展 `orgIds`/`deptIds` 数组并发送。创建/编辑表单保留单部门(`form.deptId`)互不干扰。`npm run typecheck` 0 错误。
- 文档：`ui-refactor-route.md` §17。
- 注意：多选保存依赖后端支持 `orgIds`/`deptIds` 数组字段；当前 `UserView` 仅返回单 `orgId`/`deptId`，前端用 `row.orgId?[row.orgId]:[]` 兜底初始化。

## 后端接口对齐 + 员工多部门多岗位 + 部门岗位编制视图（第三轮）
- **员工多部门多岗位（前端）**：通用引擎扩展——`types.ts` 加 `AdminField.type='multiselect'` + `AdminPageDef.detailExtra`；`admin-list-page.tsx` 的 `FieldControl` 加多选胶囊渲染（首个标「主」）、`detailValue` 兼容数组、`view` 模式跳过 multiselect 字段并由 `detailExtra` 渲染标签簇；`detail-def-list.tsx` 原生渲染 `string[]`/`number[]` 为标签簇；`page-defs.ts` 员工加「兼任部门」列(`tags`)+`depts`/`posts` 多选表单 + `detailExtra`(主部门+兼任部门+任职岗位标签) + mock 多部门多岗位数据。
- **部门岗位编制视图（前端）**：`dept-tree-page.tsx` 顶部「组织架构|岗位编制」分段切换；编制模式扁平部门列表显示 `X岗/已满Y/空缺Z`，点击 Sheet 展示三卡汇总 + 各岗位任职/空缺 + 部门任职人员；mock `STAFFING` 数据。
- **后端 mis-org 对齐**：新增 `sys_employee_dept`(实体+仓库)；`EmployeeVO` 加 `deptIds`/`primaryDeptId`/`posts`(`EmployeePostVO`)；`EmployeeCreate/UpdateRequest` 加 `deptIds`/`posts`(`EmployeePostItem`)；`EmployeeService` 维护两张关联表 + `toVo` 聚合；新增 `DeptStaffingService` + `GET /internal/v1/depts/{id}/staffing`；`SysEmployeePostRepository` 加 `findByPostIdAndStatus`/`deleteByEmployeeId`。
- **后端 mis-iam 对齐**：新增 `sys_user_org`/`sys_user_dept`(实体+仓库)；`UserVO` 加 `orgIds`/`deptIds`；`UserUpdateRequest` 加 `orgIds`/`deptIds`；`UserService` 维护两张关联表 + `toVo` 聚合。
- **迁移**：`V11__employee_multi_dept_and_user_org_dept.sql` 建 `sys_employee_dept`/`sys_user_org`/`sys_user_dept`（与 V1 同源 mis_platform 库，由 mis-migrator 管理）。
- **验收**：前端 `npm run typecheck` 0 错误。后端 Java 改动沙箱 JDK8 不可编译，需 JDK17 验证；`UserController.update`/`EmployeeController` 经 JSON 反序列化自动透传新字段，未改控制器签名。已知 `EmployeeService.toVo` 列表查询有 N+1，上线前按需批量聚合优化。
- 文档：`ui-refactor-route.md` §18。
