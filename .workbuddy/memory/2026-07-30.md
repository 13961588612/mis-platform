# 2026-07-30 工作日志

## AI Skill 表单填充引擎 — QA 完成交付

### QA 结果
- **DagBuilder 单元测试** — 9 用例落地（空输入/单节点/无依赖/线性 DAG/多根并行/有依赖/循环依赖/自引用/无效引用/null 参数）
- **前端 typecheck** — 通过 ✅（修复 2 个 null→undefined TS2345 错误）
- **后端编译** — 跳过（系统 JDK8 ≠ JDK17，需 Docker 或内网 JDK17 验证）

### 修复记录
- `frontend/mis-admin-web/src/features/ai/hooks/useSkillFill.ts:24,75` — `null` 改为 `undefined`（TS2345）

### 整体交付状态
- 5/5 任务完成（T01-T05）
- typecheck: 0 错误
- QA 用例: 9 个
- 待后续：JDK17 编译验证、端到端联调、P1 别名表+用户学习迁后端

## FormFill × Agent 平台整合 — P0 收尾（O1/O2 修复）
- **背景**：原 FormFill 引擎（mis-admin-bff Java）与 ai-platform 平行割裂；本次整合把它暴露为 agent 可调能力（企微/H5 对话触发单据填充），打通「对话→填充→HITL→写回」。走标准 SOP：PM 增量 PRD → 架构师整合设计(6 决策+T01-T08) → 两批工程师(Java BFF 批次1 + ai-platform Py/TS 批次2) → QA 跨栈回归。
- **O2（P0 功能缺口，已修）**：`formfill_execute` 的 `success` 路径原本只回 fields 不写回单据；修复为具备单据上下文时内部自动调 `submit_formfill_apply` 写回（formfill_execute.py:134-214），新增用例 `test_formfill_execute_success_autowrites_back`。
- **O1（契约缺口，已修）**：`formfill_client.execute_skill` 原把上下文裹在 `context` 键 + 多发 `sessionId`，但 Java `SkillExecuteRequest` 仅认顶层 `pageContext`（无 context/sessionId）。Spring 忽略未知字段 → 引擎拿到空 pageContext、实体解析退化。改为发顶层 `pageContext`（含 docType/docId + 内层表单上下文）、移除 sessionId/conversationId（formfill_client.py:90-94）。commit `5da8ad1`。主理人亲核 Java 三件套（SkillExecuteRequest / AiProxyController / SkillExecutionEngine）确认契约。
- **验证**：ai-platform backend pytest **198 passed / 0 failed**（formfill 套件 23 passed）；前端/gateway typecheck 0 新错。
- **提交状态**：仅 `formfill_client.py` 已提交（5da8ad1）；批次1(Java BFF 12 文件)+批次2(ai-platform 29 文件)其余在磁盘上已实现+测试但未提交（git 中还混有 mis-org MCP / docs 等疑似并行工作，未盲提）。
- **已知风险**：Java 侧沙箱 JDK8 无法本地编译（需 JDK17/Docker）；真实 BFF↔agent 反向信任、MIS JWT 验签、企微 button_interaction 往返、Redis 会话联调均未跑（待 JDK17 + 起栈）。
- **产物文档**：`docs/ai-skill-agent-integration-prd.md`（增量 PRD）、`docs/ai-skill-agent-integration-design.md`（整合设计 6 决策 + T01-T08）。

## FormFill × Agent 整合 — Round 2 闭环（QA 严过关）
- **I1 时间差澄清**：批次2工程师 commit `5da8ad1`（formfill_client.py 发顶层 pageContext）在主理人上一轮指令下、shutdown 前已落地；QA Round 1 复核快照误判 I1 未修（引用修复前 L81-88 `context` 包装）。主理人亲核磁盘确认已修复，路由 engineer-2 为 moot。
- **新增契约防护测试**：`test_formfill_client_execute_sends_top_level_pagecontext`（tests/test_formfill.py:309-348）—— spy `_client.post` 捕获真实请求体，断言顶层 `pageContext` 存在且等于业务上下文、`context`/`sessionId`/`conversationId` 缺失、内层含 docType/docId。根除「mock 不校验请求形态」类契约 bug 漏测（Round 1 指出的根因）。
- **测试结果**：formfill 专项 **24 passed**（原 23 +1）、Python 全量 **199 passed / 0 failed**；前端/gateway tsc FormFill 相关 0 新增 error。
- **契约复核 7/7 对齐**（status 小写 / apply 字段 / Result 信封 / 反向信任头 / entity-select 双端 / resumeToken 续链 / execute 请求体形态）。路由判定 **NoOne**，Round 2 闭环。
- **最终状态**：P0 整合代码+测试全部就绪；仅 `formfill_client.py` 已提交(5da8ad1)，其余 41 源文件 + 测试文件未提交（git 混有 mis-org MCP / docs 等并行改动，待按整合范围单独 commit）。Java JDK17 编译 + 真实联调仍待做。
