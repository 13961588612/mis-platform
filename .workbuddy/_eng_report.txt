寇豆码 返工与裁决全部落地，汇报如下。

## 一、三项返工改动点（文件 + 行号）

### 返工一：T19（recordParams）+ C5-2（字段脱敏）
**`backend/mis-common/mis-common-web/.../audit/OperLog.java`**
- L19-34：新增 `recordParams()` 的 Javadoc，明确 default=false 是二进制兼容充要条件（删了存量 6 个使用方会抛 IncompleteAnnotationException），并说明默认关闭第二重意义（审计表保留期长、权限宽）。
- L35：新增 `boolean recordParams() default false;`

**`backend/mis-admin-bff/src/main/java/com/mis/adminbff/audit/OperLogAspect.java`**（全面重写）
- L50：专用 `ObjectMapper MAPPER`（不注入容器实例，避免契约漂移）。
- L61-62：`SENSITIVE_KEY_FRAGMENTS = [password, pwd, secret, token, credential]`（C5-2 黑名单）。
- L65 `MASK="***"`、L68 `MAX_STRING_LENGTH=1000`、L71 `MAX_PARAMS_LENGTH=4000`、L73-75 `RESULT_COUNT_KEYS=[hits,records,list,items,rows]`。
- L88-90：把 `Object result` 提升到 try 外供 finally 读取（用于拆 resultCount）。
- L100：`writeLog(... result)` 把 result 传入。
- L132：`body.put("requestParams", operLog.recordParams() ? collectParams(pjp, result) : null)` —— **false 时写 null，6 个存量端点零变化**（这是门禁硬条件）。
- L156-196 `collectParams()`：入参全字段平铺 + 追加 resultCount，自带 try/catch 兜底返回 null。
- L207-236 `sanitize()`：递归脱敏，**命中黑名单保留键、只换值 `***`**（用 `node.fields()` 兼容 Jackson 2.x）。
- L244-255 `isSensitiveKey()`：不区分大小写**包含**匹配（覆盖 userPassword/oldPwd/accessToken/clientSecret 等变体）。
- L258-263 `truncate()`：字符串 ≤1000。
- L271-280 `isIgnorableArg()`：跳过 HttpServletRequest/Response/MultipartFile/InputStream/OutputStream/Throwable。
- L292-322 `extractResultCount()`：Result 拆包 → Collection.size → JSON 数组 → hits/records/list/items/rows 字段。
- 切面异常仍在 finally 内 try/catch（L99-103），**绝不影响业务**。

**`backend/mis-admin-bff/.../controller/KbController.java`**
- L380：`@OperLog(module = "知识库", operation = "命中测试", recordParams = true)`。
- L355-374：Javadoc 重写，明确 API 判权由 ApiPermissionInterceptor 完成（V17 已登记）、ACL 与权限码是「两道闸门语义不同、缺一不可」；recordParams=true 理由（question 属追责证据只截断不脱敏，凭据类由切面黑名单源头屏蔽）。
- resultCount 拆包路径已验证：返回 `Result<KbHitTestResultVO>`，VO 含 `List<KbHitTestHitVO> hits`（KbHitTestResultVO.java L15），`extractResultCount` 命中 `hits` 键取到条数。

### 返工二：RetrieveQueryResolverTest 断言对象（10 处）
**`backend/mis-kb/src/test/java/com/mis/kb/domain/model/RetrieveQueryResolverTest.java`**
- L64-87：新增私有辅助 `assertResolvedWeight(double, Resolution)`，断言**原始字段** `vectorSimilarityWeight()` 非 null 且等于期望（附 Javadoc：若用 effective*() 兜底会掩盖合并器断点）。
- 10 处权重断言从 `effectiveVectorSimilarityWeight()` 改为 `assertResolvedWeight(...)` 断言原始字段：
  - L110 keywordLibraryTakesEffect(0.0D)
  - L131 hybridWeightTakesEffect(0.6D)
  - L147 vectorForcesFullWeight(1.0D)
  - L177 missingSettingsFallsBackToDefaults(DEFAULT=0.3)
  - L205 multiLibraryUsesGlobalDefaults(defaults.vectorSimilarityWeight())
  - L255 overrideBeatsLibrary(0.8D)
  - L314 hybridDegradesToVector(1.0D)
  - L385 clamps high(1.0D, high)
  - L394 clamps low(0.0D, low)
  - L439 weightOverwriteNeverTouchesPersistedSettings(1.0D)
- L458-512：新增独立嵌套组 `RecordFallback`（4 例）专门验证 record 的 `effectiveVectorSimilarityWeight()` 兜底：vector→1.0 / keyword→0.0 / hybrid→0.3(DEFAULT_VECTOR_SIMILARITY_WEIGHT=0.3)，并验证显式值不走兜底。与合并器验证分开、互不掩护。

### 返工三：hit-test 端点 API 级判权（P0 定性错误修复）
**`backend/mis-admin-bff/.../controller/KbController.java`**：Javadoc（L355-374）重写，纠正「把库级 ACL 当功能判权兜底」的定性错误。
**`backend/mis-migrator/.../db/migration/V17__kb_hittest_perms.sql`**
- L19-43：头部注释大幅扩写——越权缺口分析（ACL 语义≠功能判权 + deny-unmapped=false 使未登记端点放行）、ID/code 段位依据（9106x 全仓无占用、0090 段跳开留待全模块补登记）、**V8 schema 注意**（tenant_id/app_id 已 DROP，唯一约束改 uk_api_module_code，照抄 V2/V6 旧模板会报 column not exist，本文件已避开）。
- L64：B 段注释更新（按钮节点同时充当 D 的 sys_menu_api 挂载点）。
- L106-112 D.1：sys_api catalog `91060`（知识库工具，父 0，module 91020）。
- L114-127 D.2：sys_api api `91061`（`POST /api/v1/kb/hit-test`，带 path 去重 + EXISTS 91060）。
- L129-138 D.3：sys_menu_api `91061`（menu 91052 → api 91061，带 menu/api EXISTS 校验）。
- L140-159：迁移后自检 SQL（注册表期望 `POST|/api/v1/kb/hit-test|kb:hittest:run|1`；菜单排序 91036=6/91039=7/91037=8/91038=9；行为验收期望 403）。

## 二、裁决落地
- **裁决一**：`docs/backend/knowledge-base-phase2-plan.md` 新增 §11 技术债登记 + §11.1「文档解析失败可观测性（parse_error 列）」，写清现状（kb_document 无解析失败字段、静默丢失）+ 方案 `ALTER TABLE kb_document ADD COLUMN parse_error TEXT` + 代价与状态（📝 Wave B 候选，不阻塞发布）。
- **裁决二**：`docs/backend/api-permission-mapping.md` §3.4 重写——R6 注释补正（L95-98 说明 V17 已一并写 sys_api+sys_menu_api）、命中测试两行备注标注「已 API 级登记（V17）」、实况说明框改为「除命中测试外 KB 端点均未登记 API 层」，明确 hit-test 是当前 KB 模块唯一完成 API 级登记的端点。

## 三、Resolver 单测改造后用例数与断言对象
- **用例数**：`@Test` 方法从 **20 → 24**（+4，新增 `RecordFallback` 嵌套组 4 例）。
- **断言对象变化**：10 处权重断言由「`effectiveVectorSimilarityWeight()`（兜底读取器）」改为「原始字段 `vectorSimilarityWeight()`」（经 `assertResolvedWeight` 辅助方法，含 `assertNotNull` 防御，直接证明合并器显式产出了权重）；record 兜底行为另起一组用 `effective*()` 验证，两者分开。

## 四、重跑验证结果（如实标注）
**沙箱无法执行 Bash**（每条命令均返回固定垃圾 stdout，疑似 sandbox 限制），故以下四项在本环境未实地运行，统一标注 **待环境验收**：
- 后端 Maven 编译（mis-kb / mis-admin-bff / mis-common-web）—— 待环境验收
- 全量单测（含改造后的 RetrieveQueryResolverTest）—— 待环境验收
- **`mis-common-web` 单独回归（门禁硬条件，放行前必须跑）** —— 待环境验收
- 前端 `npm run typecheck` —— 待环境验收
我已做的**静态校验**：所有改动文件类型/导入/字段一致；Resolver 构造 RetrieveQuery 时始终传入原始 `weight`（primitive double，永不为 null），10 处 assertResolvedWeight 的 assertNotNull 必然成立；KbHitTestResultVO 含 `hits` 列表→resultCount 可提取；请求字段脱敏无冲突。

## 五、IS_PASS 结论
**IS_PASS = 待运行验收（CODE PASS / RUN PENDING）。**
- 代码实现侧：三项返工 + 两项裁决全部落地，且静态自洽性已核对，**代码侧 IS_PASS = YES**。
- 运行验证侧：四项门禁（尤其 `mis-common-web` 单独回归）因沙箱限制未实证，**不能据此给运行期 PASS**。
- 综合：代码已具备放行条件，但**放行前需在可编译环境补跑四项验证**（mis-common-web 单独回归为硬条件）后才能给最终 IS_PASS = YES。

## 六、待环境验收清单（建议在你方可编译环境逐项确认）
1. V17 真实 flyway migrate：确认 sys_api/sys_menu_api 入库，注册表查询出 `POST|/api/v1/kb/hit-test|kb:hittest:run|1`。
2. JPA 映射：mis-migrator 的 sys_api/sys_menu_api entity 与 V8 schema 对齐（无 tenant_id/app_id 多余字段）。
3. RAGFlow 抓包：确认 `/api/v1/kb/hit-test` 实下发 keyword/vector_similarity_weight/rerank_id。
4. U1：V18 补授「知识管理员/运营」角色 hit-test 权限。
5. API 级拦截：未授权登录用户直连应 403（BFF 需重启或等 300s 注册表重载）。
6. 后端编译 + 全量单测 + **mis-common-web 单独回归** + 前端 typecheck（沙箱限制未跑）。

注：全程未提交 git（硬约束）。