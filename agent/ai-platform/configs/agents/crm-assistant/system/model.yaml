# =============================================================================
# crm-assistant — LLM 模型配置
# =============================================================================

model:
  primary: deepseek-v4-flash
  fallback: deepseek-v4-pro
  strategy: default-primary
  gateway: llm-gateway
