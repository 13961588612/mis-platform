# =============================================================================
# crm-assistant — LLM 模型配置
# =============================================================================

model:
  primary: qwen3.6-plus
  fallback: deepseek-v4-flash
  strategy: default-primary
  gateway: llm-gateway
