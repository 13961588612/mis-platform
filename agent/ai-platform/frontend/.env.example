# 允许向本 H5 推送 AUTH_TOKEN 的父页 origin 白名单（逗号分隔）
# 本地管理台 Vite 默认 5174；localhost 与 127.0.0.1 都要写上
VITE_PARENT_ORIGINS=http://localhost:5174,http://127.0.0.1:5174

# 本地代理目标（见 vite.config.ts；Agent Core 默认 8000）
VITE_BACKEND_URL=http://127.0.0.1:8000
VITE_GATEWAY_URL=http://127.0.0.1:3100
